# 变更记录

本文件记录本项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.0.0] - 2026-08-15

### 新增

- 本地服务器使用系统级文件夹选择器（feat(sessions)）：当服务器运行在本机（localhost / 127.0.0.1 / ::1）时，添加工作目录会优先调用操作系统原生文件夹选择对话框（macOS / Linux / Windows）；远程服务器或无法调用原生对话框的环境，自动回退到应用内置的目录浏览器。(sessions)

- 应用图标（feat(branding)）：全新自定义图标——OpenCode 云朵以应用 accent 渐变呈现于深色圆角底板上，云内嵌入粗体 `>_` 终端提示符，替换原先的 Tauri/OpenCode 默认图标（源文件 docs/opencoder-icon.svg，设计说明 docs/icon-design.md）。(branding)

- Linux 调试构建（feat(ci)）：debug 发布渠道在 macOS、Windows 之外，新增 Linux x64 安装包（deb + AppImage）的构建与上传。(ci)

- 宠物视觉与交互优化（feat(pet)）：宠物改为透明、无边框画布，采用更接近客户端背景的角色与纸箱组合视觉，默认从屏幕左下角出现，支持鼠标拖拽移动，并移除宠物窗口内的状态和设置控件。移动模式补充窗口位置权限并缩短移动间隔，确保乱跑/底部走动实际可见。(pet)

### 修复

- 默认工作区标签遮挡修复（fix(sessions)）：工作区悬浮操作按钮现在会占用独立布局空间，不再覆盖「默认」标签，确保默认工作区名称始终清晰可见。(sessions)

- 工作区标签标题修复（fix(sessions)）：桌面侧边栏的 Sessions 标签改为 Workspaces，中文显示为「工作空间」。(sessions)

- 终端连接票据鉴权修复（fix(transport)）：终端数据通道的 connect-ticket 交换现在携带真实服务器必需的 `x-opencode-ticket: 1` 标记头——缺失时 `POST /pty/{id}/connect-token` 返回 403，导致终端一连接就失败。协议控制帧（0x00 + JSON 游标）不再作为终端输出渲染。mock 同步了该头部校验，回归会在 L3 层暴露。(TASK-M6-01)

## [1.0.0-prerelease] - 2026-08-15

### 新增

- 工作区布局重构（feat(sessions)）：工作区树现在把**默认工作区**置于最顶部——带「默认」徽标、与其余工作区以分割线分隔（读取服务器持久化的默认工作区）。头部「+ 新建会话」在默认工作区内创建；其下方新增「**+ 添加工作区**」按钮，点击弹出目录选择器，选定目录后加入工作区列表。悬浮工作区行显示操作按钮：**[+]**（在该工作区目录下新建会话）、**[⋯]**（菜单：**查看文件夹**——切换到该目录并展示其文件列表；**移除此工作区**——从列表移除）。所有用户状态关闭重开均保持：默认工作区、显式工作区列表（添加的目录即使暂无会话/项目也持续显示）、移除记录。(sessions)

- 新服务器首次进入引导设置默认工作区（feat(sessions)）：**初次添加**且无任何工作目录历史的服务器，点击进入时会弹出目录选择器（带引导文案）——选择并确认后，该目录被持久化为服务器的**默认工作区**（按服务器分别存储在 localStorage），并直接在该目录进入主页面；选择「跳过」则暂不设置，且之后进入不再反复打扰。设置 → 服务器 中可查看每个服务器的默认工作区，并随时**重新选择**（选择器会直接定位到当前默认目录）。(sessions)

- 侧边栏改为工作空间树导航（feat(sessions)）：桌面侧边栏的会话列表替换为「工作空间 → 文件夹 → 会话」三级树——每个已知工作目录渲染为可折叠文件夹（GET /session?roots=true 拉取全部目录 + GET /project 提供名称与空目录），其下只列根会话；当前目录显示实时状态点；文件夹折叠状态持久化，并支持悬浮操作（打开定位到该目录的目录选择器 / 本地隐藏该文件夹）；会话 ⋯ 菜单新增「打开文件夹」（定位到会话所在目录），「批量操作」暂为灰置占位。(sessions)

- 对话框附近新增子任务面板（feat(sessions)）：会话的任务抽屉除 Todo 列表外，还展示该会话的子代理子会话（GET /session/{id}/children，合并进会话 store 后由 SSE 持续刷新）；点击子会话可跳转。子代理子会话不再出现在侧边栏树中——本面板就是它们的新家。(sessions)

- 项目切换器支持添加工作目录（feat(sessions)）：切换菜单右上角新增 ➕，打开文件夹浏览器——从文件系统**根目录**开始（GET /file 携带 `directory` 查询参数，即真实服务器的 workspace-routing），逐级进入子文件夹；「添加目录」将所选文件夹切换为服务器工作目录（project store），触发按目录重建 SSE 流并加载该目录的会话——应用**跳转到该目录**：有会话时选中第一个，无会话时立即在该目录新建一个；同时记入切换器的最近列表（自动去重）。(sessions)

- 新建会话自动生成标题（feat(settings)）：设置 → 配置 → 全局配置新增「AI 生成标题」开关（默认开启，属**客户端偏好**——1.18.11 配置契约无此字段且拒绝未知键，因此不会写入 opencode.json）。首次对话完成后，用第一条用户消息的文本为会话命名（PATCH /session/{id}，单行化并截断至 50 字符，与 opencode TUI 一致）；每个会话只命名一次，失败静默。(settings)

- 新增独立宠物设置（feat(settings)）：宠物显示与点击穿透设置现在位于独立的「设置 → 宠物」页面，不再混在「桌面」设置中。(settings)

- 宠物自定义增强（feat(pet)）：宠物现在支持小精灵、猫咪、小狗、机器人四种形象；支持大小与透明度调节；支持固定位置、到处乱跑、只在底部走动三种移动方式；主窗口「设置 → 宠物」的修改会实时同步；宠物重新显示时会同步点击穿透状态，避免界面提示与真实鼠标状态不一致。(pet)

- 服务器首页快捷添加（feat(servers)）：移除首页大号 banner 与顶部「添加服务器」按钮；服务器卡片列表旁新增大号加号卡片，无服务器时也可直接通过该入口添加。(servers)

### 修复

- 宠物点击穿透状态修复（fix(pet)）：移除宠物右键设置菜单，并同步宠物重新显示时 Tauri 对点击事件状态的重置，确保界面上的点击穿透提示与真实状态一致。(pet)

- Markdown 源码切换修复（fix(files)）：在 Markdown 标签页右键菜单中选择「查看源文件」现在会真正把当前内容从渲染预览切换为源码视图。菜单改用共享 ContextMenu 组件，并在菜单卸载前安全捕获标签页路径。(files)

- 工作区与会话管理修复（fix(sessions)）：修复工作区/会话流程的 5 个 bug。(1) **默认工作区现在始终显示**——运行时（onboarding 或设置）选定的默认工作区通过存储变更事件立即刷新；此前 memo 缓存了首次读取的值，无法感知挂载后的写入，导致选定的默认工作区直到重新挂载才出现。(2) **新建会话（头部「+」与文件夹 [+]）在聊天面板打开新会话**——选中时主视图切回聊天；上下文重建在重新同步后恢复活动会话，即使它尚未出现在最新会话列表中也恢复（新建会话可能滞后于首次 GET /session 快照；要求列表成员会导致聊天停留在「选择会话」占位）。(3) **跨目录选会话/新建会话不再导致聊天空白**——按目录的上下文重建在重新同步期间保留当前活动会话（resetSessions 此前会清空 activeSessionId，与 selectSession 的 setActiveSession 竞态丢失选中；untrack 让捕获不进入 effect 依赖）。(4) **删除服务器时清理本地工作区/默认工作区/最近项目记忆**，重新添加同一服务器可从零开始。(5) **工作区 ⋯「查看文件夹」显示所选目录的文件并切换会话上下文**——所有文件请求（子目录展开、文件内容、git 状态）都按活动目录路由，浏览文件夹而不切换上下文会让服务器按错误工作区应答：子目录无法展开（UnknownError）、文件内容为空。文件树在子目录获取与状态请求中也显式携带浏览目录。(6) **工作区树的跨目录会话列表（GET /session?roots=true）不再继承全局活动目录注入**——此前会被收窄到当前文件夹，切换上下文后其他工作区的会话会从树中消失。(7) **树快照改为按每个已知工作区刷新**：opencode 的 GET /session 不带 directory 只返回全局（根）会话，按目录的工作区（例如服务器没有项目记录的已添加文件夹）每次刷新都会丢失会话——现在对每个显式/默认工作区按各自 ?directory= 查询。(8) **「移除工作区」对当前文件夹真正生效**：un-hide 效果只响应当前目录变化（untrack），且移除当前工作区时清空上下文让自动进入选择下一个可见文件夹——此前效果会在隐藏集合写入时重跑并立即恢复刚移除的文件夹。(sessions)

- 选中没有会话的 workspace 时自动新建会话（fix(sessions)）：在项目切换器中选中任意条目——包括**重复选中当前默认 workspace**——都会按该目录自身上下文列出会话；若为空则在该目录内自动创建并打开一个会话；已有会话时改为选中第一个；失败静默（按目录的 SSE 重新同步会自行收敛列表）。(sessions)

- 工作区树与会话修复（fix(sessions)）：解决工作区重构遗留的四个 bug。(1) **默认工作区的会话现在会渲染在行下**——此前默认文件夹只渲染了表头，数量徽标显示 N 条会话却永远无法展开查看。(2) **活动会话现在能扛过任意 store 重置**，不再只依赖重建自身的重新同步：真实服务器会重连空闲 SSE 流，每次重连的 `server.connected` 都会重置会话桶，此前即使刚创建完会话也会把聊天留在「选择会话」占位页；会话 store 现在把最后一次选中记录为恢复候选（setActiveSession 时武装，删除该会话时解除），外壳在每次重置后重新选中它——删除活动会话仍会正常清空聊天。(3) **重新同步不再用服务器的按目录 /project/current 覆盖客户端的活动目录**——对于本身不是项目根目录的文件夹，该接口返回的是**父级**工作树，曾导致 SSE 上下文抖动（重建 → 重新同步 → setCurrent → 重建），并把文件/会话请求路由到错误的工作区，在文件树展开文件夹时报「Unexpected server error」（UnknownError）；现在仅在未设置时播种目录，且新进入服务器时落在其**默认工作区**（自动进入优先取持久化的默认目录，而非最近使用的文件夹）。(4) **删除会话后其行会立即从工作区树消失**——此前树快照刷新用普通对象替换列表，而 Solid 的 setStore 会将其**合并**进现有快照，被删除的会话永远留在快照中（行一直显示到重启）；现在快照改为整体替换。(sessions)

- 工作区树可访问性修复（fix(sessions)）：会话行不再嵌套可交互控件（axe nested-interactive——行容器改为非交互，可聚焦的行按钮与 ⋯ 操作按钮互为兄弟节点，沿用 SessionList 的 M9 方案）；DesktopShell 的无障碍扫描会预置引导标记，避免首次进入的默认工作区对话框（会隐藏其后的应用内容）导致 axe 空跑。(sessions)

- 侧边栏「+ 新建会话」现在直接在当前工作目录创建会话（fix(sessions)）：新建会话流程不再被工程目录选择对话框拦截——选择文件夹改由项目切换器新增的「添加工作目录」流程完成；filepicker 对话框及其 i18n 文案已删除。(sessions)

## [1.0.0-debug.2] - 2026-08-11

### 新增

- 新建会话的工程目录 filepicker（feat(sessions)）：会话列表头部「+」按钮（及空状态按钮）不再盲目创建会话，而是打开选择对话框——输入工程目录路径，对话框会给出 OpenCode 已知工程（`GET /project`，按路径或名称不区分大小写过滤）作为提示；方向键 + 回车选择、点击即填入输入框，创建时发送 `POST /session?directory=` 使会话在该工程目录启动；输入为空则保持普通新建流程。Mock 服务器在创建会话时回显 directory 查询参数并附 self-test 覆盖。(TASK-UI-01)

- 用户可调节的界面大小（feat(settings)）：「外观」分区新增仅桌面端显示的「界面大小」滑块（90%–160%，步长 5%），通过根元素字号（rem 体系）整体缩放整个界面——字号、按钮、间距与图标按比例放大，效果等同浏览器的字体大小设置，即时生效并持久化（`oc-ui-scale`）。新的默认值 110% 让桌面端开箱即用更大的字号与更大的按钮；移动端豁免（保持 1，避免原生 Liquid Glass 标签栏与 web 内容的配合被缩放破坏）。index.html 预读脚本在首帧前设置缩放因子，启动时不会出现尺寸跳变。(settings)

### 移除

- 删除主区标签栏右上角重复的设置齿轮（refactor(settings)）：chat/files 标签栏右上角的齿轮按钮已移除——左下角 rail 齿轮（所有视图常驻）现为桌面端唯一的设置入口；⌘, 与命令面板（⌘K →「打开设置」）仍可在任意位置打开设置。(settings)

### 修复

- 新建会话文件选择器输入服务器工作区外的路径不再报服务器错误（fix(sessions)）：真实 OpenCode 服务器对工作区外的 `GET /file` 请求直接拒绝（500 "Path escapes the location" → UnknownError）。选择器打开时通过 `GET /path` 获取服务器工作区根目录，对工作区外的绝对路径跳过列表请求并显示提示——工作区内的绝对路径仍可正常浏览，回车/创建按钮仍按输入路径创建。(sessions)

- 文件树文件夹无法展开（fix(files)）：真实 OpenCode 服务器上 `GET /file` 只返回**一级**直接子项，且目录路径带系统分隔符后缀（`src/`，Windows 为 `src\`）——store 的父路径精确匹配（`src/features/` 对 `src/`）永远匹配不上，任何展开都渲染为空列表；mock 因无斜杠 fixture 掩盖了该问题。树路径现在入库时统一规范化（去除尾部分隔符、兼容两种分隔符）；mock fixture 与 `/file` 路由同步对齐真实形态（单层列表、目录尾随分隔符、工作区内绝对路径解析、工作区外路径报错），并有 self-test 覆盖——此类回归会在 L3 门禁暴露而非线上。(files)

- 新建会话文件选择器只显示历史工程（fix(sessions)）：建议数据源改为服务器**实时目录列表**——`GET /file?path=` 在输入为空时列出工作区根目录子项；输入路径时列出其父目录并按最后一段前缀（不区分大小写）过滤；点击文件夹可逐层进入（尾部斜杠补全）；文件夹可选，文件只展示不可选（会话需要目录）；Enter 仍在高亮文件夹或已输入路径上创建会话。(sessions)

- 设置页关闭按钮仅显示 ✕、设置对话框尺寸固定（fix(settings)）：关闭按钮去掉可见文字（可访问名称保留在 aria-label 上）；对话框高度不再随分区切换跳动——固定 640px 面板（小屏上限 85vh），各分区内容在自己的区域内滚动。(settings)

## [1.0.0-debug] - 2026-08-08

### 新增

- 调试 CI 发布通道（chore(ci)）：新增 `.github/workflows/debug.yml`——推送 `v*` tag 时构建**未签名**的 Windows（nsis）+ macOS（universal app+dmg）安装包，并立即发布到 GitHub Release（无草稿步骤、无需任何签名密钥：不做 Apple 证书导入；`TAURI_SIGNING_PRIVATE_KEY` 缺失时按 desktop.yml 同一兜底逻辑生成一次性 CI 更新签名密钥）。release 由 version-check job 按 tag 幂等创建，复用 release.yml 的版本同步校验与 CHANGELOG notes 脚本。这些产物仅供直接下载——其 `.sig` 来自一次性密钥，不得喂给应用内 updater。(ci)

- Mock 服务器大量对话 fixture（feat(mock)）：新增 `ses_rich_01` 会话（「Heartbeat SSE 与架构讨论（大量消息）」），提供由 `scripts/gen-rich-messages.mjs` 生成的 100+ 条消息 fixture，覆盖全部 part 类型——含代码块与表格的长 markdown 文档、思考折叠、四种状态的工具调用、file/patch/snapshot/subtask/agent/retry/compaction part——外加 40 轮填充问答以压测虚拟化滚动。其他会话保持紧凑 fixture。(mock)

- 会话子树默认折叠（feat(sessions)）：fork/子代理子树初始收起，侧边栏只显示顶层会话，点击箭头按需展开（用户展开过的父节点保持打开——默认折叠种子不会再次折叠它）。(sessions)

- 按 AI 编程聊天界面设计规范重做聊天界面（feat(chat)）：消息流现已满足规范 MUST 规则——消息流容器带 `role="log"` + `aria-live="polite"` + `aria-atomic="false"`，流式增量经 1s 防抖的屏幕阅读器播报（IA-01）；助手消息带持久的强调色 AI 标识徽章（IA-05）；语法高亮防抖 250ms，流式帧不再逐 token 重跑 Shiki（IA-09）；工具卡片状态文本为「动作词 + 工具名」（如「正在运行 bash」），running/error 状态带左侧强调色边线、错误输出等宽字体（IA-19/20/28）；patch part 暴露变更意图说明插槽（IA-15）；file part 带 mime 类型徽章与等宽路径（IA-24）。输入区在生成期间显示分阶段等待文本（<3s spinner、3–10s「正在生成回复…」、>10s「仍在生成 — 按 Esc 停止」）并置于 aria-live 区域（IA-18）；会话错误横幅遵循三段式失败格式（什么失败 + 为什么 + 重试/关闭）；待办面板新增计数摘要头（IA-21）；会话列表与聊天头部按桌面密度收紧；状态栏带 `role="status"` 与 aria-label；移动端聊天页新增玻璃拟态输入条（安全区内边距）、TabBar 与页头返回按钮触控目标 ≥44px、底部导航玻璃化（ui-design §4/§5）。双主题新增设计令牌 `--ai-label-bg`/`--ai-label-text`。设计规格见 docs/chat-redesign-spec.md。(chat-ui)

- 思考折叠在生成期间自动展开（feat(messages)）：会话开始流式输出时思考折叠自动打开、生成结束时自动收起，让思考过程像 codex / claude code 那样实时可见。任意时刻仍可手动点击折叠/展开。(messages)

- 桌面端设置入口增强 (TASK-S1-03)：侧栏（服务器切换列）底部新增齿轮按钮（位于「+」添加服务器按钮之下，以 `mt-auto` 钉在侧栏底缘），使设置在任何主视图下都一键可达——侧栏在主区视图切换之外，因此入口不再在 diff/终端/变更等隐藏主区标签栏（及其齿轮）的视图中消失。按钮复用 `setMainView("settings")` 处理器与 i18n 文案（`desktop:openSettings` / `settings:settings`）；⌘, 快捷键在任意视图仍可打开设置（新增从终端视图触发的测试覆盖）。

- 设置页提供商「可添加」入口（TASK-S1-02）：模型服务商分区新增「添加服务商」按钮，打开对话框注册动态服务商——契约没有 providers 注册表键，因此对话框通过 Config PATCH 家族写入 `provider.<id>`（ProviderConfig：显示名称 + 面向 OpenAI 兼容端点的 options.baseURL / options.apiKey），默认写入全局配置，也可选择写入项目配置（当前服务器活动项目目录），随后重拉服务商目录，使新服务商出现在服务商列表与「模型」分区中（未连接状态，可通过既有密钥表单配置密钥）。服务商 ID 必须为 slug（字母、数字、短横线与下划线）；未填写基础 URL 时仍可填写 API 密钥（将发送到服务商内置端点，仅提示不拦截）。(TASK-S1-02)

- 设置页「模型」分区与全局默认模型 (TASK-S1-01)：新增 `models` 设置分区（位于模型服务商之后），可复用模型选择器 UI（搜索 / 分组 / 收藏 / 成本徽标）为每台服务器挑选默认模型。该选择是客户端按服务器持久化的偏好（localStorage `oc-default-model:{serverId}`，models store 的 `setLocalDefault`），插入模型解析链中**高于**服务端配置默认、**低于**会话显式选择的位置——未显式选择的新会话解析到该模型，输入框 chip 仍可按会话覆盖。分区展示当前生效默认（本地选择 ?? 服务端配置默认），提供「更改」对话框与「清除」按钮（重置回服务端默认），目录加载失败时提供空态与重试；持久化的选择在目录加载时从 localStorage 水合，且不会被 `setProviders`/`setConfigDefault` 覆盖。(TASK-S1-01)

### 移除

- 聊天记录中的步骤边界标记（refactor(messages)）：不再渲染「步骤」标签与其快照 id、步骤分隔线以及「步骤完成」汇总行（结束原因、token 数、成本）——流式 part 仍保留在 store 中但不再产生任何 UI，思考区域得以精简；相关的 i18n 键一并删除。(messages)

### 修复

- 聊天消息行重叠与滚动闪烁（fix(messages)）：消息行此前由**非 keyed** 的 For 渲染，任何测量高度变化（异步 Shiki 代码块高亮在占位符被测量后撑高行）都会重建整行子树——行在占位符高度与高亮高度之间反复伸缩，形成 ResizeObserver 反馈循环，行与行互相叠压（用户气泡盖在助手文本上），消息流底部永远无法稳定（「回到底部」目标每行滞后约 110px）。行现按消息 id 做 keyed 渲染（solid-primitives `Key`），重新测量只移动 `style.top`，气泡 DOM（含已高亮的代码块）得以保留；循环消除，消息流可稳定落底。回归测试模拟 ResizeObserver 高度变化并断言行 DOM 身份不变。(messages)

- 思考内容有时不完整（fix(messages)）：历史页加载（applyMessageBatch）此前**无条件覆盖**已存在的 part——SSE 流式已累积的长思考文本会被服务器较短的快照截断（打开会话时历史页与流式竞态）。历史页现只插入缺失的 part（`overwrite=false`），已流式的文本永不被快照覆盖；`lastDeltaAt` 也只在真正变化时更新。两个回归测试覆盖「短快照不截断流式文本」与「缺失 part 正常填充」。(messages)

- 打开文件树报 400 BadRequest "Missing key at [\"path\"]"（fix(files)）：`GET /file` 的 `path` 查询参数在 openapi 中是**必填**（真实 opencode 服务器校验），而 FileTree 根加载调用 `tree()` 时不传该参数——真实服务器拒绝，mock 因静态 fixture 未暴露。根加载现显式传 `path=""`（根目录约定）；mock 的 `/file` 同步改为动态路由，缺少 path 时返回与真实服务器一致的 400，并在 self-test 中断言，防止此类客户端回归再次被宽松 fixture 掩盖。(files)

- 流式期间思考折叠无法展开（fix(messages)）：part 分发 memo 在每次 `message.part.updated` 事件整体替换 part 对象时返回全新组件元素，导致思考组件被重建、展开状态被重置——生成过程中点击展开会立刻收起。memo 的相等比较现改为对比渲染的组件类型而非元素身份，同类型 part 替换时保留已挂载的组件实例与其本地状态。该条已被下方条目取代（相等比较保留了旧元素而组件函数仍被重跑，导致流式结束后无法折叠）。(messages)

- Mock 服务商目录反映新增的服务商（fix(mock)）：新用户走查（TASK-S1-04）端到端验证了 S1-01/02/03 已实现特性——添加服务器 → Rail 齿轮进设置 → 模型分区默认模型选择（展示配置默认，选择后 chip 变「本地默认」，Clear 可用）→ 切换浅色主题（data-theme 翻转）→ 切换中文（激活态正确）→ 添加服务商对话框打开/提交（PATCH /global/config 写入 `provider.myllm`）——但添加**之后**新服务商未出现在 Providers 列表：`GET /provider` 返回的是**静态 fixture**，而真实 opencode server 会把配置中声明的 provider 加载进目录。mock 的 `GET /provider` 现改为动态处理——把 `globalConfig.provider` 条目合并进 fixture 目录（合成 Provider：`source: "config"`、env/options 取自 ProviderConfig、models 为空——未配 key 前显示「未连接」），保留 fixture 的 `default`/`connected` 记录；客户端代码零改动（添加后本就会重拉目录）。mock self-test 新增断言（新 id 出现、静态目录与 default/connected 保留）；走查结果记录于 docs/plans/settings-center.md。(TASK-S1-04)

- 思考折叠在流式结束后仍可折叠（fix(messages)）：part 分发 memo 此前缓存的是 JSX **元素**——每次 `message.part.updated` 整体替换 part 对象时，组件函数被重新执行（生成全新信号），而自定义相等比较又保留旧元素，DOM 事件绑定的还是旧信号：流式结束后点击折叠，更新的是 DOM 已不再绑定的信号（折叠失效，part 文本增量也随之错位）。memo 现改为缓存渲染的**组件类型**，经 Solid 的 `Dynamic` 挂载——props 响应式更新而实例不重建；同类型 part 替换保留已挂载组件（及其本地状态），真正的类型变化仍会切换组件。回归测试：流式中展开 → part 被替换 → 流式结束 → 折叠/展开仍可用。(messages)

- 代码块配色跟随应用主题（fix(messages)）：Shiki 高亮器此前固定 `github-dark`，浅色模式下代码块仍是深色背景、浅色 token。高亮器现同时加载 `github-dark` 与 `github-light`，按 `<html data-theme>` 选择主题（dark/oled → 深色，light → 浅色）；MarkdownText 监听主题属性，切换时重新高亮已水合的代码块。测试覆盖主题选择与切换重高亮。(messages)

- part 展开按钮不再显示 hover 背景（fix(chat)）：思考、工具、子任务、压缩与文件 part 的展开/折叠按钮移除 `hover:bg-accent-soft`——浅色模式下该悬停底色看起来像一块阴影；键盘焦点样式（`focus:bg-accent-soft`）保留。(chat-ui)

- 右键菜单 hover 高亮不再超出圆角（fix(ui)）：玻璃菜单面板使用大圆角（--r-xl）而菜单行高亮只有小圆角，鼠标滑过时高亮色块会戳出圆角之外。菜单行现按面板圆角裁剪（阴影仍在外层面板上）；主菜单与子菜单均生效。(chat-ui)

- 补丁卡文件行可展开内联差异（feat(messages)）：点击补丁卡中被修改的文件行此前毫无反应（onOpenDiff 钩子从未接线）。每行现在可以展开/收起内联差异——每张卡片只从 `GET /session/{id}/diff`（按消息过滤）拉取一次，用共享的 DiffFileGroup 渲染：带行号配色的 unified 行、文件 +增 -删 统计与状态徽章，以及加载中/失败/空内容三种状态；再次展开直接使用缓存。(messages)

- 应用外壳在所有平台固定（feat(ui)）：html/body/#root 锁定为视口尺寸（`overflow: hidden` + `overscroll-behavior: none`），所有元素统一 `overscroll-behavior: none`——滚动到边界不再回弹（iOS 橡皮筋、桌面 WebView 的 overscroll 光晕/滚动链），整个应用呈现桌面端手感而非网页。(TASK-UI-01)

- 服务器 OAuth（RFC 9728）支持 OAuth 保护的服务器（feat(servers)）：受 OAuth 资源服务器保护（参考流程为 Cloudflare Access managed OAuth）的服务器现在可从客户端授权——添加服务器探测时发现 `/.well-known/oauth-authorization-server`，授权对话框构造 PKCE 授权请求（S256 challenge + state；verifier 永不离开进程），打开系统浏览器并把粘贴的授权码交换为令牌；访问令牌以 `Authorization: Bearer` 注入每个请求（优先于 Basic 凭据），刷新令牌可按需轮换访问令牌。健康监控标记被 401/403 拒绝的探测（`authRequired`）；OAuth 服务器的卡片出现「重新认证」操作；401 或 `authRequired` 健康事件自动打开授权对话框；凭据持久化在服务器注册表中（经专用命令交换/刷新）。Mock 服务器新增发现文档、授权页与令牌端点（code + refresh 授权），并附 self-test 覆盖。(TASK-UI-01)

- 设置改为全平台模态对话框（feat(ui)）：设置中心现在以浮层形式悬浮于当前视图之上，而非替换视图——桌面端在暗色遮罩上显示居中的玻璃面板（齿轮按钮 / 命令面板 / ⌘, 均可打开；Esc、点击遮罩或 ✕ 关闭按钮均可关闭，聊天视图保持挂载于下层）；移动端设置标签打开全屏浮层覆盖当前页面（不再切换离开你的内容；Android 系统返回键优先关闭它）。页面头部按钮由「← 返回」改为「✕ 关闭」。(TASK-UI-01)

- iOS 应用启动不再崩溃（fix(mobile)）：tauri 2.11 自带的 reqwest 0.13 依赖以 `rustls-no-provider` 编译，每次 `Client::new()` 都会 panic（「未配置 rustls 加密提供方」）——应用在 iOS 上一打开就立即退出。现于 Tauri builder 运行前安装 ring 加密提供方；updater 插件（其 reqwest 0.13 同样需要该提供方，而移动分发并不使用 updater——App Store 更新机制已覆盖）改为仅桌面端注册。(mobile)

- 默认模型展示未过滤不可用的本地选择（fix(settings)）：设置页「模型」分区展示客户端本地默认时未校验其提供商仍处于连接状态、模型仍在目录中，因此可能显示「本地默认」+「新会话使用该模型」，而实际解析链（以及新会话）已回退到服务端配置默认。本地默认的展示现与解析链应用同一可用性规则（提供商已连接 + 模型在目录中），本地选择失效时回退显示配置默认与 Default 徽标；「清除」按钮仍可清除过期槽位。(TASK-S1-01)

- 发送消息后用户消息重复显示（fix(messages)）：乐观插入的 local-* 消息在收到仅含元数据的服务端回显时，会将其 part 重命名到 `prt-{echoId}` 下；但真实 opencode server 会在回显后紧接着推送该消息自己的 `message.part.updated`——重命名产物与真实 part 同时存在，导致提示文本渲染两次。现按会话记录重命名出的 part id，并在回显的真实 part 到达时删除它们，文本恰好渲染一次。(messages)

- 聊天页无法滚动到最后一行，且流式期间闪烁（fix(messages)）：自动滚动以虚拟列表的**估算**行高计算目标，而浏览器真实 `scrollHeight` 异步完成布局——跟随钉底在 rAF 执行前就消费了目标，单次 `scrollTo` 被钳制到旧高度后不再自愈，最后一行留在视口外；near-bottom 判定又用虚拟总高对比信号 scrollTop，既错过真实底部（自动跟随永久关闭）又在滚动中途误复位（把列表往用户反方向拽 = 闪烁）；`scrollTo` 还乐观写入位置信号，为 DOM 从未到达的位置渲染行（视口空白）。现跟随钉底逐帧重贴，直到真实 `scrollHeight - clientHeight` 目标稳定且 DOM 真正到位；near-bottom 判定与「新消息」跳转改用容器真实滚动度量；`scrollTo` 从浏览器实际应用的位置同步信号；失去调用方的 `scrollToIndex` 一并移除；「加载更早消息」spinner 的类名笔误（`animate-n` → `animate-spin`）一并修复。(messages)

- 会话较多时 session 列表撑破窗口边界（fix(desktop)）：桌面侧边栏中包裹 `SessionList` 的键盘焦点 wrapper div 没有任何 class，打断了 flex 高度链——列表的 `flex-1 min-h-0` 在无界 block 父级下失效，内部 `overflow-y-auto` 永远等不到溢出条件，整个侧边栏随会话数量增长突破窗口。wrapper 现为 `flex min-h-0 flex-1 flex-col`，恢复列表内部滚动。(desktop)

- 真实服务器上项目/会话列表为空（fix(api)）：ApiClient 门面只注入了当前目录，从未注入激活服务器的 `serverID`——所有经 invoke 传输的领域请求到达 Rust 时都缺少 `serverID`，被以 "missing url or serverID" 拒绝，ProjectSwitcher 吞掉错误后渲染空列表。`getApiClient()` 现从注册表 store 注入 `getServerID`；显式传入的 serverID 与基于 URL 的探测不受影响。(api)

- mDNS/SSE 在启动时崩溃（fix(transport)）：同步命令 `start_mdns_discovery` 与 `sse_subscribe` 在主线程执行、处于任何 tokio 运行时上下文之外，其裸 `tokio::spawn` 触发 "no reactor running" panic。现改为经 `tauri::async_runtime` 派生（先进入运行时上下文），与健康监控的既有修复一致；中止句柄经 `task.inner().abort_handle()` 解析。(transport)

## [1.0.0] - 2026-08-05

### 新增

- README 双语定稿（TASK-M10-05）：`README.md` 与 `README-zh.md` 重写为完整用户向版本——头部（名称、标语、CI/许可证/版本徽章、语言切换）、已实现功能清单（多服务器管理——健康检查 + mDNS 发现 + 二维码分享；桌面/移动双形态；162 端点完整 API 覆盖；流式聊天——工具/思考/待办/权限；文件、搜索、diff 与 VCS；终端；iOS 26 Liquid Glass；萌宠；en+zh 双语；主题与强调色）、服务端要求（opencode CLI ≥ v1.18.11 与精确启动命令——`opencode serve --port 4096`、`--mdns` 局域网发现、`OPENCODE_SERVER_PASSWORD` Basic Auth）、五平台安装表（macOS universal dmg / Windows nsis+msi / Linux deb+AppImage 来自 GitHub Releases、iOS 走 TestFlight/App Store、Android CI 产 APK——并如实标注脚手架待验证）、快捷键表精确对齐 `src/features/settings/shortcuts.ts` 实现清单（⌘K / ⌘N / ⌘P / ⌘⇧F / ⌘1-9 / ⌘[ / ⌘] / ⌘Enter / Esc / ⌘B / ⌘J / ⌘D / ⌘, / Tab / ↑，含自定义说明）、更新版架构图、开发指南（前置要求、完整脚本表含 `check:links`、纯浏览器 `VITE_TRANSPORT=fetch` 开发模式、iOS/Android 构建说明）与文档地图；六张真实截图落入 `docs/screenshots/`——四张桌面截图以 Playwright + E2E Tauri shim 驱动真实 UI 对接 Mock OpenCode Server、覆盖浅色/深色主题，另两张 iOS 模拟器截图（iPhone 17 / iOS 26.0 上全新拍摄的服务器主页——应用已连接宿主机上的 Mock OpenCode Server；该拍摄还暴露并修复了一个真实的启动崩溃：健康监控在 setup 钩子中直接用 `tokio::spawn` 派生任务，而 setup 运行在主线程、无 tokio 运行时上下文，导致任何带已保存服务器的应用在启动时即中止；现改为经 `tauri::async_runtime` 派生——以及归档的 M7-02 Liquid Glass 演示）；新增 Markdown 链接检查脚本 `scripts/check-links.mjs`——扫描 README.md / README-zh.md / CONTRIBUTING.md / docs 顶层全部 *.md 的链接与图片目标，跳过外部协议（http(s)/mailto/…）与纯锚点，相对路径按大小写不敏感校验（macOS 与 Linux 一致）并剥离 `#anchor` 后缀，逐个以 file:line 报告并 exit 1——暴露为 `pnpm check:links` 并接入 `pnpm verify` 新步骤「L0 links」（verify 现为 11/11）。

- 发布流水线（TASK-M10-04）：三个新脚本——`scripts/check-version.mjs <version>`（校验发布 tag 推导的版本与**三处**版本源一致——package.json / src-tauri/tauri.conf.json / src-tauri/Cargo.toml——任一不匹配即 exit 1 并列出全部不匹配项；已接入流水线并在任何构建前执行）、`scripts/release-notes.mjs <version> [--out file]`（从 CHANGELOG.md 提取 `## [X.Y.Z]` 版本段作为 Release notes，版本段尚不存在时回退 `[Unreleased]` 段——M10-06 才归集为正式版本段——两段皆无则 exit 1）与 `scripts/gen-latest-json.mjs <assets-dir> <tag>`（从 release 的 updater 产物组装 M8-09 应用内 updater 消费的 latest.json——schema `{ version, notes, pub_date, platforms: { "<os>-<arch>": { signature, url } } }`——URL 用**公开** `releases/download/<tag>/<asset>` 路径；平台映射镜像 tauri-action 的 uploadUpdaterJson——universal macOS 产物同时映射 darwin-aarch64 与 darwin-x86_64、Windows 优先 NSIS zip 其次 MSI、Linux 优先 AppImage 其次 deb，且同时写旧格式 `{os}-{arch}` 键与新格式 `{os}-{arch}-{bundle}` 键；`--require` 缺平台即失败）；新增 `.github/workflows/release.yml`——`v*` tag（或手动 workflow_dispatch，默认 dry-run：全矩阵构建但完全不触碰 GitHub Release）→ `version-check` job（解析 tag、跑 check-version.mjs、提取 notes、`gh release create --draft`）→ 构建矩阵（macOS universal app+dmg / Windows x64 nsis+msi / Linux x64 deb+AppImage，与 desktop.yml 相同的签名条件），每个 job 经 tauri-apps/tauri-action@v0 上传产物到草稿 release（按 tag 幂等——找到上面创建的草稿后不修改其 name/body，仅追加本 job 产物；`uploadUpdaterJson: false`——action 原生清单写 api.github.com 资源 URL（限速且依赖 auth），gen-latest-json.mjs 则用公开下载 URL）→ finalize job（下载 updater 产物、`--require` 四平台、上传 latest.json）；决策已记录于 docs/tasks/M10.md——Release 始终为 **draft**（发布是 M10-06 的显式收尾动作；draft 产物公开不可下载，updater 仅在发布后可用）、tag 构建缺 `TAURI_SIGNING_PRIVATE_KEY` 时快速失败（updater 签名必须匹配 app 内置公钥；一次性 CI 密钥只覆盖 dry-run，其产物绝不发布）、构建步骤与 desktop.yml 有意重复（GitHub Actions 无 composite action 时无法跨文件复用 steps）；`pnpm verify` 保持 10/10 全绿。

- 移动端发布包构建（TASK-M10-03）：iOS 模拟器 **release** 构建端到端验证通过——`pnpm tauri ios build --target aarch64-sim --ci --no-sign`（注意 `tauri ios build` 默认即 release，无 `--release` 参数，首个命令踩到并已记录）产出 `src-tauri/gen/apple/build/arm64-sim/opencoder.app`（12MB，arm64 Mach-O release 二进制，`com.opencode.opencoder` / 0.1.0 / NSCameraUsageDescription 齐备），iPhone 17 模拟器（iOS 26.0）install ✓ + launch ✓（PID 5601）+ 截图渲染正常；release 构建暴露一个桌面里程碑从未看到的**真实编译 bug**——`tauri-plugin-window-state` 2.4.1 为整 crate `#![cfg(not(any(target_os = "android", target_os = "ios")))]`（移动端编译为空 crate），而 `src-tauri/src/lib.rs` 的注册未加 `#[cfg(desktop)]` 门控（M7-01 的 iOS debug 构建早于 M8-04 window-state 落地，移动 CI 骨架的全局 continue-on-error 又一直兜底掩盖）——已按 barcode-scanner/single-instance 同一纪律加门控，并核实 updater/store 插件移动端可编译无需门控；M7-02 的「failed to rename app … Directory not empty」注记已纠偏——tauri-cli 把 archive 内的 app rename 到 `build/<target>/opencoder.app`，**残留的旧产物目录**才是阻塞点（实测仅删 archive 仍复现，`rm -rf src-tauri/gen/apple/build` 通过）；`.github/workflows/mobile.yml` 由 M7-01 骨架加固——移除全局 continue-on-error：`ios` job（simulator release，**必过**，验证链）、新增 `ios-device` job（device release unsigned——本机亦已验证：`--no-sign` 下产出 6.5MB `.ipa`，`codesign` 确认完全未签名即不可真机安装；job 级 continue-on-error——CI 只验证 aarch64-apple-ios 工具链）、`android` job（显式 `tauri android init --ci`——gen/android 未入库（本机无 Android SDK/JDK），CI 现场脚手架；随后 debug 构建**必过**（debug keystore 自动签名）+ release 构建 step 级 best-effort（无 keystore 必然失败，按规格设计如此并文档化））；`docs/mobile-signing.md` 扩充 release 章节——iOS 设备签名唯一入口是 Xcode 工程的 `DEVELOPMENT_TEAM`（M7-01 占位的 `tauri.ios.conf.json` `ios.teamId` 经 tauri 2.11.4 config schema 核实**不存在**并已更正；CI 签名走 tauri-cli 2.11 的 `IOS_CERTIFICATE` / `IOS_CERTIFICATE_PASSWORD` / `IOS_MOBILE_PROVISION` 环境变量自动配置）、release 构建模式表（模拟器 / 设备 unsigned / App Store 导出三行）、App Store Connect 上架清单（截图尺寸 1290×2796 / 1284×2778 / 1242×2208 / 2048×2732 与拍摄方法；隐私清单项——无追踪、无第三方 SDK、相机用途已由 NSCameraUsageDescription 声明，决策：仅在 Apple 工具检出 required-reason API 时再补 PrivacyInfo.xcprivacy；年龄分级问卷含用户生成内容如实勾选；隐私营养标签；先 TestFlight 内部测试）、Android keystore 路线（keytool + Gradle signingConfigs env 模板，待 gen/android 落地后应用；无 keystore 的 release 构建失败属设计如此）、权限最小化章节（仅相机用于扫码——iOS 仅 NSCameraUsageDescription、Android 仅 CAMERA 待脚手架，模板默认之外无任何权限）；docs/tasks/M10.md 记录完整落地备注与如实验收对照（真机安装 = 文档化 + CI best-effort；Android = 环境阻塞、文档化）

- 桌面三平台打包与签名（TASK-M10-02）：`src-tauri/tauri.conf.json` 的 bundle 配置落定为完整三平台集合——显式 `targets: ["app", "dmg", "nsis", "msi", "deb", "appimage"]`（各平台只构建自己的子集，Linux 不再产出 rpm）、`createUpdaterArtifacts: true`（每次构建产出签名 updater 产物——macOS 的 `opencoder.app.tar.gz` + `.sig`、Windows 安装包旁的 `.sig`，供 M8-09 updater 消费；`latest.json` 由 M10-04 发布流水线组装），以及分平台子配置——`windows.nsis`（installMode currentUser）+ `windows.wix`（msi，language en-US；注意 Tauri 2 schema 中 MSI 配置键是 `wix`、target 名仍为 `msi`）、`linux.deb.depends`（libwebkit2gtk-4.1-0 / libayatana-appindicator3-1 / librsvg2-2）、macOS 保持 hardenedRuntime 默认（签名/公证走环境变量）；`.github/workflows/desktop.yml` 由 M0-07 骨架升级为全量流水线（M10-02 接管）——矩阵 macos-14 `--target universal-apple-darwin`（app+dmg）、windows-latest（nsis+msi）、ubuntu-22.04（deb+AppImage）并补齐完整 Tauri v2 Linux 依赖（libwebkit2gtk-4.1-dev、build-essential、curl、wget、file、libxdo-dev、libssl-dev、libayatana-appindicator3-dev、librsvg2-dev、patchelf），按平台指定 rust targets，macOS 证书导入一次性 CI keychain，产物上传 artifact，条件签名——macOS 仅在 APPLE_* secrets 存在时签名+公证（否则 unsigned），`TAURI_SIGNING_PRIVATE_KEY` 缺失时以 `tauri signer generate --ci` 生成一次性 CI 密钥保证未签名验证构建全绿（其产物不得发布），Windows 默认 unsigned；新增 `docs/release-signing.md` 文档化各平台签名路线——Apple Developer ID 证书 + 公证环境变量（APPLE_CERTIFICATE / APPLE_CERTIFICATE_PASSWORD / APPLE_SIGNING_IDENTITY / APPLE_ID / APPLE_PASSWORD / APPLE_TEAM_ID）与 `spctl` / `xcrun stapler validate` 验证命令、Windows 可选 Authenticode（本地 `certificateThumbprint` 或 CI 用 `signCommand` 扩展点）、Linux 默认 unsigned + 可选 GPG，以及 updater 密钥引用（M8-09 密钥对，绝不入库）；图标核查——scaffold 图标集（32x32 / 128x128 / 128x128@2x / icon.icns / icon.ico / icon.png + Square*/StoreLogo 移动集，Tauri v2 默认图标）覆盖三平台 icns/ico/png 全部尺寸，原样保留（无 icon.svg——桌面端不要求）；本机 macOS（arm64）验证如实记录于 docs/tasks/M10.md——带密钥 `pnpm tauri build --ci --bundles app,dmg` 产出 `opencoder.app` + `opencoder_0.1.0_aarch64.dmg` + updater `.tar.gz` + `.sig`（sig key-id 与已配置 pubkey 匹配），Info.plist 为 `com.opencode.opencoder` / 0.1.0 / 10.13，首启走查 `open` 启动 bundle 内 app 且主进程持续存活（以 pkill 退出——AppleScript quit 不被处理，与 close-to-tray 设计一致）；universal 构建（`--target universal-apple-darwin`，本机约 18 min 含双架构全量编译）同样成功——`lipo -info` 确认 fat binary（x86_64 + arm64）并产出 `opencoder_0.1.0_universal.dmg`；本地构建前提已记录——rustup 工具链需补 `llvm-tools-preview` 组件（PATH 首位的 Homebrew rust 缺 rust-objcopy，icu_*/zmij 等 build script 编译失败），启用 updater 后无签名密钥的构建会快速失败；Windows / Linux 安装包按验收注明为 CI 验证；docs/tasks/M10.md 记录落地备注

- E2E 套件与覆盖率门禁（TASK-M10-01）：12 条 L4 旅程（`tests/e2e/*.spec.ts`，docs/testing.md §3 L4）以 Playwright（仅 chromium）跑在 vite dev（dev-only fetch 传输 `VITE_TRANSPORT=fetch`）+ Mock OpenCode Server（--cors）之上，双进程由 `playwright.config.ts` 的 webServer 数组自动拉起；浏览器侧 Tauri IPC shim（`tests/e2e/tauri-shim.js`，经 `addInitScript` 注入）替代应用所需的 Rust 桥——内存服务注册表、1.5s 健康监控（轮询 /global/health）、EventSource→Channel 的 SSE、客户端 PTY 回显通道、事件插件与其他命令的良性默认值——UI 在纯 Chromium 中原样运行；旅程覆盖 E01 添加服务器+健康变绿+进入项目、E02 mDNS 一键添加、E03 新建会话→发 prompt→text+tool 流式渲染→完成态、E04 中断生成→状态归零、E05 权限允许(remember)+队列排空、E06 问题回答、E07 会话搜索/重命名/删除、E08 文件树+⌘P 快速打开、E09 全文搜索跳转命中行、E10 会话 diff 视图（unified+split）、E11 终端创建→输入→回显往返、E12 断网→健康变红→重连→状态自动对齐；测试装置对 mock 的时序控制确定性化——`holdEventStream`（按住 /event 请求至旅程就绪，场景时间线自放行起播）、`useScenario`（改写 /event URL 选择 permission-flow / question-flow / sse-drop）与新增 `?syncDelay=` mock 扩展（客户端的 server.connected 全量 re-sync 会清掉同步快照往返期间到达的场景事件；shim 为每个 SSE URL 追加 `syncDelay=250`）；`pnpm test:e2e` 取代 M0-07 占位脚本，`pnpm test:coverage` 以 vitest v8 覆盖率门禁约束 `src/services/**` 与 `src/stores/**`（阈值：行/函数/语句 ≥80、分支 ≥70——实测 97.7/98.7/95.9/91.0%）；`e2e.yml` 工作流转正（仅 chromium、`playwright install --with-deps`、失败上传报告；tauri-driver 壳级用例记为环境受限——桌面打包验证归 M10-02/03）；E2E 首跑暴露真实 app bug：`src/stores/events.ts` 的 `server.connected` 处理器会重置 project store 桶，而 DesktopShell 的按目录订阅 effect 跟踪该桶——重置→重建流→新流的 server.connected→再重置，形成无限重订阅循环（约 70 次/秒）；该处理器不再重置 project 桶（syncAll 本就整体重放 project 列表与 current），循环消除且 re-sync 保持完整；结果、决策与剩余的 re-sync/事件竞态注记归档于 docs/tasks/M10.md

- 无障碍与性能终检（TASK-M9-08）：axe-core（4.12，WCAG 2.x A/AA 标签）关键屏幕扫描（`src/a11y/a11y.test.tsx`——DesktopShell、设置中心、会话列表、消息列表、输入框，断言零 critical/serious 违规）发现并修复三处真实违规——SessionList 的 `nested-interactive`（`role="button"` 行内嵌「⋯」动作按钮与树折叠按钮；行重构为非交互包装层转发点击/键盘 + 行主体改为原生 `<button>`，两个控件升为兄弟节点，`aria-current`/`aria-haspopup` 保留）、SettingsPage 分区导航的 `aria-allowed-attr`（普通按钮上的 `aria-selected` → `aria-current`）与 DesktopShell 主区视图切换条（「视图切换 + 动作按钮」混合条不是 tablist——移除 `tablist/tab` 角色、活动视图用 `aria-current`；侧栏真 tablist 保留，测试断言同步更新）；对比度走查（token 级 WCAG AA 核算表归档于新增 `docs/a11y-report.md`）修复 `src/styles/tokens.css` 四处 token：暗色 `--fg-faint` #5c6575（3.22:1）→ #788296（4.89:1，实际承载占位符/空态/提示文本）、亮色 `--fg-faint` #8b93a3（2.88:1）→ #646d7e（4.86:1）、亮色 `--accent` #4f63f5（4.42:1、accent-soft 徽标上 3.69:1）→ #3d4fd8（5.92:1 / soft 上 4.81:1）、亮色 `--success` #059669（3.52:1）→ #047857（5.12:1）——其余组合原已达标（暗色 fg-secondary 7.43:1、亮色 warning 4.68:1 / danger 4.51:1、暗色 accent 6.35:1）；index.css 新增全局 `:focus-visible` accent 焦点环（双主题 accent ≥ 3:1 非文本线）+ reduced-motion 补漏——Tailwind v4 preflight 已不含 `prefers-reduced-motion` 覆盖，`animate-spin/pulse/ping/bounce` 工具类与全部 CSS 过渡现于降级时归零（宠物/打字光标/shimmer/进度条/高亮闪烁/页面转场/Sheet 原有降级路径保持，完整清单见报告）；包体瘦身（数字归档于新增 `docs/performance.md`）：启动主包 1,368.4KB（gzip 402.5KB）→ **1,000.4KB（gzip 304.8KB，−27%）**——终端（xterm + addon-fit）经 Solid `lazy()` + Suspense 懒加载为按需 `TerminalPanel` chunk 346.2KB（gzip 86.7KB，DesktopShell 与移动端页面注册表双入口），`qrcode` 改为动态 `import("qrcode")`（两个二维码对话框 → 25.8KB 按需 chunk；shiki 原已按语言分包懒加载）；M2-09 性能基准复跑并输出数字（虚拟列表 1000×1000：19.6ms、300 条消息整表渲染：60.2ms、token delta 上屏：**0.78ms**（目标 <50ms）、store 300×4 批量：17.2ms，均远低于 CI 上限）；docs/architecture.md §9 挂上实测记录指针，首屏/安装包/内存三项环境受限指标记为豁免待测（M10 打包/e2e 环境落地后回填）；`pnpm verify` 10/10 全绿。

- 状态栏与诊断中心（TASK-M9-07）：桌面状态栏（`src/shells/desktop/DesktopShell.tsx`）在 M4-08 分支芯片旁新增三枚芯片——LSP 芯片（已连接 LSP 服务器计数；GET /lsp 挂载与每次上下文重建后拉取；`lsp.updated` SSE 事件的 properties 为空对象（已对照 EventLspUpdated 核实），故事件路由 bump 新 store（`src/stores/lsp.ts`，按服务器粒度）的版本计数器、芯片据此重拉——指示随事件实时更新）、格式化器芯片（GET /formatter 中 enabled 的格式化器名，挂载拉取一次，无对应事件）与 tokens/费用芯片（**当前会话**，直接读 session store 中服务端计算的 Session `tokens`（input+output+reasoning，cache 不计入展示）与 `cost` 字段，由 `session.updated` 事件保持新鲜——已记录决策：服务端记账为准，不做客户端估算；纯函数 `formatTokens`/`formatCost`/`usageOf` 位于 `src/shells/desktop/statusBar.ts`）；设置中心新增诊断分区（`src/features/settings/diagnostics/DiagnosticsSection.tsx`，注册于 配置 与 关于 之间，id `diagnostics`）——前端日志控制台（DesktopShell 挂载时安装应用级 `window.onerror` + console.error/warn 钩子，`src/features/settings/diagnostics/logCapture.ts`，200 条环形缓冲，全部/错误/警告 三级筛选 + 清空）、日志回传开关（持久化 `oc-diagnostics` 偏好；开启后捕获的错误/警告按 `{ service: "opencoder-webview", level, message }` 逐条 POST /log 回传——触发点：20 条批量、30s 间隔、关闭时排空；批内按 level+message 去重、失败静默丢弃，`src/features/settings/diagnostics/logForward.ts`）、服务端版本只读（健康快照）与过旧提示（M8-09 的 `installation.update-available` store：「服务端 vX — 有新版本 vY — 重启 opencode serve 生效」；`POST /global/upgrade` **刻意不提供按钮**——规格为仅展示，服务端自行升级）、已保存权限规则列表（GET /api/permission/saved → `{ data: PermissionSavedInfo[] }`，每条 `{ id, projectID, action, resource }`；两步确认后 DELETE /api/permission/saved/{id}（204），失败内联、空态与加载失败态）——V2 列表/删除方法扩展进 `src/services/permission.ts`，GET /lsp 与 GET /formatter 归入新服务 `src/services/lsp.ts`，POST /log 归入 `src/services/log.ts`；mock server 注册 lsp/formatter 声明式路由 + 动态 /log（body 校验、400）/ 已保存权限（可变列表、204 删除）/ global-upgrade（成功形状，仅契约覆盖）路由，新增 `lsp.status.json` / `formatter.json` / `permission.saved.json` fixtures 与 7 组 self-test 断言；全部文案走每语言 21 个新增 settings:* 键与 3 个 desktop:* 键（en/zh 对齐）；测试——L1（lsp/log 服务载荷组装含 L3 块、permission saved 列表/删除、lsp store、statusBar 纯函数、事件路由 lsp.updated case、logCapture 环形/钩子/过滤/还原、logForward 偏好/批量/间隔/排空/去重/幂等），L2（DesktopShell 状态栏：LSP 计数 + lsp.updated 重拉、格式化器芯片、tokens/费用芯片 + session.updated 实时更新；DiagnosticsSection：控制台预置/筛选/清空/实时捕获、回传开关关停排空 POST /log、服务端版本 + 过旧提示、规则列表/确认删除/失败/加载失败；SettingsPage 导航注册）与 L3（self-test 各端点形状断言）；契约核实结论与 tokens/费用数据源决策记录于 docs/tasks/M9.md 与 docs/api-coverage.md §5。

- MCP 服务器管理（TASK-M9-06）：MCP 领域服务（`src/services/mcp.ts`——`createMcpService(client)` 封装 GET/POST `/mcp`（状态表 / 添加，body 为 `{ name, config }`，config 为 `McpLocalConfig`（`type:"local"`、`command: string[]`）或 `McpRemoteConfig`（`type:"remote"`、`url`）联合）、POST `/mcp/{name}/connect` 与 `/disconnect`（纯 boolean）、OAuth 三件套——POST `/mcp/{name}/auth`（返回 `{ authorizationUrl, oauthState }`）、POST `/mcp/{name}/auth/callback`（body `{ code }`）、POST `/mcp/{name}/auth/authenticate` 与 DELETE `/mcp/{name}/auth`（`{ success: true }`）；已对照 1.18.11 契约核实：MCPStatus 联合共**五态**（connected / failed+error / disabled / needs_auth / needs_client_registration+error）且**无 tools 字段**——卡片渲染状态与错误详情但无工具数，`mcp.tools.changed` 事件仅携带 server 名并触发列表刷新，契约**无 `DELETE /mcp/{name}`**——按规格不做删除按钮）；设置中心新增 MCP 分区（`src/features/settings/mcp/McpSection.tsx`，注册于 模型服务商 与 服务器 之间，id `mcp`）——五态状态卡片（彩色徽标，failed / needs_client_registration 附可展开错误详情）、connect/disconnect（全局在途锁、失败内联且保留旧状态）、添加对话框（本地/远程两个 Tab：本地为命令行拆成契约的 `command: string[]` + 环境变量键值行；远程为 url + 请求头行；空键行丢弃；POST 响应直接应用并 toast），needs_auth 服务器经 `McpOAuthDialog.tsx` 走授权流（镜像 TASK-M5-07 的 provider 流——auth start → opener 插件打开浏览器 → `pollUntil` 每 2s 轮询 authenticate 的 `poll` **query 扩展**最长 60s，另常驻「粘贴授权码」输入走 callback；取消即中止轮询）；`mcp.tools.changed` SSE 事件路由进新的版本计数器 store（`src/stores/mcp.ts`），分区内 `createEffect(on(serverId:version))` 在每次 bump 时刷新列表（Solid 的 `on()` 默认 `defer:false` 首次运行即回调——测试暴露的 mount+effect 双加载 bug 已通过仅依赖该 effect 收敛）；mock server 将 MCP 族注册为动态路由（添加 → disabled、connect 可修复 failed、disconnect → disabled、OAuth 三件套 + 模拟 IdP 页面 `GET /mcp/oauth/authorize?state=`（访问即完成）、不支持 OAuth 400、未知服务器 404、固定码 `mock-oauth-code`），新增 `mcp.status.json` fixture（connected / failed+error / disabled / needs_auth）与 10 组 self-test 断言；全部文案走每语言 39 个新增 settings:* i18n 键（en/zh 对齐；对话框复用 provider OAuth 键）；测试——L1 服务载荷组装（目录透传、local/remote 添加 body、OAuth 端点、`poll` query、ApiError 透传）+ 对活 mock 的 L3 块（列表形状、添加与连接流转、自动流往返、码流、400/404、auth 移除），L2 分区套件（五态卡片 + 错误展开、connect/disconnect 刷新 + 全局锁 + 失败内联、添加对话框 local/remote 载荷与校验与行增删与失败保留、OAuth 对话框自动流/码完成/取消停轮询/启动失败、tools-changed 事件刷新、加载失败 + 重试、空态、纯函数 commandArray/rowsToRecord），另有事件路由新 case 与 SettingsPage 导航注册；契约核实结论、mock OAuth 扩展与状态流转说明记录于 docs/tasks/M9.md 与 docs/api-coverage.md §5。

- OpenCode 配置管理（TASK-M9-05）：配置领域服务（`src/services/config.ts`——`createConfigService(client)` 封装 GET/PATCH `/config` 与 `/global/config` 及 POST `/instance/dispose`；已对照 1.18.11 契约核实：Config schema 为 36 个顶层键的扁平对象、`additionalProperties: false`——**无 `theme` 键**（主题仍是客户端 oc-theme 偏好，TASK-M9-03；服务端拒绝未知键），PATCH body 按**深合并**处理（嵌套纯对象递归合并、数组/标量整体替换、未出现的键保持不变）并返回**完整更新后的 Config**，dispose 返回纯 boolean，且 1.18.11 契约内同时存在 `/instance/dispose` 与 `/global/dispose`（服务均已封装；UI 仅提供 instance 级入口））；设置中心新增配置分区（`src/features/settings/config/ConfigSection.tsx`，第十一个分区，注册于 更新 与 关于 之间，id `config`）——项目/全局两个作用域 Tab 分别加载对应端点，常用项按契约表单化（`model` 经模型 store 目录的 provider+model 双下拉，写回时用限定形式 `providerID/modelID`、读取时兼容裸 id 与限定 id；`default_agent` 经 GET /agent 目录下拉含「未设置」项；`share` manual/auto/disabled；`autoupdate` true/false/notify；`permission` ask/allow/deny——值为按工具规则对象时隐藏下拉改提示），dirty 跟踪 + Save 按钮（按需 PATCH 合并、成功刷新基线并 toast、失败内联报错且表单保留），高级 JSON 编辑器（只读美化视图 ↔ 编辑态：实时解析校验非法 JSON 阻止保存、按 schema 提示未知顶层键、合并补丁语义说明、保存失败将文本域回滚到最后保存值），危险区实例释放（确认面板说明连接的服务器实例将被关闭——连接断开、应用退回服务器列表——随后 POST /instance/dispose，成功 toast、失败内联）；mock server 将 config 族注册为动态路由（按目录隔离的项目级配置状态、深合并 PATCH、非对象 body 返回 400、全局配置往返、instance/global dispose → true），新增 `config.json` / `global.config.json` fixtures 与 7 条 self-test 断言；全部文案走每语言 51 个新增 settings:* i18n 键（en/zh 对齐）；测试——L1 服务载荷组装（目录透传、patch body、ApiError 透传）+ 对活 mock 的 L3 块，L2 分区套件（作用域切换、表单回填、dirty/保存/失败保留、模型限定写入、permission 对象提示、JSON 校验/未知键提示/保存/回滚/取消、释放确认/取消/失败、加载失败 + 重试、纯函数 modelRefOf/configModelString）与 SettingsPage 导航注册；契约核实结论与 theme/PATCH 语义说明记录于 docs/tasks/M9.md。

- 设置中心框架 (TASK-M9-04)：设置视图围绕分区注册表（`src/features/settings/sections.tsx`——有序列表 `{ id, titleKey, hintKey, icon, keywords, component }`，覆盖 通用/外观/语言/模型服务商/服务器/快捷键/桌面/通知/更新/关于 十分区）重构——注册表同时驱动桌面侧边导航、移动端横向 chip 导航（`SettingsPage` 新增 `variant: "desktop" | "mobile"` prop；`src/shells/mobile/pages.tsx` 的设置 Tab 由占位页改为渲染真实设置中心）、设置搜索（输入框按译名/提示/英文关键词过滤导航，无匹配显示空态提示，被过滤掉的活动分区仍保持渲染）与活动分区渲染（组件内状态、无 URL）；两个新分区——通用（`GeneralSection.tsx`：应用名与版本展示、文档 / AGENTS.md / GitHub 链接（opener 插件）、两步确认的「重置设置」——清除全部 oc-* localStorage 键并重新加载）与关于（`AboutSection.tsx`：应用版本、活动服务器的版本与实时状态（connection store）、MIT 协议、GitHub / 文档 / LICENSE 链接、版权行），另有服务器分区（`ServersSection.tsx`：全部已保存服务器 + 实时状态圆点、每台服务器的通知开关（oc-notifications）与主题覆盖快捷设置（oc-theme-server），列表跟随 servers-changed 事件）——全部分区可达可配；`src/stores/connection.ts` 新增 `resetConnections()`（store setter 为合并语义，重置须逐键删除）；全部文案走每语言 19 个新增 settings:* i18n 键（en/zh 对齐；移除 M9-04 占位键 `moreSections`）；L2 测试——SettingsPage 全十分区导航可达、搜索过滤/无匹配/活动分区保持、通用两步重置、关于版本与链接、服务器开关、移动 chip 变体、MobileShell 设置 Tab 接线，外加三个分区独立套件（GeneralSection/AboutSection/ServersSection），DesktopShell 设置视图断言随新默认分区更新

- 主题与强调色系统 (TASK-M9-03)：主题 store（`src/stores/theme.ts`）实现主题随服务器/全局两级记忆——全局模式（`"dark" | "light" | "system"`，localStorage `oc-theme`，默认 `system`）+ 可选每服务器覆盖（`oc-theme-server`，serverId → 模式映射；解析规则 服务器覆盖 ?? 全局，`effectiveMode`），强调色（六个预设——indigo/emerald/amber/coral/cyan/purple，`ACCENT_PRESETS`——或自定义 hex，持久化于 `oc-accent`，`accentColor` 解析、非法值回退默认靛蓝）与移动端专属 OLED 真黑开关（`oc-oled`）；`applyTheme()` 经 `matchMedia` 解析 system 模式后写入 `documentElement.dataset.theme`（dark / light / 新增 `oled` 值——仅当 OLED 开启**且**平台为移动端（`platform.kind`）时生效）并设置 `--accent` CSS 变量，`--accent-soft` 由 tokens.css 既有的 color-mix 自动派生——预设与自定义 hex 共用一条路径；**切换无闪烁**——index.html 内联预读脚本在应用 bundle 运行前读取 `oc-theme`/`oc-accent`/`oc-oled`（移动 UA 判定）并立即设置 `data-theme` + `--accent`（与 store 解析逻辑镜像），store 再经 App 的 `setThemeServer(selected()?.id)` effect 在启动时复应用——进入服务器即刻生效其覆盖、退出恢复全局；OLED 真黑配色（tokens.css 新增 `[data-theme="oled"]`——纯黑表面 + 更不透明的黑色玻璃，`color-scheme: dark`）；设置页新增外观分区（`src/features/settings/AppearanceSection.tsx`，第七个分区）——三个主题按钮（深色/浅色/跟随系统，`aria-pressed`）、OLED 开关（仅移动端渲染）、六个强调色色板 + 原生取色器与 hex 文本输入（非法 hex 忽略）、服务器覆盖子区（跟随全局 / 深色 / 浅色 / 跟随系统 + 覆盖生效时出现的清除覆盖按钮）——每项控制即时生效并经 store 持久化；全部文案走 21 个新增 settings:* i18n 键（en/zh 对齐）；L1 测试（模式解析含 matchMedia mock 与无 matchMedia 回退、强调色应用以 documentElement `--accent` 样式断言、OLED 移动端门控、服务器覆盖解析与进出服务器复应用、各字段持久化）与 L2 测试（外观分区：主题按钮应用 + 持久化、预设色板与自定义 hex 输入含畸形输入拒绝、OLED 桌面隐藏 / 移动端真黑、服务器覆盖 UI 含跟随全局 + 清除覆盖、SettingsPage 分区切换）以及**双主题快照走查**——桌面无法截图，故按验收以 L2 快照 ×2 主题落地：外观分区文档快照分别在 `data-theme="dark"` 下与切到浅色后各存一份

- 全量文案收敛与中文润色 (TASK-M9-02)：所有用户可见字符串统一走 i18n 资源——键集从 M9-01 的 143 键骨架扩展到每语言 520 键（+377；命名空间由 17 增至 19，新增 `palette` 与 `notifications`），含 52 个插值键（`{{name}}`/`{{count}}`/`{{version}}` …）与 8 组复数键，约 500 处 `t()` 调用点覆盖全部功能模块（messages/sessions/servers/files/vcs/permissions/questions/commands/models/terminal/settings/desktop/mobile …）——对话框、菜单、占位符、aria-label、错误横幅与 toast 全部随语言切换响应式重渲染；zh-CN 文案按新术语表（`docs/glossary.md`——session=会话、prompt=提示词、permission=权限请求、provider=模型服务商…）润色；URL/OAuth/CLI/⌘ 组合键等技术性 token 两种语言下原样保留，状态芯片措辞与术语表一致；错误文案归类为 i18n 键（`src/services/errors.ts` 的 `errorTitleKey` + `src/components/errorCopy.ts`——原始服务器消息作为详情行原样展示、不做翻译，与译后标题重复时省略）；硬编码扫描器（`scripts/check-hardcoded.mjs` + 白名单 `scripts/i18n-allowlist.json`）扫描 `src/**/*.tsx` 中残留的用户可见英文文案（JSX 文本与 aria-label/placeholder/title/alt 属性值；开发专用演示内容白名单放行），接入 `pnpm verify` 成为新的 L0 步骤，回归即 CI 失败；L2 中文渲染抽查套件（`src/i18n/language.zh.test.tsx`）端到端验证切换（`setLang` 后分类错误标题与功能文案渲染为中文，切回英文恢复）；M9-02 评审补刀迁移了最后一批未收敛文案（各对话框 Cancel、壳层视图标题、侧栏/右键菜单 aria-label，以及被扫描器多行盲区漏掉的 Updates 分区文案），并清理经核实无引用的骨架键（vcs:branch/noChanges、common:confirm/reset、五个 errors:* 占位键、五个 pet:* 标签、updates:install/appVersion/serverUpdate）

- i18n 基础设施与双语资源 (TASK-M9-01)：i18next 26.3.6 单例（`src/i18n/index.ts`）以内联英文 + 简体中文资源初始化，`fallbackLng: "en"`——生效语言为 localStorage `oc-lang` 手动覆盖（存在时），否则跟随系统区域（任意 `zh*` 的 `navigator.language` 映射为 `zh-CN`，其余为 `en`，导出纯函数 `detectLanguage()`）；Solid 响应式——模块级 `createSignal` 持有当前语言，`useT()` 返回的 i18next `t` 函数在**调用时**读取信号（JSX 中的 `t("ns:key")` 随切换即时重渲染）并显式传入解析出的 `lng`，`setLang("en" | "zh-CN")` 更新信号、触发 `i18next.changeLanguage` 并持久化 `oc-lang`（存储失败吞掉，desktopPrefs 纪律）；17 个命名空间全量骨架（`src/i18n/en.json` / `src/i18n/zh-CN.json`——common/servers/sessions/messages/files/vcs/permissions/questions/commands/models/terminal/settings/pet/mobile/desktop/updates/errors，各 143 个键）含代表性真实文案，覆盖插值（`{{count}}` / `{{in}}` …）与复数形式（`newMessages_one`/`newMessages_other`），en/zh 键集一致性由 CI 脚本强制；L0 键完整性检查（`scripts/check-i18n.mjs`，`pnpm check:i18n`）——扫描 `src/**/*.{ts,tsx}` 中的 `t("...")` / `t('...')` 调用点（`t(` 前非标识符字符、跳过注释行与动态键），裸键解析到 `common` 命名空间、`ns:key` 解析到对应命名空间、接受复数后缀变体，断言每个键在**两份资源**中都存在且 en/zh 键集完全一致，失败退出 1 并列出全部问题——已接入 `scripts/verify.mjs`（"L0 i18n keys" 步骤）；设置页新增 Language 分区（`src/features/settings/LanguageSection.tsx`——首个 `t()` 消费方），English / 简体中文 两个按钮以 `aria-pressed` + 强调色标示当前语言，接入 SettingsPage 导航成为第六个分区（既有硬编码文案**刻意不迁移**——属 TASK-M9-02）；L1 测试（`src/i18n/index.test.ts`——键集相等、系统区域探测与覆盖优先 + 畸形覆盖回退、`setLang` 信号与持久化、`useT` 中英文、裸键默认命名空间、插值、英文复数规则）与 L2 测试（SettingsPage 语言分区：切换 → localStorage `oc-lang` + 标题/文案随目标语言重渲染）；依赖新增 `i18next`（无 Solid 封装包——手写响应式 hook 已覆盖）

- 应用自动更新 (TASK-M8-09)：官方 tauri-plugin-updater 2.10.1 + tauri-plugin-process（crates.io + npm `@tauri-apps/plugin-updater` / `@tauri-apps/plugin-process`）注册于 `src-tauri/src/lib.rs`，capability 授权 `updater:default` + `process:default`；tauri.conf.json 的 `plugins.updater` 配置——`active: true`、endpoint 指向 GitHub releases latest.json 占位（`https://github.com/charleypeng/opencoder/releases/latest/download/latest.json`，M10-04 发布流水线产出签名产物后生效）、**真实生成的公钥**（密钥对来自 `pnpm tauri signer generate`；**私钥存于 `~/.tauri/opencoder.key`、绝不入库**——位置仅记于门面 doc 注释，首次发布前必须备份）；门面（`src/services/updates.ts`）——`getAppVersion()`（`@tauri-apps/api/app`）、`checkForUpdates()` → `Update | null`、`installAndRelaunch(update, onProgress)`（downloadAndInstall 的 Started/Progress/Finished 事件累加 → 字节数 + 比例，成功后 `relaunch()`），全部套用 events.ts outside-Tauri no-op 守卫；纯函数每日检查 `shouldAutoCheck(lastCheckMs, nowMs)` / `loadLastCheck()` / `recordLastCheck()`（localStorage `oc-updates`，desktopPrefs 纪律：畸形丢弃、失败吞掉；**先记录再检查**——失败的 endpoint 不会每次启动重试）；设置页 Updates 分区（`src/features/settings/UpdatesSection.tsx`）——应用版本只读（Tauri 外显示 “—”）、手动检查按钮（checking 转圈 → 已是最新 / 有更新 / 内联错误）、安装流程（进度条百分比，未知总量时显示字节数；Install & restart；安装失败内联报错；成功即重启）；DesktopShell 启动时每日一次自动检查（发现更新以 toast 提示去 Settings → Updates；失败静默）；服务端更新提示——`installation.update-available` SSE 事件路由进按服务器粒度的 `serverUpdate` store（`src/stores/serverUpdate.ts`，payload 已对照 1.18.11 schema 核实：`properties.version` 必填字符串），运行版本取自健康快照回填，`installation.updated` 清除提示（服务器已自行升级），DesktopShell 渲染可关闭横幅（“Server update available: vX (running vY) — restart opencode serve to apply”）并跟随活动服务器；验收——真实端到端更新闭环需要 M10-04 发布流水线产出的签名产物与 latest.json，故闭环以文档记录（docs/tasks/M8.md），UI 与流程基于禁用/模拟状态验证；L1 测试（门面 no-op 守卫/检查/下载进度/重启语义/shouldAutoCheck 矩阵/oc-updates 持久化、serverUpdate 应用/清除/重置/按服务器隔离/校验、events 路由映射 + 无版本拒绝 + installation.updated 清除）与 L2 测试（UpdatesSection 版本/检查状态/安装进度/字节数/失败、SettingsPage 分区切换、DesktopShell 横幅显示/关闭/跟随服务器）覆盖全链；docs/tasks/M8.md 记录落地备注

- 萌宠动画与编码事件联动 (TASK-M8-08)：宠物状态机（`src/features/pet/petState.ts`，ui-design §6）——纯 reducer `reducePetState(current, event, ctx)` 把 `session.status`（busy/retry/idle/error）、`permission.asked/replied`、`question.asked`、`interaction`、`transient.expired` 映射到六态（idle/working/waiting_permission/success/error/attention），**显示优先级 error > waiting_permission > working > success > attention > idle** 决定并发事实谁渲染；watcher 每次 fold **全量重述事实**（聚合会话状态 + 权限/问题队列），释放序列在**单次转发**内解析到位——中间态永不送达宠物窗口、无闪烁；success（3s）/attention（5s）为瞬态，由 watcher 定时器回退、离开瞬态即清定时器（新生成打断旧 success 定时器不误回退）；`waiting_permission` 经 `toAnimationState` 映射为动画态 `waiting`（M8-07 序列化契约零改动）；**渲染器决策——无 .riv 资产，以纯 CSS 宠物落地**（blob + 双眼，状态键帧：idle 轻浮、working 打字抖动（动画时长由强度内联驱动 950ms→400ms）、waiting 举手挥手、success 弹跳 + 眯眯眼、error 垂头 + 眼睛下垂、attention 弹跳 + 星闪（转发）/心跳（摸头）；prefers-reduced-motion 全部归零为静态宠物且保留状态特征），资源 < 500KB 天然满足，**Rive 接入点已就绪**——`PetRenderer` 接口（setState/setIntensity）即契约，资产到位后可插拔替换、状态机与联动零改动；事件联动（`src/features/pet/petEvents.ts`）——`startPetWatcher(serverId)` 按 **活动服务器** 挂载于 DesktopShell（与通知 watcher 同一 createMemo 纪律，切服销毁重建），每次变更把会话/权限/问题 store 聚合 fold 后**单次** `setPetState`（**dispose 守卫**拦截 Solid 调度器在 root 释放后的残留 flush——旧服务器状态绝不外发），另有 token 速率 effect：`message.part.delta` 经 events 路由 `bumpTokenRate()`（`src/features/pet/tokenRate.ts` 滑动 1s 窗口，惰性启动的 250ms 剪枝定时器自动衰减）→ `workingIntensity`（0-100，25 增量/秒饱和）经**新独立 `pet-intensity` 事件通道**转发（值不变不重发，不刷 IPC）；Rust 侧新增 `pet_set_intensity` 命令（clamp 0-100，记忆 `last_intensity` 并在 `pet_show` 时随状态一并重发——新窗口起步即同步），`pet_set_size` clamp 下限 120→48（`COLLAPSED_PET_SIZE`，供双击收展；设置滑杆下限仍 120）；PetShell 交互——**单击 = 摸头**（本地行为：attention + 心跳 5s 后回落到**最后转发的状态**；转发状态随时压过本地摸头；**刻意不 invoke pet_set_state**——主窗口 watcher 独占转发，自回声会污染回退锚点，与任务建议的偏差已记录）、**双击 = 收展**（收起为 48×48 tiny blob + 恢复图标，pill/齿轮/设置弹层隐藏、收起时单击与右键惰性，双击还原存储尺寸），单击带 220ms 防双击抖动（双击首击不误触发摸头）；mute 偏好按计划以 no-op 消费（CSS 宠物无声，标志留给未来音效渲染器）；L1 测试（reducer 优先级矩阵 + 瞬态生命周期 + 三条释放路径 + waiting 映射 + ctx 无关性、tokenRate 窗口滑动/自动衰减/重置 + intensity 映射含垃圾输入、watcher 完整编码流 working→success→idle + 错误/解除 + 权限等待与排空落基态 + question attention 5s 回退 + 新生成打断瞬态 + 强度转发不重发 + dispose 停止、门面 setPetIntensity 守卫/参数/事件校验、events 路由 delta 计数）与 L2 测试（PetShell blob 状态类 + 强度驱动时长 + 摸头回退 + 转发压过摸头 + 星闪/心跳区分 + 双击收展还原 + 收起惰性、DesktopShell watcher 按活动服务器挂载 + 切服重建 dispose 旧 watcher）外加 2 例新 Rust 单元测试（collapse 尺寸 clamp、强度 clamp）；docs/tasks/M8.md 记录落地备注与偏差（摸头本地化、独立强度通道、clamp 下限、mute no-op）

- 萌宠窗口基建 (TASK-M8-07)：透明无边框 always-on-top 小窗**完全由 Rust 侧创建与持有**（`src-tauri/src/pet.rs`，`#[cfg(desktop)]` 门控，与 desktop.rs 同一纪律）——`pet_show` 懒创建 WebviewWindow（label `"pet"`、url /pet、`transparent`、无边框、`always_on_top`、`skip_taskbar`、无阴影、不可缩放、160×160 物理像素，先建为不可见再 show——首次出现即已携带设置；**不抢焦点**），此后复用（hide 保留窗口、show/hide 幂等）；macOS 透明需 `tauri.conf.json` 的 `app.macOSPrivateApi: true` **且**同步开启 tauri crate 的 `macos-private-api` feature——tauri-build 校验二者一致否则构建失败（实测）；私有 API 为 App Store 审核注意事项（本项目走 GitHub releases）；10 个命令——`pet_show`/`pet_hide`/`pet_is_visible`/`pet_set_state`（主窗口前端转发动画状态，Rust 经 `emit_to` 向 pet 窗口发 `pet-state` 事件、记忆最后状态并在每次 show 时重发——新窗口从当前状态起步；`PetAnimationState` 小写序列化与前端 TS union 逐字对齐）/`pet_set_ignore_mouse`（`set_ignore_cursor_events` 点击穿透）/`pet_set_size`（120-200 clamp 并改窗）/`pet_set_opacity`/`pet_set_topmost`/`pet_set_mute`（纯存值——M8-08 音效消费；pet 窗口本地亦持久化）/`pet_set_dock`；**文档化偏差——Tauri 2 核心删除了运行时窗口透明度 API**（tauri 2.11.5 核心、tauri-runtime、JS API、capability schema 四处核实均无），`pet_set_opacity` 只存 clamp 后的 0.4-1.0，**由 PetShell 以 CSS opacity 应用在内容上**（透明背景不受影响，视觉等价）；边缘停靠——Rust `on_window_event` 的 `Moved` 监听，窗口自身 monitor 局部坐标内（多显示器安全）30px 阈值吸附 flush 到四边（纯函数 `docked_position` 全矩阵单元测试），**吸附结果先比较再 set_position**——已贴边不重入监听（无吸附循环）；前端门面（`src/services/pet.ts`——events.ts outside-Tauri no-op 守卫、camelCase 参数、`pet-state` 订阅带 payload 校验与 unlisten）；pet 窗口路由——App 查 `getCurrentWindow().label === "pet"`（internals 守卫，web 构建不触碰 window API）渲染 `src/features/pet/PetShell.tsx` 而非工作区（无路由器——窗口 label 即路由；生产环境 /pet 经 tauri 资产协议回退 index.html，tauri 2.11.5 `get_asset` 源码核实）；PetShell 为 `data-tauri-drag-region="deep"` 页面：静态 CSS 圆角 blob + 双眼占位（Rive 动画归 M8-08）+ `pet-state` 事件驱动的状态 pill，设置弹层（齿轮按钮或 **右键 blob** 打开）：尺寸滑杆 120-200 / 透明度滑杆 0.4-1.0 / 置顶·静音·停靠·点击穿透四开关 / Hide pet——每项改动即调命令即持久化到 pet 窗口**自有** localStorage（`oc-pet`，`src/features/pet/petPrefs.ts`，desktopPrefs 纪律：畸形丢弃、形状校验、失败吞掉），挂载时 `applyPetPrefs` 重放（仅存过的字段，缺省保持窗口默认）；入口——TitleBar 右侧 🐾 按钮（**App 层、全桌面屏幕可见含服务器首页**——欢迎页可关靠它兜底，ui-design §3.1 mockup 亦将 🐾 画在 TitleBar；show/hide 切换、`aria-pressed` 镜像 `pet_is_visible` 挂载值）+ Desktop 设置分区新增 **Show pet** 开关（desktopPrefs `petEnabled`，缺省 = 开；先动作后持久化、失败内联）+ DesktopShell 挂载时 `petEnabled() !== false` → `pet_show`（best-effort 吞错）；新 capability（`capabilities/pet.json`——`core:default` + `core:window:allow-start-dragging` 供注入的拖拽脚本；自定义命令不经 ACL 门控）；三平台窗口行为记录（验收项，docs/tasks/M8.md）——macOS 需私有 API 开关且无任务栏（Cmd+Tab 会列出 pet 窗口，记录为限制），Windows 原生支持透明/无边框/置顶/跳任务栏/WS_EX_TRANSPARENT 穿透，Linux 透明依赖合成器（无合成器时降级为不透明圆角小窗——即规格所述 fallback，blob 照常渲染）；L1 测试（门面 no-op 守卫/调用参数/事件校验/unlisten 生命周期、petPrefs 持久化/校验/clamp/apply 子集）与 L2 测试（PetShell 渲染 + 状态事件 + 弹层 + 滑杆/开关即改即存 + hide + 挂载重放、TitleBar 全平台含 mac 的 🐾 按钮 + 唤起/隐藏 + 可见性镜像、DesktopSection Show pet 默认/翻转/持久化/失败、DesktopShell 挂载默认唤起/显式关闭跳过、App pet-label 路由）外加 9 例 Rust 单元测试（尺寸/透明度 clamp 含 NaN/Infinity、docked_position 矩阵、状态序列化往返）；壳级 E2E（真实拖拽贴边、多显示器、焦点行为）随 M10 L4 基建落地；docs/tasks/M8.md 记录落地备注

- 系统通知中心 (TASK-M8-06)：采用官方维护的 tauri-plugin-notification 2.3.3（crates.io + npm `@tauri-apps/plugin-notification`，plugins-workspace）注册于 `src-tauri/src/lib.rs`，capability 授予 `notification:default` 并补入 `core:window:allow-set-focus`（`allow-is-focused` 已在 `core:window:default` 内）；spike 事实——桌面投递走 notify-rust（macOS 经已废弃的 NSUserNotificationCenter：**无授权弹窗**，`request_permission` 无条件 Granted、guest-js init 将 `window.Notification.permission` 置为 granted；Windows toast；Linux libnotify），`sendNotification` 即被 patch 的 `window.Notification` 构造器（落到 `plugin:notification|notify`），**桌面无点击通道**——`onAction`（`actionPerformed`）仅 iOS/Android 原生端发出、notify-rust `show()` 不注册点击回调，2.3.3 下桌面「点击通知聚焦」无法实现（记录为限制；移动端点击订阅保留给 M7 复用并接线为聚焦窗口——权限/问题 Sheet 全局呈现，窗口前台即自动弹出）；门面（`src/services/notifications.ts`）——`notify({ title, body, tag? })` 沿用 events.ts outside-Tauri no-op 守卫，**权限首次使用才请求**（模块级缓存；桌面直接 granted 不弹窗，移动端拒绝后保持拒绝直至系统设置重新开启），`isWindowFocused()`（`getCurrentWindow().isFocused()`，非 Tauri 恒 true），`focusWindow()` 与 `subscribeToNotificationClick(onClick)`（onAction → `PluginListener.unregister()` 生命周期；`tag` 仅为 API 对齐而接受——2.3.3 的 NotificationData 无 tag 字段）；通知偏好 store（`src/stores/notifications.ts`）——`{ enabled?, perServer? }` 持久化于 localStorage `oc-notifications`（desktopPrefs 纪律：畸形载荷丢弃、字段形状校验、storage 失败吞掉），**缺字段 = 开启**（`notificationsEnabled`/`serverNotificationsEnabled`/两个 setter）；设置 UI——SettingsPage 新增 Notifications 分区（`src/features/settings/NotificationsSection.tsx`）：免打扰总开关 + 每服务器开关（服务器列表经 `listServers()` 拉取，失败仅保留总开关；本地 Switch 组件命名 `ToggleSwitch` 避开 solid-js 的 `Switch` 导出冲突——JSX 解析实测）；事件接线（`src/services/notificationEvents.ts`）——三个纯映射（`generationCompleted` busy/retry→idle、`queueGainedItem` 队列新 id、`shouldNotify(focused, enabled, serverEnabled)`——**仅窗口未聚焦** + 双开关开启才通知）+ `startNotifications(serverId)` 沿用 hapticEvents 纪律（createRoot、挂载时同步基线快照——快照不是事件、删除会话基线修剪、按服务器隔离）：三个 effect 分别盯会话状态/权限队列/问题队列，命中后**按触发时刻**读取偏好与窗口聚焦再发送——完成通知 body 为会话标题（title→slug→id 回退），权限/问题通知 body 携带所属会话标题；DesktopShell 经 `createMemo` 值相等性守卫按活动服务器挂载 watcher（抑制 onMount `setActiveServer` 的重复触发——Solid 对任何信号变更都会重跑 effect、与计算结果是否相同无关）并注册点击聚焦 handler；移动端（M7）复用同一封装零改动（首次发送弹系统授权、点击事件聚焦窗口）；L1 测试（门面 no-op/权限流程含**仅请求一次**（`vi.resetModules` + 动态 import 隔离模块级缓存）/send 参数/聚焦/点击生命周期、偏好 store 持久化/校验/缺省 ON/setter 合并语义、纯映射 + watcher 含三类门禁与 dispose）与 L2 测试（NotificationsSection 渲染/开关/持久化/回显/失败、SettingsPage 分区切换、DesktopShell watcher 挂载 + 切服重建（旧 watcher dispose）+ 点击订阅聚焦窗口）；docs/tasks/M8.md 记录落地备注与桌面点击限制

- 系统托盘与全局唤起 (TASK-M8-05)：桌面模块（`src-tauri/src/desktop.rs`，`#[cfg(desktop)]` 门控——与 single-instance 同一纪律，移动端整个模块不编译；`Cargo.toml` 增 `tauri-plugin-global-shortcut` 2.3.2 与 tauri `tray-icon` + `image-png` features——`Image::from_bytes` 需 image-png，内置托盘支持无需第三方插件）——托盘图标（`TrayIconBuilder`，`icons/32x32.png`，tooltip "opencoder"）+ Rust 侧菜单（Show/Hide 切换主窗口显隐；New session 发 `tray-new-session` 事件，DesktopShell 复用 ⌘N 同一 handler；分隔符 + Quit 走 `app.exit(0)`——绕过 close-to-tray 的真实退出路径）；全局唤起快捷键（默认 "Alt+Space"——"Option+Space" 解析为同一快捷键 id，global-hotkey 大小写不敏感）在 Rust setup 经 `app.global_shortcut().on_shortcut` 注册，`Pressed` 处理器 unminimize/show/set_focus 主窗口并发 `global-summon` 事件；四个命令——`set_close_to_tray`/`get_close_to_tray`（flag 同时门控 `on_window_event` 的 `CloseRequested` 拦截：`prevent_close()` + hide 而非退出）、`set_global_shortcut`（`Shortcut::from_str` 验证 **且要求至少一个修饰键**——裸键会全局劫持打字；**先注册新键、成功后注销旧键**——被 OS 拒绝的快捷键不会砸掉现有绑定；"Alt+Space"↔"Option+Space" 按 id 幂等短路；错误字符串内联展示于设置页）、`get_global_shortcut` 与 `tray_set_badge`（待处理权限计数经 `tray.set_title` 推送——macOS 徽标、Linux 视 DE 显示图标旁文字、Windows 明确 no-op；`badge_title` 封顶 "9+"，0 清除）；前端门面（`src/services/tray.ts`——沿用 events.ts 的 outside-Tauri no-op 守卫、`DEFAULT_SUMMON_SHORTCUT`、`tray-new-session`/`global-summon` 事件订阅带 unlisten）+ 托盘角标同步（`src/services/trayBadge.ts`——纯函数 `pendingPermissionCount` 汇总所有服务器权限队列长度；`startTrayBadgeSync` 在 createRoot 下监听 store 推送计数，DesktopShell 挂载；非正数跳过 IPC——新启动的托盘不可能有徽标，代价是队列清零后徽标保留到下次启动，Rust 侧 `set_title(None)` 清除通道留作后续扩展）；设置 UI（SettingsPage 新增 Desktop 分区——`DesktopSection.tsx`：关闭到托盘开关 + 全局唤起快捷键输入，均立即应用到 Rust 并经 localStorage `oc-desktop`（`desktopPrefs.ts`）持久化，`applyDesktopPrefs` 在 DesktopShell 挂载时重放——默认快捷键跳过，因 Rust 启动时已注册）；三平台差异记录（验收项，docs/tasks/M8.md）——Linux 需要 StatusNotifier/appindicator 宿主（GNOME 默认无、需扩展；KDE Plasma 原生支持）、**必须设置菜单**图标才可见、左键点击事件不受支持、title 文字是否显示取决于 DE；Windows 忽略 title；快捷键/托盘失败均为 best-effort（记录不致命）；L1 测试（门面守卫/调用参数/事件生命周期、角标计数 + watcher dispose 语义、偏好持久化/校验/重放）与 L2 测试（DesktopSection 加载/开关/保存/失败/禁用态、SettingsPage 分区切换、DesktopShell tray-new-session 创建会话、global-summon 监听、badge 随 permission store 同步、挂载重放偏好）外加 5 例 Rust 单元测试（badge_title 三态、is_valid_shortcut 接受/拒绝矩阵）；docs/tasks/M8.md 记录落地备注

- 自定义 TitleBar 与窗口管理 (TASK-M8-04)：自定义窗口 chrome（`src/shells/desktop/TitleBar.tsx`）挂载于 App 层、覆盖每个桌面屏幕——macOS 保留原生装饰 + Overlay 标题栏样式（分平台配置 `src-tauri/tauri.macos.conf.json`：`decorations: true` + `titleBarStyle: "Overlay"` + `hiddenTitle: true`，系统红绿灯悬浮于我们的 bar 之上，左侧 78px 避让区留位、不绘制自定义按钮），Windows/Linux 无边框（`tauri.windows.conf.json` / `tauri.linux.conf.json`：`decorations: false`）并在右上角绘制自定义最小化/最大化/关闭按钮（46px 命中区，close hover 变红），经 `getCurrentWindow()` 调用 `minimize` / `toggleMaximize` / `close`，最大化/还原图标通过初始 `isMaximized` 查询 + `onResized` 监听保持真实（window-state 插件恢复最大化、Win+↑、snap 等外部变更均同步），卸载时清理监听；bar 为 Tauri 拖拽区（`data-tauri-drag-region="deep"`）——Tauri 注入的 drag 脚本原生接管窗口拖拽**与**双击最大化（`internal_toggle_maximize`；macOS 走可取消的 mouseup 路径，与系统一致），可点击元素自动阻断拖拽，故组件**不写任何 dblclick 处理**；非 Tauri 环境渲染 inert bar（`isTauri()` 守卫、不触碰 window API、无控件）；分平台配置文件按 JSON Merge Patch（RFC 7396）合并语义**整体替换**基础 `app.windows` 数组，故各文件重复完整窗口条目；窗口管理——window-state 插件（cargo `tauri-plugin-window-state`，`Builder::default()`——尺寸/位置/最大化跨启动自动保存恢复，无需 JS 侧）与 single-instance 插件（cargo `tauri-plugin-single-instance`，与移动端专属 barcode-scanner 镜像，`#[cfg(desktop)]` 注册；二次启动回调对主窗口 `unminimize` + `set_focus` 实现聚焦）；macOS 26 窗口玻璃——Rust setup 内 `#[cfg(target_os = "macos")]` 块应用 `window_vibrancy::apply_liquid_glass`（LiquidGlassOptions::default），低版本回退 `apply_vibrancy(NSVisualEffectMaterial::Sidebar)`，两调用均吞错不阻塞启动；capability 增补自定义 chrome 所需的四个写权限（`core:window:allow-start-dragging` / `allow-minimize` / `allow-toggle-maximize` / `allow-close`；is-maximized 与 internal-toggle-maximize 已在 `core:default`）；布局——App 将内容包在 TitleBar 之下（`h-dvh` flex 列；TitleBar 38px），DesktopShell 根 `h-screen` → `h-full`、ServerHome 根 `min-h-dvh` → `min-h-full` 以填满剩余空间（TitleBar 挂在 **App 层**而非计划中的 DesktopShell 内——无边框窗口在服务器首页也必须有拖拽区，记录于 docs/tasks/M8.md）；L2 测试覆盖 web 模式（拖拽区属性 + 标题、无控件、不触碰 API）、macOS 模式（红绿灯避让、无按钮、跳过 isMaximized + resize 监听——无自定义按钮则省掉每次 resize 的 IPC 往返）、Windows 模式（按钮驱动 mock window API、toggle 后图标换为 Restore）与 Linux 模式（jsdom 默认 UA），外加 App 挂载断言（桌面首页上有 titlebar、移动端无）；壳级 E2E（拖拽/最大化、状态跨重启持久化、二次启动聚焦、玻璃视觉）随 M10 L4 基建落地（记录于 docs/tasks/M8.md）；Windows 11 snap layout 悬停预览（最大化按钮 hover 弹布局网格）为文档化 backlog 项（原生贴边 snap 仍可用）

- 右键菜单系统 (TASK-M8-03)：统一复用菜单组件（`src/components/ContextMenu.tsx`）替换三处手写/kobalte 菜单并新增选中文本菜单——`ContextMenu` 以 fixed 面板渲染于 (x,y) 并 clamp 进视口（纯函数 `clampMenuPosition`/`submenuX` 决定落位，挂载后以真实测量尺寸二次校正；jsdom 报 0×0 故测试断言视口 clamp），带全屏 backdrop（点击关闭 + 抑制原生菜单）、window scroll/resize 关闭与完整键盘导航——↑/↓ 高亮移动（循环、跳过禁用行与分隔符）、Home/End 跳首/尾可选项、Enter/Space 选中、Esc 关闭（先关子菜单）、→/← 开/关子菜单；Menu 键从任意带 `onContextMenu` 的可聚焦元素免费获得（浏览器在焦点元素位置派发 contextmenu——触发契约）；`MenuItem` 支持标签/图标/右对齐快捷键提示/danger 样式/禁用行/分隔符/嵌套子菜单（hover/点击/→ 打开，贴父项右缘、溢出翻转到左缘）与 `keepOpen` 项（文件树「✓ Copied」剪贴板反馈）；四处接入：(1) SessionList——行「⋯」按钮与行右键打开**同一**菜单（Fork / Share / Move to server——置灰「Not available」占位子菜单，跨服务器拖拽迁移为 ui-design §3.2 backlog 项——/ Compress context / Generate AGENTS.md / Rename / Delete），对话框仍按目标会话挂在列表上，六个行级回调收敛为单个 `onMenu(session, position)`；(2) MessageActions——「⋯」下拉与右键弹层**统一**为共享 ContextMenu（触发器在按钮下缘打开，移动长按子集 复制文本/复制代码/删除 复用同组件，消息列加 `tabIndex={0}` + `aria-haspopup` 使 Menu 键可达）；(3) FileTree——手写弹层迁移为共享组件（同项目集，copy/reference 的「✓ Copied」反馈经 `keepOpen` 保留），DesktopShell 将 `onReference` 接线到 composer 预填 store（`@path` 进 PromptBox——M7-10 分享接收同一通道，无 hook 时保留剪贴板兜底）；(4) 选中文本菜单——DesktopShell 的 window 级 contextmenu 监听（最后冒泡，`defaultPrevented` 放行使消息/文件/会话菜单优先，且跳过打开的 `[data-context-menu]` 区域），非空选中 → Copy / Quote in chat（`quoteBlock` 将每行前缀 `> ` 组成 Markdown blockquote 进 composer 预填，PromptBox 消费时自动 focus；composer 未挂载时文本排队等待）；L1（纯函数与组件：标签/hint/图标/danger 渲染、分隔符、禁用行、视口 clamp、箭头循环导航、Home/End、Enter/Space、子菜单 hover/点击/方向键与 Esc 优先、backdrop/Esc/滚动/缩放关闭、keepOpen）与 L2（SessionList 右键 + Esc + backdrop 关闭 + 占位子菜单，MessageActions 统一菜单含移动子集，FileTree 右键菜单零回归，DesktopShell 文本菜单复制/引用/聚焦 + 门控 + 文件引用预填）测试；docs/tasks/M8.md 记录落地备注

- 快捷键体系与自定义 (TASK-M8-01)：快捷键注册中心核心（`src/features/settings/shortcuts.ts`）——`Combo` 模型（`key` + ctrl/meta/shift/alt；`ctrl` 字段即主修饰键 ⌘/Ctrl，捕获时归一化、派发时**任一**修饰键均可命中——务实的 webview 选择，使 ⌘ 与 Ctrl 在各平台等价并与注册中心之前的测试兼容；`meta` 为显式 Super/Windows 键，仅在非 mac（Super ≠ 主修饰键）平台有意义）、`parseCombo`/`formatCombo` 字符串互转（"Ctrl+Shift+P"，mac 为 "⌘+Shift+P"——仅主修饰键用 ⌘ 字形，Esc/Tab/↑/Enter/1-9/Space 均有键名映射）、`comboFromEvent` 捕获构建器（纯修饰键按下忽略、⌘ 归一化进主修饰键）、`sameCombo`/`keyOverlaps`（`switchServer` 项的 `1..9` 为键区间，与任意数字键重叠）、`findConflicts` 两两冲突检测（修饰键相同 + 键重叠，结果确定、携带共享 combo）与输入守卫目标判定（input/textarea/contenteditable）；**ui-design §3.3 全量默认表**（`DEFAULT_SHORTCUTS`：⌘K 命令面板、⌘N 新会话、⌘P 快速打开、⌘⇧F 全文搜索、⌘1-9 切换服务器、⌘[ / ⌘] 上/下一个会话、⌘Enter 发送、Esc 中断/关弹层、⌘B 侧栏开关、⌘J 终端、⌘D 会话 diff、⌘, 设置、Tab 切换 agent、↑ 上一条 prompt）带作用域（global/chat/list）与逐项 `inputGuard` 豁免（⌘Enter 与数字键必须在输入框内可触发；其余快捷键在打字时一律守卫——保留注册中心之前的既有行为）；持久化自定义 store（`src/features/settings/shortcutStore.ts`——localStorage `oc-shortcuts` 以快捷键 id 为键的覆盖表，加载时校验并丢弃畸形 combo 与未知 id，保存/单项重置/全部重置，`effectiveCombo` 自定义优先回退默认表，经模块重载测试验证重启保留）；派发 hook（`src/features/settings/useShortcuts.ts`——单个 window keydown 监听，对 overrides store 响应式（自定义 combo 即时生效），作用域门控 global 恒触发 / chat、list 仅对应聚焦区触发（`activeScope` 信号），输入守卫，`event.defaultPrevented` 放行（聚焦控件优先），preventDefault 后执行动作，卸载清理）；DesktopShell 迁移——四个临时 hook（⌘P/⌘⇧F/⌘D/⌘J）与 ⌘1..9 数字切换**原样**移入注册中心动作（newSession 复用既有 createSession 动作与 fork 错误横幅、会话上下步进按 store 顺序循环、侧栏显隐状态（`data-collapsed` aside）、设置视图），并接线作用域：按聚焦主区域设置 `activeScope`（composer 包裹层 → "chat"、会话列表包裹层 → "list"、其余 → "global"，经 `data-active-scope` 暴露供测试/调试）；⌘K（命令面板）**刻意不接线**（归 M8-02），⌘Enter / Tab / ↑ / Esc 输入本地行为留在 PromptBox 与 sheets 内——注册表列出但壳不注册，文件头注释说明；设置 UI（`src/features/settings/ShortcutsSection.tsx`——全表 + 平台化 combo 显示，点击进入捕获（捕获阶段 + stopImmediatePropagation，被捕获按键绝不会到达全局派发器）、Esc 取消、纯修饰键忽略、冲突拒绝并内联警告列出冲突项、单项 Reset + Reset all，全部持久化；`SettingsPage` 在 Providers 旁新增 Shortcuts 分区导航）；L1 测试（parse/format 往返含 "1-9"↔"1..9" 规范键、冲突矩阵含数字区间重叠与默认表零冲突、事件匹配含大小写不敏感与 1..9 区间、捕获构建器、输入守卫目标判定；store 持久化/校验/重置/重载存活；hook 按 combo 派发、preventDefault、作用域门控、输入守卫豁免、defaultPrevented 放行、自定义 combo 重接线、卸载清理）与 L2 测试（ShortcutsSection 渲染/捕获/取消/冲突命名/与区间冲突/reset-all/挂载即显示自定义/捕获阻断传播；SettingsPage 分区切换；DesktopShell：⌘N 新建、⌘B 折叠恢复、⌘[/] 循环步进、⌘, 打开设置 + Esc 保持上下文本地、自定义 combo 替换默认派发、active-scope 跟随焦点、⌘K 尚未接线）；docs/tasks/M8.md 记录落地备注

- Android 平台适配打磨 (TASK-M7-10)：系统返回键接入移动导航——调研核实（tauri 2.11.5 源码 `AppPlugin.kt`）：Tauri 2 的**内核内置** app 插件把原生返回处理门控在其**自有监听器注册表**上，该注册表只能经 `plugin:app|register_listener` 填充——即 JS 侧用 `@tauri-apps/api/app` 的 `onBackButtonPress` 注册（普通事件 `listen("back-button")` 是 Rust 侧订阅、到不了 Kotlin，web 监听不生效；无需第三方插件、无需 Kotlin override），无监听器时走 Android 原生默认；`src/services/androidBack.ts` 实现纯决策（`resolveBack`——可关闭 Sheet 优先关闭、其次活动 Tab 栈弹回、pinned Sheet/根页面均无处理）与 `startAndroidBack` 门面——以 Solid createRoot 响应式跟随上下文（Sheet 开关 + 栈深）**动态注册/注销**原生监听器：根页面时注销监听器、Android 原生返回行为（退后台）自然恢复（已核实 `plugin:app|exit` 不在 core:app ACL 中，不尝试 exit 调用）；Sheet 注册表（`src/stores/sheets.ts`）收集打开的 Sheet 及其可关闭性——PermissionSheet/QuestionSheet 以 **pinned** 注册（返回键永不关闭必须作答的请求：Sheet 打开期间不注册监听器，原生默认将应用退后台——**按键未被吞掉**；弹回与关闭均被阻塞）、ModelPicker 以可关闭注册（返回键优先关闭），MobileShell 在 Android（`supportsSystemBack`）挂载；Android 分享接收——`src/services/shareReceive.ts` 实现文档化 `share-received` window 事件契约（纯 `resolveSharePayload` 载荷校验 + 守卫的 window 监听；原生投递为 `onNewIntent` ACTION_SEND override 的 Kotlin 片段，待 gen/android 脚手架落地后应用）接入 composer 预填 store（`src/stores/composer.ts`——pending 文本跨 PromptBox 卸载存活，PromptBox 一次性消费进 textarea 并 focus）；Material 3 映射（`src/styles/tokens.css`）：色彩角色↔token 对照表（primary→`--accent`、onSurface→`--fg-primary`、surface→`--bg-elevated`、error→`--danger` 等）+ 官方 M3 emphasized 动效曲线 token `--ease-emphasized`（cubic-bezier(0.2, 0, 0, 1)，经 material-web v0.192 token 源码核实）作 Android 原生动效别名；沉浸式状态栏/导航栏（WindowInsets）Kotlin 片段文档化于 docs/tasks/M7.md（待脚手架落地）；L1（androidBack 决策表 + 动态注册生命周期 + 事件分发、shareReceive 载荷/守卫/监听、sheets 注册表、composer store）与 L2（MobileShell：返回键弹回且回根注销监听、可关闭 Sheet 优先于弹回、pinned 权限 Sheet 打开即注销监听、share 事件落入 composer store；PromptBox：预填一次性应用 + 挂载前预填生效）测试；docs/tasks/M7.md 记录落地备注、Kotlin 片段与如实 L5 checklist（真机项全部阻塞于本机无 Android SDK，见 M7-01）

- 移动端文件与终端适配 (TASK-M7-09)：文件树移动版（`src/features/files/FileTree.tsx` 新增 `variant: "desktop" | "mobile"`）——单层目录浏览 + **整行触控目标**（min-h-11，无 hover），点目录进入（经共享 files store 懒取并嫁接子树，watcher 刷新后 expanded 目录自动 refill 保持子树）、点文件打开；顶部**面包屑返回条**（Workspace 根 + 路径段，祖先段一键跳回，当前段为标签）取代桌面缩进树；右键菜单移动形态不挂载（长按菜单按规格留给 M8）；文件 Tab（`src/shells/mobile/FilesPage.tsx`）为树页面，点文件 `openTab`（原样复用 viewer store）后推入 FileView 页面（`FileViewPage.tsx`——Back 头 + 共享 FileViewer 的 `fullscreen` 形态；`routeKey` 增加 `path` 参数维度，连续打开不同文件正确重挂载）；FileViewer 全屏形态启用移动能力——可平移触摸滚动（`touch-action: pan-x pan-y`）与**双击代码缩放 100%/150%**（CSS `transform: scale(1.5)` + `transform-origin: top left`，tab 栏有镜像状态 chip，切文件复位 100%）——规格中真双指 pinch 按任务指引以双击简化并文档化（L5 待确认）；移动终端 Tab（`TerminalPage.tsx`）渲染既有 TerminalPanel 的 `variant="mobile"`——多 Tab 终端 + **辅助键条**（`src/features/terminal/TerminalKeyStrip.tsx`，仅移动渲染）：Esc（`\x1b`）/Tab（`\t`）/`|`/方向键（CSI 码）+ 粘性 Ctrl 模式（露出一次性控制字母行 A/C/D/E/K/U/W/Z → 对应 ASCII 控制字节，如 Ctrl+C 中断——「Ctrl 前缀模式」的文档化简化）；`TerminalInstance` 暴露 `onApi: { sendInput }`（本地回显 + PTY 通道发送；exited 实例可用、dispose 后 no-op）并新增移动形态**双击字号 13px↔16px**（refit 后经 REST resize 通道同步行列数）；键条按键路由到**激活 Tab** 的实例；终端页面根带 `landscape-terminal` 类并细化为真全屏——100dvh、z-index 盖住底部 web 导航、横屏隐藏页头（iOS 原生玻璃 bar 悬浮其上保持可点）；L2 测试覆盖移动树（面包屑/进目录/已加载目录不重取/面包屑跳转/触控行/无右键/目录加载与空文件夹态）、查看器缩放（门控/切换/transform/touch-action/切文件复位）、Files Tab 推入流转（树→查看→返回状态保持）、键条（序列/一次性 Ctrl 字节）、实例 API 与字号缩放、面板路由与页面级终端流转；docs/tasks/M7.md 记录 L5 checklist——真机项待 iOS 重建（Android 仍阻塞于本机无 SDK，见 M7-01）

- 扫码添加服务器 (TASK-M7-08)：采用官方维护的 tauri-plugin-barcode-scanner 2.4.5（crates.io + npm `@tauri-apps/plugin-barcode-scanner`，plugins-workspace；iOS AVFoundation + Android ML Kit）接入移动形态——该 crate 以 `#![cfg(mobile)]` 声明（桌面端编译为空 crate），因此 `src-tauri/src/lib.rs` 在 `#[cfg(mobile)]` 守卫下注册、capabilities 授予 `barcode-scanner:default` 权限集；iOS `Info.plist`（及其 XcodeGen 源 `project.yml`）补充 `NSCameraUsageDescription`；扫码门面（`src/services/scanner.ts`）沿用 haptics 双重 no-op 纪律——`canScan()`（Tauri 且移动平台）与一次性 `scanQrCode()`（返回解码文本），非 Tauri/桌面一律拒绝调用；连接 URL 编解码（`src/features/servers/qrConnect.ts`，纯函数）：`encodeConnectUrl` 生成 `opencode://connect?url=…&name=…`（只含 url + name，**绝不编码凭据**；parse 侧容忍可选 `token`），`parseConnectUrl` 为其严格逆运算（仅接受 `opencode://connect` scheme+host、无 scheme 的 url 与表单一致做规范化、缺失/非法字段拒绝）；移动端入口：Add Server 向导新增可选 `scanEnabled` prop（ServerHome 在 `platform.kind === "mobile"` 时传入），URL 输入框旁渲染「Scan QR」按钮——扫码预填 name+url 并自动触发探测（规格：扫码→预填→探测→保存），非连接码/相机失败均有内联错误，编辑模式永不显示；桌面端出口：服务器卡片右键/菜单按钮菜单新增「Show QR code」项，打开 `src/features/servers/ServerQrDialog.tsx`——Kobalte 对话框含只读连接 URL、复制按钮（Clipboard API + execCommand 兜底，「Copied!」反馈）、二维码图（`qrcode` 包 160px，沿用 ShareSessionDialog 惯例）与「二维码仅含 URL 与名称、绝不含凭据；扫码设备需能访问该服务器」提示；L1（编解码：encode/往返/不含凭据、编码字符、畸形/缺字段/错误 scheme 拒绝、token 透传；门面：双重 no-op 守卫 + 调用参数 + 失败传播）与 L2（AddServer：按钮门控含编辑模式排除、扫码→预填+自动探测、非连接码拒绝、相机失败；ServerHome：菜单→对话框 QR 图、载荷不含凭据、复制反馈、Esc 关闭、移动平台扫码按钮接线）测试；docs/tasks/M7.md 记录落地备注与 L5 checklist——真机扫码验证待 iOS 重建（Android 阻塞于本机无 SDK，见 M7-01）

- 扫码添加服务器 (TASK-M7-08)：采用官方维护的 tauri-plugin-barcode-scanner 2.4.5（crates.io + npm `@tauri-apps/plugin-barcode-scanner`，plugins-workspace；iOS AVFoundation + Android ML Kit）接入移动形态——该 crate 以 `#![cfg(mobile)]` 声明（桌面端编译为空 crate），因此 `src-tauri/src/lib.rs` 在 `#[cfg(mobile)]` 守卫下注册、capabilities 授予 `barcode-scanner:default` 权限集；iOS `Info.plist`（及其 XcodeGen 源 `project.yml`）补充 `NSCameraUsageDescription`；扫码门面（`src/services/scanner.ts`）沿用 haptics 双重 no-op 纪律——`canScan()`（Tauri 且移动平台）与一次性 `scanQrCode()`（返回解码文本），非 Tauri/桌面一律拒绝调用；连接 URL 编解码（`src/features/servers/qrConnect.ts`，纯函数）：`encodeConnectUrl` 生成 `opencode://connect?url=…&name=…`（只含 url + name，**绝不编码凭据**；parse 侧容忍可选 `token`），`parseConnectUrl` 为其严格逆运算（仅接受 `opencode://connect` scheme+host、无 scheme 的 url 与表单一致做规范化、缺失/非法字段拒绝）；移动端入口：Add Server 向导新增可选 `scanEnabled` prop（ServerHome 在 `platform.kind === "mobile"` 时传入），URL 输入框旁渲染「Scan QR」按钮——扫码预填 name+url 并自动触发探测（规格：扫码→预填→探测→保存），非连接码/相机失败均有内联错误，编辑模式永不显示；桌面端出口：服务器卡片右键/菜单按钮菜单新增「Show QR code」项，打开 `src/features/servers/ServerQrDialog.tsx`——Kobalte 对话框含只读连接 URL、复制按钮（Clipboard API + execCommand 兜底，「Copied!」反馈）、二维码图（`qrcode` 包 160px，沿用 ShareSessionDialog 惯例）与「二维码仅含 URL 与名称、绝不含凭据；扫码设备需能访问该服务器」提示；L1（编解码：encode/往返/不含凭据、编码字符、畸形/缺字段/错误 scheme 拒绝、token 透传；门面：双重 no-op 守卫 + 调用参数 + 失败传播）与 L2（AddServer：按钮门控含编辑模式排除、扫码→预填+自动探测、非连接码拒绝、相机失败；ServerHome：菜单→对话框 QR 图、载荷不含凭据、复制反馈、Esc 关闭、移动平台扫码按钮接线）测试；docs/tasks/M7.md 记录落地备注与 L5 checklist——真机扫码验证待 iOS 重建（Android 阻塞于本机无 SDK，见 M7-01）

- 触觉反馈与高级动效 (TASK-M7-07)：采用官方维护的 tauri-plugin-haptics 2.3.2（crates.io + npm `@tauri-apps/plugin-haptics`，plugins-workspace；桌面 Rust 侧显式 no-op，iOS Swift `UIImpactFeedbackGenerator`/`UINotificationFeedbackGenerator` 与 Android Kotlin `VibrationEffect` 随 crate 自带——spike 结论：不手写桥）注册于 `src-tauri/src/lib.rs` 并按命令授权（`haptics:allow-impact-feedback` + `haptics:allow-notification-feedback`；该插件无 default 权限集）；触觉门面（`src/services/haptics.ts`）——纯 kind→pattern 映射 `hapticPlan`（send→impact light、complete→notification success、permission→warning、error→error，判别联合使插件调用免断言）+ fire-and-forget 的 `haptic(kind)`，双重 no-op 守卫（非 Tauri 不调用；桌面平台不调用——插件本就 no-op，省掉 IPC 往返）；事件→触觉接线（`src/services/hapticEvents.ts`）——两个纯映射 `statusHaptic`（busy/retry→idle=complete、→error=error；首次观察不触发）与 `permissionHaptic`（队列出现新 id=permission；首次观察不触发），`startHapticEvents(serverId)` 用两个 createEffect 观察会话状态与权限队列、**基线在挂载时同步快照**（挂载即存在的状态是快照不是事件；Solid 的 effect flush 是延迟调度的，基线不能等首次 run），由 MobileShell 挂载/卸载；PromptBox 发送路径在乐观提交时触发 `haptic("send")`；移动端页面转场（`src/shells/mobile/transitions.ts`——纯 `pageEnterDir` 深度差映射 + `routeKey`）由 MobileShell 在 **keyed 页面容器**上落地（推入 `page-enter-forward` 右滑入+淡入、弹回 `page-enter-back`、首挂载无动画；时长走 `--dur-*` token，prefers-reduced-motion 下归零为瞬时切换）；共享元素转场（会话卡片→聊天页标题，ui-design §4.1）以**简化版 scale-fade** 落地（ChatPage 根 `page-enter-zoom`）——完整 FLIP 转场列入遗留并在文档注明；性能护栏注释写入 `.glass` 定义处（tokens.css——全仓唯一 backdrop-filter；同屏 ≤4、固定尺寸、不动画 blur）并对生成中特效盘点（呼吸光标 + 工具卡 shimmer 均存在且 reduced-motion 兜底）；L1（门面映射/守卫/调用参数、hapticEvents 纯映射与 watcher 生命周期含 dispose 语义、transitions）与 L2（MobileShell 推入/弹回转场类 + 触觉 watcher 挂载、PromptBox 发送触觉、shimmer/光标由既有套件覆盖）测试；docs/tasks/M7.md 记录落地备注与 L5 checklist——真机手感项待 iOS 重建

- 手势系统 (TASK-M7-06)：移动端手势 hooks（`src/shells/mobile/gestures.ts`）遵循统一纪律——**方向锁定**（手势移动 ~10px 前不表态，之后按主轴锁定：横向 = 手势接管指针，纵向 = 交给滚动容器，因此各手势与滚动永不打架；可滑动行保留 `touch-action: pan-y` 让浏览器原生处理纵向滚动）、窗口级 move/up/cancel 监听（释放与卸载双路径拆除）、pointercancel（浏览器抢走手势——滚动窃取/系统返回/双击缩放）时归位到当前已提交状态但不触发动作（M7-05 遗留项：`Sheet.tsx` 同步补上 pointercancel——吸附到最近档位、绝不关闭）；四个 hook：`useSwipeActions`（列表行横向拖拽露出操作——40px 提交阈值、可配置露出宽度、已露出行点按即收起且吞掉点击、拖拽后的 click 守卫防误触行内按钮）、`useLongPress`（按住 500ms 触发回调并带按压坐标；位移超过 10px 取消，滚动不会误触发；长按结束后的释放点击被吞掉，手指下的按钮不会激活）、`usePullToRefresh`（基于 touch 事件——这是唯一能 preventDefault 原生纵向滚动的通道；仅 scrollTop=0 时生效、阻尼封顶、释放超过 64px 执行刷新并保持指示器直至返回的 Promise 落地、失败静默）、`useEdgeSwipeBack`（~24px 左边缘右滑 ~40px 弹回上一页）。业务接入：移动端会话 Tab（`SessionsPage.tsx`）新增左滑露出 改名/删除 行（复用 RenameSessionDialog/DeleteSessionDialog 同一套 mutation，同时只允许一行露出）与下拉刷新（新增 `refreshSessionList` 辅助（`src/shells/mobile/refresh.ts`——重新拉取 GET /session + GET /session/status 写入 store，即 `syncAll` 的会话半部；移动端暂无目录/项目 UI），下拉跟随指示器 + 刷新期间停留 spinner）；移动端聊天页边缘右滑返回；移动形态下消息气泡长按弹出操作菜单（MessageList/MessageBubble/MessageActions 新增 `mobile` 标志——触屏菜单只保留 复制文本/复制代码/删除，抑制 iOS 文本选择 callout，桌面 hover/右键行为零回归）；L1（hooks：方向锁定、提交阈值、防误触 slop、取消路径、定时器/监听清理、click 守卫）与 L2（SessionsPage 左滑露出 + 阈值 + 单行互斥 + 对话框、下拉刷新触发列表+状态刷新并保持/释放指示器、ChatPage 边缘滑动在壳层内弹回、MessageActions 长按菜单 + 复制/删除 + 滚动取消 + 释放点击吞掉、Sheet pointercancel 吸附不关闭）测试覆盖；docs/tasks/M7.md 记录落地备注

- Bottom Sheet 交互体系 (TASK-M7-05)：可复用的底部 Sheet 基础组件（`src/components/Sheet.tsx`）——固定 scrim（点击关闭、打开时淡入）+ 底部锚定面板（固定 95vh 高、顶部圆角 `var(--r-xl)`、`pb-safe`），三档吸附按视口高度百分比（low 25% / mid 60% / high 95%，面板高度固定为高档位、各档位即 translateY 偏移，**手势全程几何不变**）；`--ease-spring`（时长 `--dur-med`）弹簧吸附；handle 与面板上的 pointer 拖动（拖动期间 `transition: none` 1:1 跟手；向下超过 ~120px 或快速下甩 > 0.8 px/ms → 关闭，其余松开 → 吸附到**最近**档位）；拖动结束的 click 守卫（面板作用域 capture 监听器吞掉紧随拖动的点击，500ms 自移除，防误触面板内按钮且不跨面板泄漏）；`prefers-reduced-motion` 双层降级（tokens 媒体查询把 `--dur-med` 归零 + JS 改用 `0ms linear` 过渡并跳过 rAF 交接；jsdom 无 matchMedia，代码做了存在性守卫）；`role="dialog"` + `aria-modal`，打开时焦点移至面板、关闭时还原到打开前焦点元素（打开时捕获标记，因关闭时面板可能已卸载）、Esc 关闭、打开期间 body 滚动锁；`dismissible` 门控统一管理 scrim/Esc/下滑关闭三个关闭触发器——权限/问题卡片必须被回答、不可跳过，接入时固定 `dismissible={false}`（拖动仍可换档位，只是不关闭）。业务接入：PermissionSheet/QuestionSheet 抽出共享卡片主体（`PermissionCard`/`QuestionCard`，桌面 kobalte overlay 与移动 Sheet 同一渲染），`variant="sheet"` 落地为 `snap="high"` + 不可关闭的 Sheet，remembered 模式自动回复不再受 variant 门控；ModelPicker 抽出 `ModelPickerContent`，移动端（`platform.kind === "mobile"`）用 Sheet 包裹（可关闭、`snap="high"`），桌面保持 kobalte Dialog 零回归；MobileShell 挂载两个 Sheet 变体（DesktopShell overlay 挂载不动）。L2 测试覆盖基础组件（渲染/三档吸附/离屏起步无闪烁路径/scrim、Esc、阈值下滑、快速下甩关闭/最近档位吸附/拖动中禁用过渡并恢复/body 滚动锁/焦点移动与还原/reduced-motion/dismissible 全触发 no-op/拖动后不触发下层按钮）、两个业务 Sheet 变体（渲染 + 动作生效 + 不可关闭 + 自动回复）、ModelPicker 移动形态（Sheet 呈现 + scrim 关闭 + 桌面 Dialog 回归断言）与 MobileShell 挂载（两条队列入队 → 两个 Sheet 出现，出队 → 消失）

- 安全区与键盘适配 (TASK-M7-04)：viewport meta 增加 `viewport-fit=cover` + `interactive-widget=resizes-content`（内容延伸至刘海/home 指示区之下，`env(safe-area-inset-*)` 才有真实值；键盘弹出时 WebView 视口收缩到键盘上方，底栏/输入框无需 JS 即可吸附键盘顶）；`src/styles/index.css` 新增全局安全区工具类——`pt-safe`/`pb-safe`/`px-safe`/`pb-safe-bar`（「基础值 → constant() → env()」回退链，`--safe-bar-h` token 承载原生栏预留高度）与横屏 `.landscape-terminal` 全屏类（100dvh，应用于移动端 Terminal 占位页；M7-09 细化）；原生玻璃栏改为**门控**——`GlassPlugin.swift` 默认创建隐藏 bar（`tabBarHidden` 状态兜底异步注入竞态），处理新的 web → native `setHidden { hidden }` 桥消息，并经 `#available` 隐藏 iOS 26 `scrollView.topEdgeEffect`；web 侧经 `src/shells/mobile/glassControl.ts`（`setGlassBarShown`/`setGlassBarHidden`，无桥 no-op）驱动门控：App 在移动端启动时断言隐藏（首页无底部导航——修复 M7-03 review 的「首页出现带 inert tab 的原生栏」问题），MobileShell 挂载时显示、卸载时隐藏；安全区应用：MobileShell 根 `h-dvh` + 原生模式内容 `pb-safe-bar`（替换 `pb-20` 占位）+ web 导航 `pb-safe`，ServerHome 根 `min-h-dvh` + `pb-safe` 且 sticky header padding 以 `max()` 纳入刘海/横屏 inset（桌面端 env()=0 零回归），PromptBox 底部 padding `max(0.75rem, env(safe-area-inset-bottom))`；L1（glassControl setHidden 消息形状 + 无桥 no-op）与 L2（MobileShell 挂载显示/卸载隐藏生命周期、`pb-safe-bar`/`pb-safe` 类、App 移动启动隐藏/桌面不调用）测试覆盖；docs/tasks/M7.md 记录 L5 checklist——iOS 重建验证待跑（环境同 M7-02/03）

- 移动端 Shell 与底部导航 (TASK-M7-03)：平台探测（`src/platform/index.ts`——`detect()` 依据 UA + WebView 信号 + 视口兜底（docs/architecture.md §3）解析出 `{ kind: "desktop", os } | { kind: "mobile", os: "ios" | "android" }`，另提供供测试使用的 `refreshPlatform()`）与能力表（`src/platform/capabilities.ts`——`supportsPet`/`supportsGlobalShortcut`/`supportsTray`/`supportsNativeGlass`/`supportsSystemBack`），App.tsx 在服务器首页选中后按探测平台挂载 DesktopShell 或全新的 MobileShell；移动端壳层（`src/shells/mobile/MobileShell.tsx`）替换 M7-02 玻璃 demo——四个 keep-alive 底部 Tab（会话/文件/终端/设置：四个 Tab 根全部保持挂载，非激活时仅 CSS 隐藏，因此每个 Tab 的滚动/输入状态在切换后保留）叠加每 Tab 独立推入栈 store（`src/shells/mobile/navigation.ts`——`push`/`back`（根页面时 no-op）/`selectTab`，`TAB_ORDER` 与原生 item 顺序一致，`TAB_ROOT_PAGE`），经页面注册表渲染（`src/shells/mobile/pages.tsx` + `PageHeader.tsx`，含文件/终端/设置/Diff 占位页与 NotFound 兜底）；真实会话 Tab（读取 session store 的行列表 + 状态圆点，点按推入聊天页）与真实聊天页（复用桌面 MessageList、Back 头、「View diff」推入 Diff 占位页）验证了列表 → 聊天 → diff 推入流转；底部导航在玻璃桥可达时采用 iOS 原生 Liquid Glass UITabBar（隐藏 web 导航、内容区 `pb-20` 为原生栏预留 padding——安全区由 M7-04 细化），其余情况（Android + 无桥 iOS 兜底）渲染 Material 3 风格 web 导航，原生 tab 点按经 `window.__glassTabSelected` 到达，桥接辅助位于 `src/shells/mobile/glass.ts`（install/cleanup、web → native 的 `setActive` `postGlassMessage`）；glass 插件原生 tab 标题已改为英文（Sessions/Files/Terminal/Settings——M9 i18n 再做本地化）；L1（平台探测 UA/WebView/视口各分支（含 iPadOS 桌面 UA 模式与触屏兜底）、能力表、导航 store 推入/弹出/根页回退/切 Tab 状态保持/reset、玻璃桥辅助）与 L2（壳层：四个 Tab 渲染、切换保持隐藏 Tab 挂载且栈存活、列表 → 聊天 → 返回流转、空状态、原生玻璃模式隐藏 web 导航并路由原生点按、无桥 iOS 回退 web 导航；App：平台切换挂载对应壳层）测试覆盖全链路；原生栏改动仅涉 Swift（无 cargo 编译）——验证英文标题的 iOS 重建留待 L5 checklist

- 子会话树与同步消息 (TASK-M6-07)：会话列表改为父子树视图——父会话显示箭头（展开/折叠，折叠后隐藏子树），行按层级缩进并带左侧连接线边框，子行保留 fork 徽标，时间分组只对树根生效（子树直接挂在父会话下方、不受分组影响）；纯函数树辅助位于 `src/features/sessions/sessionTree.ts`（`childrenOf`/`topLevelRoots`/`buildSessionTree`），同时修复 M6-03 遗留 bug：祖辈与孙辈同时命中搜索、中间父辈未命中时，孙辈不再作为独立根行重复出现（根的计算排除任何具有传递命中祖先的会话）；重新展开父会话时一次性调用 `GET /session/{id}/children`，返回的会话经 upsert 写入 store，使未及时到达 SSE 流的子会话在展开时补入树（失败静默，store 为数据源）；会话服务新增 `children(sessionID, dir?)`（GET /session/{id}/children——1.18.11 契约返回完整 Session 对象数组）与 `sendSync(sessionID, body, dir?)`（POST /session/{id}/message——请求体与 prompt_async 相同，但为同步语义：服务端直接返回创建的 assistant 消息 `{ info, parts }`，供简单一次性调用场景；按 M6 规格不设 UI）；SubtaskPart 卡片（TASK-M3-03）将 M6 占位提示替换为「Open child session」按钮（仅在提供 `onOpenChild` 回调时显示）——1.18.11 SubtaskPart 契约不含子会话 id（经 OpenAPI 核对），因此接线处理器打开的是该 part 所属会话的第一个子会话，接线链路 DesktopShell → MessageList → MessageBubble → SubtaskPart；mock server 注册 children 端点（fixture 树 sess_01 → sess_02 → sess_03 → sess_04，写入 session.list.json；未知会话 → 404 NotFoundError）与同步 message 端点（与 prompt_async 相同的 part 校验，返回创建的 assistant 消息，载荷非法 → 400）并附自测断言；L1（children/sendSync 服务载荷（含 directory 透传）、sessionTree 纯函数（含传递根修复））、L2（列表：深层嵌套 + 层级/连接线、箭头折叠/展开、叶子行无箭头、保留 fork 徽标、混合命中搜索时孙辈仅在匹配祖辈子树内出现一次、重新展开时拉取 children + 失败容忍；子任务：按钮可见性与回调；壳层：打开第一个子会话流程 + 无子会话时无操作）与 L3（live-mock children 列表/逐层/叶子/404、同步消息成功/非法载荷 400）测试覆盖全链路

- 摘要压缩与 AGENTS.md 初始化 (TASK-M6-06)：会话行菜单新增「Compress context」「Generate AGENTS.md」两项，分别打开两个新对话框（`src/features/sessions/SummarizeDialog.tsx` / `InitDialog.tsx`，与改名/分享对话框一样挂在 SessionList 本地状态上）；压缩对话框向 `POST /session/{id}/summarize` 提交紧凑的 provider/model 选择（`src/features/models/ModelSelect.tsx`——两个下拉框，数据来自 models store 且仅列出已连接 provider，经解析链默认选中会话自身模型，store 为空时挂载即拉取目录，无已连接 provider 时显示提示），请求进行中禁用并显示「Compressing…」进度提示——1.18.11 契约请求体为 `{ providerID, modelID, auto? }`、响应为纯 boolean（压缩后的上下文另行以会话 `summary` 呈现，因此对话框只管发起与确认）；初始化对话框按契约要求携带完整 `{ modelID, providerID, messageID }` 请求体，预选会话最近一条用户消息（取自 messages store 的 `infos`，`latestUserMessage` 辅助函数），无用户消息时禁用确认并引导用户先发分析请求（「Send an analysis prompt first, then run Generate AGENTS.md」）；两个对话框的成功结果经新落地的极简 toast 系统反馈——`src/stores/toasts.ts`（`createToast(message, kind)` → 3 秒自动消失、`dismissToast`、`clearToasts`）+ `src/components/Toast.tsx`（右上角固定宿主，success/error/info 变体 + 关闭按钮）并在 DesktopShell 挂载一次——成功后关闭对话框（「Context compressed」/「AGENTS.md generated」），失败内联报错并保留对话框，Esc/遮罩不会让进行中的请求失去归属；会话服务新增 `summarize(sessionID, body, dir?)` 与 `init(sessionID, body, dir?)`（schema 派生的 SessionSummarizeInput/SessionInitInput，directory 透传）；mock server 注册两个端点（summarize 校验 provider/model 必须为已知目录条目，init 另校验 messageID 必须为已知 fixture 消息——否则 400 BadRequestError，两者均返回 `true`）并附自测断言；L1（服务载荷拼装（含 directory 与 auto 标志）、toast store 计时）、L2（压缩对话框：会话模型预选、仅已连接 provider 列表、无 provider 提示 + 禁用确认、确认载荷、切换 provider、进行中锁定 + 提示、失败内联、Esc；初始化对话框：无用户消息提示 + 禁用、最近用户消息预选、确认载荷、进行中锁定、失败内联、Esc；列表：两个菜单项打开对话框；toast 宿主：变体渲染 + 关闭；壳层：toast 宿主挂载）与 L3（live-mock 压缩成功/未知模型 400/缺字段 400、初始化成功/未知 messageID 400/缺字段 400）测试覆盖全链路

- 会话分享与二维码 (TASK-M6-05)：DesktopShell 聊天头部新增分享图标（会话已分享时着色并标记）+ 会话行菜单新增「Share」项，两者都打开分享对话框（`src/features/sessions/ShareSessionDialog.tsx`）；未分享的会话提供 Share 动作（`POST /session/{id}/share`——经 1.18.11 契约核对无请求体，返回更新后的会话），分享后对话框展示只读分享 URL + 复制按钮（Clipboard API + execCommand 兜底，「Copied!」反馈）、由 URL 生成的二维码（`qrcode` 包 → data URL → `<img>`，160px，供移动端扫码）、「Open in browser」按钮（tauri `@tauri-apps/plugin-opener` 的 `openUrl`，能力已授予）、Unshare 动作（`DELETE /session/{id}/share`）以及「扫码设备需能访问服务器」的提示；所有状态经会话 store 流转——`shareSession`/`unshareSession` 动作以服务端返回的更新会话替换 store 中的会话，因此已分享徽标（会话行内的「shared」小徽章，tooltip：「Shared — anyone with the link can view」）与 URL 完全由契约的 `Session.share: { url }` 标记推导（经 1.18.11 OpenAPI 核对，无独立的分享状态字段或事件），分享/取消状态与服务端保持一致；失败路径（分享/取消/打开浏览器）内联报错并保留原状态，对话框在请求进行中不可关闭；会话服务新增 `share(sessionID, dir?)` 与 `unshare(sessionID, dir?)`（directory 透传）；mock server 注册分享端点族（POST 返回带 `share: { url }` 的更新会话，DELETE 返回不带标记的更新会话）并附自测断言，另有 L1（服务载荷拼装（含 directory 透传）、shareSession/unshareSession store 替换与失败路径）、L2（对话框：分享 → URL + 二维码图 + 复制 + 打开浏览器调用 opener、取消分享 → 清空并回到 Share 按钮、已分享直开、复制反馈、分享/取消/打开失败路径、可达性提示、Esc；列表：shared 徽标、行菜单 Share 项打开对话框、菜单分享后行出现徽标；壳层：头部分享图标开关/已分享标记、分享 → 二维码 → 打开 → 取消分享全流程）与 L3（live-mock 分享设置标记、取消清除标记）测试

- 消息回滚与恢复 (TASK-M6-04)：消息菜单「Revert to here」（任意消息均可用——用户与助手消息皆可）打开二次确认对话框，说明该消息之后的文件变更将在服务端回退（取消不发起任何请求；回滚失败时对话框保留并显示内联错误），快照芯片（TASK-M3-02）作为第二个入口接通——点击芯片即对承载快照的消息发起同一回滚流程；会话服务新增 `revert(sessionID, messageID, dir?)`（`POST /session/{id}/revert`，携带契约要求的 `{ messageID }` 请求体——可选的 `partID` 本任务未使用——返回更新后的会话）与 `unrevert(sessionID, dir?)`（`POST /session/{id}/unrevert`，无请求体），`revertSession`/`unrevertSession` 动作以服务端返回的更新会话替换 store 中的会话；回滚状态完全由契约中的 `Session.revert` 标记推导（`{ messageID, partID?, snapshot?, diff? }`——经 1.18.11 OpenAPI 核对，无逐消息标记、v1 亦无专属事件）：MessageList 在标记存在时渲染吸顶「Reverted to message …」条 + 一键 Unrevert 按钮（POST unrevert 清除标记），并将回滚点之后已加载的消息灰显（回滚点本身与更早消息保持活跃），状态统一由会话 store 驱动（SSE `session.updated` 与 REST 响应共用同一数据源）；mock server 注册回滚端点族（revert 校验 messageID 必须存在且为已知 fixture 消息——缺失/非法或未知均返回 400 BadRequestError——并返回带标记的更新会话；unrevert 返回不带标记的更新会话）并附自测断言，另有 L1（revert/unrevert 服务载荷拼装（含 directory 透传）、revertSession/unrevertSession store 替换与失败路径）、L2（消息菜单项禁用/回调（含右键菜单）、快照芯片惰性 vs 触发行为、列表内回滚条 + 灰显 + unrevert、壳层取消确认/确认回滚/unrevert/快照芯片全流程）与 L3（revert 设置标记、未知 messageID → 400、unrevert 清除标记）测试

- 会话分叉 (TASK-M6-03)：消息菜单「Fork from here」（任意消息均可用——用户与助手消息皆可，opencode 在消息点分叉）或会话行菜单「Fork」（整会话分叉）创建子会话；会话服务新增 `fork(sessionID, messageID?, dir?)` 封装（`POST /session/{id}/fork`，可选 `{ messageID }` 请求体，schema 派生的 SessionForkInput），`forkSession` 动作将返回的子会话写入 store 并自动打开（upsert + set active，来源标注由子会话的 parentID 承担）；会话列表将分叉子会话缩进渲染在父会话正下方并带「fork」徽标（tooltip：「Forked from <父标题>」）——父会话匹配搜索时整棵子树一并显示，仅子会话匹配时子会话带徽标独立成行，更深层嵌套递归渲染（M6-07 将以完整树视图替换）；DesktopShell 接线消息级分叉流程（失败在聊天区上方的内联横幅呈现，会话级失败在列表横幅呈现）；mock server 注册分叉端点（返回带 parentID 的子会话；messageID 存在时必须为已知 fixture 消息，否则 400 BadRequestError）并附自测断言，L1（服务载荷拼装（含/不含 messageID 与 directory 透传）、forkSession store/激活行为与失败路径）与 L2（消息菜单项禁用/回调（含右键菜单与助手消息行）、列表缩进/徽标/搜索交互、行菜单分叉流程、壳层分叉并自动打开全流程）测试，另有 L3 契约块（分叉、已知 messageID、未知 messageID → 400）

- 终端面板（xterm.js）(TASK-M6-02)：桌面壳新增 Terminal 主视图（`src/features/terminal/TerminalPanel.tsx`）——从 Chat|Files 标签栏的终端图标或临时 ⌘/Ctrl+J 快捷键打开（输入控件聚焦时屏蔽；再按一次切回聊天视图，自带 Back 头返回聊天，打开时与 diff/changes/settings 视图一样隐藏标签栏）——多 Tab 管理与 per-server ptys store 联动：Tab 以 pty id 为键、显示 shell/命令标题，每 Tab 带关闭按钮，"+" 按钮弹出 shell 选择器（`GET /pty/shells`：默认 shell 行、逐 shell 行（不支持者带标记）、加载/出错+重试状态、创建失败内联错误、Esc 关闭），经 `POST /pty`（`{ command, title }` 或服务器默认）创建终端并激活新 Tab；Tab 切换时所有实例保持挂载（CSS 隐藏而非卸载——PTY 随 WS 断开而死亡，切换绝不能断链）；关闭 Tab 时 DELETE 服务端 pty（终止进程）并从 store 移除；每个 Tab 渲染一个 `TerminalInstance`（`src/features/terminal/TerminalInstance.tsx`），拥有一个 xterm.js 6 实例（新增 `@xterm/xterm` + `@xterm/addon-fit` 依赖，主题取设计令牌（暗色兜底），字号 13，fit addon），经 M6-01 的 Rust WS 通道连接：入站 ArrayBuffer 帧 UTF-8 解码写入终端、按键 UTF-8 编码经 `ptySend` 发送、尺寸变化经 `PUT /pty/{id}` `{ size: { rows, cols } }` 同步（契约规定的 REST resize 通道）、ResizeObserver 监听容器尺寸变化重 fit（容器隐藏时跳过，避免隐藏 Tab 把 PTY 缩到 1×2 并 PUT 给服务器）、通道关闭或建连失败将 store 中的 pty 标记为 exited；退出 Tab 显示 "Process exited (code N)" 提示 + 关闭按钮并保留至用户关闭；卸载时关闭通道并 dispose 终端；L2 测试覆盖面板（空状态、store 实时 Tab、shell 选择器创建流程 + 默认行 + 列表重试 + 创建失败、Tab 切换保持实例挂载、关闭 → 通道关闭 + DELETE + 相邻激活、退出 Tab 提示）与实例（挂载/主题、通道接线、UTF-8 出入、resize PUT、可见性守卫的 fit、通道关闭 → 退出标记 + 提示、退出不建连、卸载清理）；E2E E11 随 M10 基础设施落地（docs/tasks/M6.md 已注明）

- PTY 服务层与 Rust WebSocket 通道 (TASK-M6-01)：类型化 PTY 领域服务（`src/services/pty.ts`——list/create/get/update/remove 覆盖 `GET,POST /pty` 与 `GET,PUT,DELETE /pty/{id}`、`shells` 覆盖 `GET /pty/shells`、`connectToken` 覆盖 `POST /pty/{id}/connect-token`，schema 派生类型 + directory 透传）；Rust WS 数据通道（`src-tauri/src/transport/ws.rs`——`pty_ws_connect`/`pty_ws_send`/`pty_ws_close` Tauri commands）：connect-ticket 获取与 `ws(s)://{base}/pty/{ptyID}/connect?ticket=…` URL 组装全部在 Rust 侧完成（1.18.11 契约参数名为 `ticket` 而非 `token`），tokio-tungstenite + rustls 建连，二进制帧以 `{ "bytes": [...] }` JSON 包裹经单个 tauri Channel 推送前端（tauri 2.11 无原始字节 Channel send），终止以 `{ "type": "pty.ws.closed" }` 控制帧通知，不实现重连（PTY 随 WS 断开而死亡），cargo 进程内测试覆盖帧编解码、URL 组装、注册表生命周期、服务端关闭清理与完整的 token→建连→回显往返；TS 门面（`src/services/ptyWs.ts`——`ptyConnect`/`ptySend`，返回 `{ connectionId, close }`，bytes 信封 → ArrayBuffer，关闭回调）；per-server ptys store（`src/stores/ptys.ts`——列表/顺序、upsert、退出标记、删除、reset）由事件路由新增的 `pty.created`/`pty.updated`/`pty.exited`/`pty.deleted` 分支驱动，并在重同步时随服务器重置；Mock Server 注册 PTY 路由族（fixture 列表 + shells fixture，动态 create/get/带校验的 resize update/delete/connect-token，`GET /pty/{id}/connect` 返回 426 Upgrade Required + JSON 说明——express 无法原生 WS 升级），另附独立 WS echo 服务器（`tests/mock-server/ws-echo.mjs`，基于 `ws` 包）与含二进制发送/回显往返的自测断言；L1（服务载荷组装、门面信封处理、ptys store、事件路由含重置）与 L3（live-mock PTY 契约 + WS echo）测试覆盖全链路；协议验证记录（contract-based，待真实服务端确认——本环境无真实 `opencode serve`）已写入 docs/tasks/M6.md 附录，含偏差说明（ticket vs token、契约将 connect 文档化为普通 HTTP boolean、resize 仅走 REST）

- Skill 引用与会话内 shell 命令 (TASK-M5-08)：输入框 `@` 引用菜单新增 skills 分组（位于文件结果上方）——技能目录（`src/services/skill.ts`，GET /skill）在首次 `@` 触发时拉取一次（按挂载缓存、请求中防重），查询按客户端本地过滤，行内显示技能名与描述，选中插入纯文本 `@skillName` 引用（服务端在回显消息中将 `@name` 提及解析为 AgentPartInput part，与 `@path` 解析为文件 part 一致——客户端不做 part 映射，见 docs/api-coverage.md 说明）；`!` 前缀将发送路由到 `POST /session/{id}/shell` 而非普通提示（`src/features/sessions/sendShell.ts`——`shellCommandOf` 为纯路由函数，单独的 `!` 回退到普通提示路径）：请求体携带会话当前 agent（1.18.11 契约必填）与当前 model，同步返回的 `{ info, parts }` 消息一次性写入消息 store（回复以普通 assistant 消息渲染，无 SSE 回显），`!` 条目不记入提示历史，失败时恢复已提交文本并显示内联错误横幅；会话服务新增 `shell` 封装（schema 派生的 ShellRunInput/ShellRunResult 类型）；Mock Server 提供 3 技能 fixture（契约无 hidden 字段——隐藏技能由服务端过滤）、带校验的 shell 端点（缺 agent/command 或 model 形状错误 → 400，返回创建的 assistant 消息），以及接受 AgentPartInput 形状的 prompt_async part 校验，均含自测断言；L1（技能服务载荷组装、shell 服务载荷组装、`shellCommandOf` 路由 + `runShell` store 应用、@ 菜单 `atEntriesFor`/`atInsertText` 纯函数）、L2（@ 菜单 skills 分组过滤/插入/按挂载一次拉取、shell 发送携带 agent+model、回复经 MessageList 渲染、失败恢复、单独 `!` 回退、普通提示不受影响）与 L3（skills 列表 + shell 执行契约）测试覆盖全链路

- Tauri 2 + SolidJS 工程脚手架，devtools 仅限 debug 构建启用 (TASK-M0-01)

- 设计令牌与 Tailwind CSS v4 基座，支持深浅色主题切换 (TASK-M0-02)

- OpenAPI 类型生成管线：`openapi-typescript` 从版本锁定契约生成类型，带漂移检测 (TASK-M0-03)

- Mock OpenCode Server REST 骨架：健康检查 / 项目 / 会话端点、故障注入、可选 Basic Auth (TASK-M0-04)

- Mock Server SSE 流与场景脚本：happy-chat、permission-flow、question-flow、sse-drop (TASK-M0-05)

- 真实响应样本 fixtures，含录制脚本与 schema 校验 (TASK-M0-06)

- CI 流水线与 `pnpm verify` 质量门禁，husky + lint-staged 提交前钩子 (TASK-M0-07)

- 双语工程文档骨架：README / CHANGELOG（中英）+ CONTRIBUTING (TASK-M0-08)

- Rust REST 传输通道（reqwest/rustls、Basic Auth、超时、取消、错误分类）与 TS ApiClient 门面（invoke/fetch 双传输实现）(TASK-M1-01)

- Rust SSE 订阅管理器（跨 chunk 逐行解析、`tui.*`/`workspace.*` 事件过滤、16ms 批刷 Channel 推送、指数退避重连、心跳判死）与 TS 订阅门面（tauri Channel 封装）(TASK-M1-02)

- Rust 服务器注册表：持久化 CRUD commands（`list_servers`、`add_server`、`update_server`、`remove_server`、`resolve_server_base_url`），基于 tauri-plugin-store，带 `servers-changed` 同步事件；传输通道改为经由注册表解析服务器地址 (TASK-M1-03)

- Rust 健康监控器：每服务器独立 15s `GET /global/health` 轮询（复用 REST 传输层），记录延迟与版本，连续 3 次失败判定 down 并支持恢复，状态变化经 `server-health` 事件推送前端；新增 SolidJS 连接 store 同步健康快照 (TASK-M1-04)

- 添加服务器向导：名称/URL/可选认证表单，URL 规范化、实时「测试连接」探测（显示版本/延迟）、明文 HTTP 风险提示，保存走注册表 commands；并为注册表 commands 新增类型化 TS 封装 (TASK-M1-05)

- 服务器导航首页：App 启动落点，响应式服务器卡片网格（名称/URL/状态灯/版本/延迟/最近连接），经 `servers-changed` 与 `server-health` 事件实时更新；卡片右键菜单（含菜单按钮）提供编辑/重连/删除（二次确认）；添加服务器向导支持编辑模式（`update_server`）；空状态引导页 (TASK-M1-06)

- mDNS 局域网自动发现：Rust 扫描 `_opencode._tcp`（及 `opencode serve --mdns` 发布的 `_http._tcp` 广播），按实例去重并 emit `server-discovered` 事件，提供幂等的 `start_mdns_discovery` / `stop_mdns_discovery` / `get_discovered_servers` commands，局域网不可达时静默降级；添加服务器向导新增「附近的服务器」区块，支持一键填充与自动探测 (TASK-M1-07)

- 服务器工作区壳：三栏桌面骨架（服务器图标 Rail 含每服务器健康状态点与 ⌘/Ctrl+1..9 快速切换、侧栏、主区）替换占位视图；新增激活服务器 registry store 为各 per-server store 注入上下文（切换不串数据）；离开时预留 SSE 断开挂载点（M2 接入）；移动端壳占位（M7）(TASK-M1-08)

- 凭证重认证与连接错误处理：分类错误文案（`errorTitle`/`errorDetail`）集中在 `services/errors.ts`（401/网络不可达/超时/5xx/非法 URL/已取消/响应格式异常）；ServerHome 加载与重连失败使用可关闭的 `ErrorBanner`；401 触发凭证重输对话框，新凭证先经探测验证通过后才持久化（`update_server`）；TS 侧探测响应校验将非健康负载判定为 `invalid_response`（TASK-M1-09）

- 项目/会话/消息领域服务层：基于 ApiClient 的工厂形态类型化服务（`createProjectService` / `createSessionService` / `createMessageService`），覆盖 `/project`、`/project/current`、`/path`、会话 CRUD（`/session` GET/POST、`/session/{id}` GET/PATCH/DELETE）、`/session/status`、带 `limit`/`before` 分页的消息历史（`/session/{id}/message`）、`prompt_async` 与 `abort`；类型全部取自 schema，错误按 ApiError 透传；mock server 新增 `/path`、`/session/status`、创建/更新/删除、`prompt_async`（204）、`abort` 与支持 `limit` 的消息分页，并实现跨 fixture 根目录的 fixture 回退（TASK-M2-01）

- SSE 事件路由与归一化 store：per-server 会话 store（列表/顺序/状态/当前会话 id）、消息归一化 store（`parts: Record<id, Part>` + `order`，delta 追加 O(1)，delta 先于 part 到达时建 text stub，part/消息删除）、项目 store（项目列表 + 当前目录），以及 `events.ts` 路由表将 `session.*` / `message.*` / `message.part.*` 事件分发进各 store；`server.connected` 经 `subscribeToServerEvents` 触发 `syncAll` 全量对齐（会话列表 + 状态表 + 项目列表 + 当前目录）；L1 测试直接驱动 mock happy-chat 场景断言最终 store 状态（TASK-M2-02）

- 项目/文件夹切换器：Sidebar 上段下拉组件（项目名 + 路径、当前项高亮、每服务器「最近项目」localStorage 记忆，上限 5 条），数据来自 `/project` + `/project/current`；切换项目即切换全局 `?directory=` 上下文（显式 per-call directory 现在优先于全局值），DesktopShell 重建该目录的 SSE 订阅（先退订旧流、再订阅新目录并全量对齐，切换前清空会话/消息状态保证跨目录互不串扰），新增 `project.updated` / `project.directories.updated` 项目列表刷新路由；mock server 扩展为双项目 fixture（`/session` 与 `/project/current` 按目录返回隔离数据，新增 labs 会话列表）（TASK-M2-03）

- 会话列表与状态徽标：Sidebar 下段按本地时间分组（今天/昨天/本周/更早，周一为周起始，跨 DST 安全）渲染会话，每条会话带状态徽标（busy/retry 转圈、idle 圆点、错误红点含 message 提示），由 SSE 驱动 store 实时更新；本地大小写不敏感搜索过滤（按标题/slug，含无结果与空列表空态）、当前会话高亮、行选择接线（`setActiveSession` + 回调）、hover 操作占位「⋯」按钮（改名/删除由 M2-05 接线）与虚拟滚动准备注释；DesktopShell 在侧栏挂载会话列表，主区占位改为回显所选会话 id（聊天视图 M2-06/08 落地）（TASK-M2-04）

- 会话创建/改名/删除：「+ New session」按钮（侧栏头部 + 空态）经 `POST /session` 创建空会话并直接打开；行级操作菜单替换 M2-04 占位按钮，提供改名（预填对话框，Enter 提交 / Esc 取消，`PATCH /session/{id}`）与删除（二次确认对话框，`DELETE /session/{id}`）；三类变更均先落 store（乐观更新），失败时回滚到捕获的原值并在对话框内联/横幅展示错误；DesktopShell 主区占位改为由 store 的当前会话 id 驱动（TASK-M2-05）

- 消息历史渲染（初版）：主区聊天转录（`src/features/messages/`）在挂载/切换会话时拉取 `GET /session/{id}/message` 历史并合并进归一化消息 store（新增按消息 id 的 `infos: Record<id, Message>`，保留原单条 info 槽位兼容旧逻辑），用户气泡右对齐加 accent 底色、助手气泡左对齐，附 hh:mm 时间戳；Part 经 `TextPart`（纯文本保留空白与换行）、`ReasoningPart`（默认折叠，带预览与箭头展开）与 `ToolPart`（v1 卡片：工具名 + pending/running/completed/error 状态图标与文案，展开可见 JSON 美化后的 input 与原始 output/error）渲染；未支持的类型静默跳过；含加载/空态/内联错误 + 重试状态，流式 part 先于消息 info 到达时按助手角色兜底渲染，以及自动滚动（贴近底部时跟随、上翻暂停并显示「New messages」跳转按钮）；DesktopShell 为当前会话挂载消息列表（无会话时保留占位）（TASK-M2-06）

- Markdown 渲染与代码高亮：文本 Part 经 markdown-it 管线渲染（GFM：表格、删除线、任务列表、linkify、typographer），禁用原始 HTML 并将外链改写为新窗口打开（`target="_blank"` + `rel="noopener noreferrer"`），随后再过一层 DOMPurify（allowlist 扩展以放行 Shiki 内联样式、任务列表复选框与链接 target）；围栏代码块经懒加载的 Shiki 单例异步高亮（预载常用语言、其他内置语言包按需 `loadLanguage` 加载、任一步失败回退为纯转义 `<pre>`），每块代码带语言标签与复制按钮（Clipboard API + `execCommand` 回退，点击显示「Copied!」反馈）；基于设计令牌补齐 markdown 正文样式（标题/表格/任务列表/引用/行内代码/代码围栏）；安全用例覆盖双层消毒下的 script/onerror/javascript:/data: 注入以及复杂 markdown fixture 渲染（TASK-M2-07）

- 消息输入与异步发送：主区消息列表下方固定输入框（`src/features/sessions/PromptBox.tsx`），自适应高度 textarea（上限约 10 行）、⌘/Ctrl+Enter 发送（裸 Enter 只换行）、空输入 ↑ 召回并循环浏览每服务器 prompt 历史（内存态，上限 20 条，最近优先，↓/Esc 回退）；发送时先乐观插入本地用户消息（`local-*` 本地 id，会话 agent/model 字段映射为消息 schema 形态）并立即清空输入框，`POST /session/{id}/prompt_async` 失败时回滚乐观消息并展示内联错误横幅；POST 进行中锁定发送，store 状态 busy/retry 时锁定输入（占位符「Generating…」+ 顶部细进度条；Esc 中断由 M2-10 接线）；附件按钮为禁用占位（M3 接线）；DesktopShell 为当前会话挂载输入框；L2 测试覆盖发送/键盘/历史/锁定/失败流程，并含全链路用例（乐观发送后驱动 happy-chat SSE 场景，断言最终渲染包含用户消息与助手回复；E2E E03 随 M10 基建落地）（TASK-M2-08）

- 流式增量渲染管线：消息 store 新增 `messageParts` 分组映射（消息 id -> 有序 part id 列表，仅在 part 成员变化时整体替换）与每会话 `lastDeltaAt` 流式时间戳，并提供 `applyMessageBatch` 将整段历史载荷在一次 produce 内落地；转录列表改用手写虚拟列表（`createVirtualList`：固定估算高度 + 实测高度，ResizeObserver 感知流式行增高，带 overscan 与 `scrollToIndex`），长对话只挂载可见区间的行；每条消息抽成独立 `MessageBubble` 组件只订阅自身 info/parts，delta 到来时仅重渲染对应 part 行（消除 M2-06 每个 delta 全量重新分组渲染）；新增呼吸式打字光标（`TypingCursor`，追加在流式消息最后一个已渲染 token 的行内），由 `useStreamingIndicator`（busy 状态 + 5 秒 delta 窗口 + 1s 滴答定时器）驱动；会话生成期间聊天区顶部显示 2px 细进度条（删除 M2-08 PromptBox 自带进度条，统一以会话 busy 为唯一信号）；自动滚动跟随/暂停/跳转逻辑在虚拟列表之上保持可用；L1 测试覆盖分组不变性（文本 delta 不触碰 `messageParts`）、批量应用正确性/对账与 `lastDeltaAt`，含 1000-delta 性能基准；L2 覆盖气泡级细粒度重渲染（兄弟 part DOM 节点保持不变）、虚拟化行数、进度条与光标生命周期（fake timers）；性能基准套件（`perf.bench.test.tsx`）约束虚拟列表（1000 行 x 1000 次滚动定位）、300 条消息转录渲染与单 token delta 成本（TASK-M2-09）

- 中断生成与错误处理：会话生成中（busy/retry 状态）输入框的 Send 按钮替换为 Stop 按钮（方形危险图标，本地在途锁防止双击重复），点击即 `POST /session/{id}/abort`；Esc 经 window 级监听在任意焦点位置触发相同中断（生成时 textarea 被禁用，无法自行接收按键）；abort 失败以内联横幅呈现，服务端 `session.status` idle 事件到达后恢复 Send 按钮（store 驱动的输入自动恢复）；`session.error` 事件渲染为消息列表与输入框之间的可关闭横幅（`SessionErrorBanner`）：标题使用分类文案（`errorTitle`/`errorDetail` 新增限流分类——状态 429 或消息含 "rate limit"/"429" 提示 → "Rate limited — try again shortly"），原始消息置于可展开的「Show details」区；含 Retry 按钮，经共享 `sendPrompt` 管线（自 PromptBox 提取，输入框与横幅共用）重发该服务器 prompt 历史中的最后一条（重发失败以分类错误重挂横幅，成功则关闭）；横幅由 store 状态条目派生，任何替换状态（idle/busy）到达即自动隐藏；session store 新增 `dismissSessionError` 动作将 error 状态回退为 idle；L2 测试覆盖停止/Esc/abort 失败/idle 恢复、横幅渲染/关闭/自动隐藏/重试（含防双击重试）与限流文案，L1 测试覆盖 `dismissSessionError`、`getLastPrompt` 与 sendPrompt 管线（E2E E04 随 M10 基建落地）（TASK-M2-10）

- 工具调用完整状态渲染：按工具类型定制卡片（bash 终端视图含命令提示符与退出码、edit 内联 diff 预览、read/write 代码块、glob/grep 结果列表带计数徽标、通用 JSON 兜底），经工具注册表按工具名分发组件；完整四态状态机（pending 呼吸时钟 / running 旋转图标 + CSS shimmer 扫光 + 实时跳动耗时并在状态离开 running 时清理定时器 / completed 对勾 + 实测或 metadata 耗时 / error 红叉 + 危险色错误信息）；输入参数可折叠（原始 JSON 披露区），输出可展开并可复制（「Copied!」反馈）；all-parts fixture 补齐全部工具族与四个状态，每类工具卡片均有 L2 快照覆盖（TASK-M3-01）

- 文件/补丁/快照 Part 渲染：文件附件卡片（按 mime 区分 image/text/file 图标，文件名带 URL 路径兜底，点击展开内联预览——base64 data-URL 图片或可折叠文本块——仅读取 Part 自带的内联 source 文本、绝不加载远程 URL，无内容时显示「Content unavailable」提示）；补丁卡片（短提交哈希徽标 + 文件总数，每个改动文件一行，作为跳转 diff 的占位点击项，经 `onOpenDiff` 回调留给 M4 接线）；快照标识 chip（相机图标 + 短快照 id，暂为惰性样式并附 M6 revert 提示）；三类 Part 均接入 MessageBubble 的 part 分发，含 L2 测试与各类型 fixture 快照，以及 all-parts fixture 气泡整体渲染用例（TASK-M3-02）

- 步骤/子任务/Agent Part 渲染：步骤边界以细分隔线呈现（「Step」标签 + 可选起始快照短 id），步骤完成行展示 reason、格式化 token 数（k/M 取整）与 USD 费用；子任务卡片（prompt 标题 + 共享 agent chip，可展开描述与 model/command 元信息，附 M6 子会话导航占位提示——1.18.11 schema 无 status 与子会话 id 字段，故暂无状态徽标与导航）；agent 引用高亮 chip（accent 色调 + 机器人图标 + 提及来源 tooltip）；四类 Part 均接入 MessageBubble 的 part 分发；含 `formatTokens`/`formatUSD` 单元用例、每类 L2 测试与 fixture 快照；all-parts fixture 追加第二组 step 对与 agent part 以覆盖多步骤层级渲染，另含 all-parts fixture 气泡整体渲染用例（TASK-M3-03）

- 重试与压缩提示渲染：retry part 渲染为系统样式 chip（刷新图标 + 「Retrying (attempt N)」尝试次数 + 失败原因——限流错误即 429 状态码或消息含 "rate limit"/"429" 提示时用危险色，其余用弱化色）；调用方提供 `retryAt` 纪元毫秒时（1.18.11 schema 无调度字段）倒计时标签每秒滴答递减「next attempt in Xs」，无定时数据时回退为静态「Retrying…」，且定时器在组件卸载时清理；compaction part 渲染为「Context compacted」系统行（压缩图标），可展开 schema 提供的明细（自动/手动模式、overflow 标记、被截断的 tail part id），part 无明细字段时仅渲染单行且不出现展开开关；两类 Part 均接入 MessageBubble 的 part 分发；L2 测试覆盖尝试次数与错误展示、限流分类、倒计时滴答/归零回退/卸载清理（fake timers）、展开收起与单行回退，含 fixture 快照及 all-parts fixture 的气泡与列表整体渲染用例（TASK-M3-04）

- 消息历史分页与向上加载：转录列表打开时只拉取最新一页（`GET /session/{id}/message?limit=50`），用户滚动到顶部时自动加载更早一页（`before` 游标），聊天区上方显示细加载指示条，按消息 id 去重合并，并保持滚动位置（视口 scrollTop 随插入高度精确增长重新锚定到先前可见内容，前置插入行不会造成跳动，也不会误触「New messages」按钮）；消息 store 为 `applyMessageBatch` 新增前置插入模式（更早页面插入已加载历史之前，且不覆盖最新消息槽位）；抽出纯函数 `mergePages` 分页助手（去重 + 游标推进 + hasMore 启发式 + 服务端忽略 `before` 时的回放检测）供 L1 覆盖；mock server 现支持 `before` 游标（`limit` 返回最近一页、`before` 仅返回更早消息、未知 id 返回空数组），self-test 增加翻页链断言；新增 120 条消息长 fixture（生成脚本随仓库提交），L2 覆盖仅加载最近页、分段加载、加载指示、防重复请求、游标重叠去重、回放终止以及 1000+ 消息分段遍历（无跳动、无重复）（TASK-M3-05）

- 消息操作菜单：每条消息的 hover 工具条（「⋯」按钮，行悬停时显现）与右键上下文菜单共享同一组操作——复制全文（消息全部 text part 以换行拼接）、复制代码（仅提取 `围栏` 代码块内容，无围栏时禁用）、用户消息「编辑并重发」（对话框预填第一个 text part 内容，经 `PATCH /session/{id}/message/{mid}/part/{partID}` 提交整份 Part——响应回写 store——再走共享 sendPrompt 管线重新发 prompt；任一步失败对话框保持打开并内联报错）、用户消息「删除」（二次确认，`DELETE /session/{id}/message/{mid}`，乐观删除复用既有 store 动作，失败时经新增 `restoreMessage` store 动作将 info 与 parts 恢复到记录的原顺序位置）与「查看 diff」占位项（M4-07 接线前禁用）；message 服务新增 remove / updatePart / removePart 封装，mock server 新增 DELETE message 与 PATCH/DELETE part 路由及 self-test 断言；L1（服务载荷拼装 + store 恢复）、L2（动作回滚 + 组件复制/编辑/删除/右键菜单全流程）与 L3（真实 mock 契约）测试覆盖全链路（TASK-M3-06）

- Todo 实时任务面板：单会话任务清单（`src/features/sessions/TodoPanel.tsx`）在挂载/切换会话时经 `GET /session/{id}/todo` 拉取初值并写入新增的 per-server per-session todos store，渲染完全由 store 驱动，因此 `todo.updated` SSE 事件（在 `stores/events.ts` 路由：兼容 schema 的完整 `todos` 数组与防御性的单条 todo 载荷，`server.connected` 全量对齐与目录切换时按服务器清空）实时刷新列表；每行含状态图标（pending 空心圆 / in_progress 旋转图标 / completed 对勾 / cancelled 叉）、优先级色点（high 红 / medium 琥珀 / low 灰，缺失时省略）与已完成项的删除线 + 弱化文本，附「No todos yet」空态与拉取失败的内联错误 + 重试；DesktopShell 将其挂载在新增聊天头部行（会话标题 + Todos 按钮）触发的右侧固定抽屉中（Esc / 点击遮罩 / 关闭按钮均可关闭），组件预留 `variant` 属性供 M7 移动端底部 Sheet 使用；happy-chat mock 场景新增连贯的 todo 生命周期（in_progress → completed）事件流并附带 self-test 断言；L1（服务载荷拼装 + store 动作 + 事件路由载荷形态）、L2（面板渲染/空态/实时 store 更新/拉取与重试、抽屉开关/Esc/遮罩）与 L3（真实 mock 契约）测试覆盖全链路（TASK-M3-07）

- 输入框附件与 @ 文件引用：剪贴板图片与拖拽/选取的文件变为 textarea 上方的可删除附件 chips（图片与大型/二进制文件以 base64 data URL 读取，小型文本类文件以纯文本读取、发送时编码为 utf-8 data URL）；回形针按钮启用为系统文件选择器，另设 M7 图片选择器禁用占位按钮；输入框内词首 `@` 触发防抖（150ms、在途响应作废）文件引用菜单，走新增的 `createFindService().files()`（`GET /find/file`），支持 ↑↓/Enter/Esc 键盘导航与鼠标点选，选中后在光标处插入 `@路径`；共享 sendPrompt 管线新增 attachments 参数，将 FilePartInput 部件追加在文本部件之后（乐观插入与历史仅覆盖文本部件；纯附件发送亦可）；发送成功清空 chips、失败保留以便重试；新增纯函数 `attachmentToPart` 完成附件→schema 映射（data URL 透传、纯文本编码为 data URL、可选磁盘路径挂到 FileSource）；mock server 注册 `/find` 与 `/find/symbol` fixtures 及按 query 过滤的 `/find/file`（空 query 返回空数组），self-test 断言覆盖文件部件 prompt 与三个搜索端点；L1（find 服务参数拼装、附件→部件映射、FileReader 附件转换）、L2（粘贴/拖拽/文件选择 chips、删除 chip、带附件发送的请求体与成功清空/失败保留、@ 菜单防抖/键盘导航/插入/Esc、词首门槛）与 L3（真实 mock 搜索与文件部件契约）测试覆盖全链路（TASK-M3-08）

- 文件/搜索/VCS 服务层：基于 ApiClient 的工厂形态类型化服务——`createFileService`（`/file` 扁平 FileNode 树，可选 `path` 子树展开，`/file/content`、`/file/status`）、`createVcsService`（`/vcs` 分支信息、`/vcs/status`、`/vcs/diff` 契约必填的 `mode` 默认 git 并可传 `context`、`/vcs/diff/raw` 从 `bodyText` 取 text/x-diff 文本、`/vcs/apply` 提交 `{patch}` 体、`/session/{id}/diff` 可选 `messageID` 供 M4-07 diff 视图）以及 `createFindService().symbols` / `.search`（`/find/symbol`、`/find`，带 schema 派生 FindMatch 类型）——类型全部取自 schema，`dir` 透传、错误按 ApiError 透传；mock server 注册 M4 文件/VCS 全端点路由与 fixtures（file.tree / file.content / file.status、vcs / vcs.status / vcs.diff / vcs.diff.raw、session.diff），`/vcs/diff/raw` 以 text/x-diff 返回、`/vcs/apply` 返回 `{applied:true}`、`/session/{id}/diff` 按 `messageID` 过滤（固定子集，未知 id 返回空数组），并补全 self-test 形状断言与 fixture 模式覆盖；L1（每方法精确载荷拼装）与 L3（真实 mock 契约）测试覆盖全部端点；录制脚本为新增 JSON 端点补充 targets，录制 index 将 `session.diff` 映射到已捕获的 diff.session 载荷（TASK-M4-01）

- 文件树与状态标记：新增 per-server 文件 store（`src/stores/files.ts`），从扁平 GET /file 载荷逐层构建工作区树（直接子级、目录优先、字母序），维护每路径展开状态、path→git 状态映射（added/deleted/modified）与版本计数器；`file.watcher.updated` / `file.edited` SSE 事件（在 `stores/events.ts` 路由，重同步时按服务器清空）递增版本号，挂载中的 FileTree 视图随版本变化重拉树与状态，实现 watcher 事件自动刷新；`src/features/files/FileTree.tsx` 在侧栏渲染文件树：目录懒加载展开（GET /file?path= 拉取子树并嫁接子节点，失败回卷展开状态并展示错误横幅）、文件类型图标（文件夹 + 按扩展名区分的文档字形）、git 状态色点（added 绿 / modified 琥珀 / deleted 红）、`ignored` 条目灰显加斜体、右键菜单（复制路径；「在会话中引用」在 prompt 插入钩子落地（M8）前复制 `@路径`；「打开」走 M4-03 查看器回调），以及加载中/空态/错误 + 重试状态；DesktopShell 侧栏新增 Sessions|Files 分段切换，Files 视图以文件树替换会话列表；L1（树构建/嫁接/展开折叠/状态/版本递增/清空）与 L2（渲染、懒展开与折叠不重拉、状态点、ignored 样式、右键菜单 + 剪贴板、watcher 触发重拉、加载/空态/错误重试、壳层视图切换）测试覆盖全流程（TASK-M4-02）

- 带编辑器式标签页的文件查看器：新增 per-server viewer store（`src/stores/viewer.ts`）维护有序打开文件标签（标题取路径 basename，显式名称优先），提供打开/激活/关闭/清空动作——重复打开同一文件仅激活既有标签、关闭当前标签激活左邻（或补位标签）、未知路径激活被忽略；`src/features/files/FileViewer.tsx` 渲染标签栏与当前文件内容，分四类分支——Shiki 高亮只读文本（语言由扩展名映射、复用既有懒加载高亮单例，失败回退为纯转义文本）、图片（mimeType + encoding 拼装 base64 data URL，或透传 `data:` 内容）、unified diff/patch（text/x-diff mime、`diff` 字段、`patch` hunks、或 `diff `/`--- ` 内容前缀判定）按行着色（新增绿/删除红/hunk 与文件头弱化，无 diff 依赖库）、以及其他二进制载荷的「Binary file」提示——按 (server,path) 缓存内容（重新激活已缓存标签不再拉取；仅成功载荷入缓存，反应式 view-state memo 在取到新缓存后立即刷新渲染），含加载中/错误 + 重试/空文件/无标签页空态，并预留 `fullscreen` 属性供 M7 移动端全屏页；DesktopShell 主区新增 Chat|Files 标签切换——Files 页挂载查看器，侧栏文件树点击文件即打开标签并切到主区 Files 视图，上下文重建时 viewer store 与会话/消息/todos 一并清空；L1（store 动作：打开/激活/关闭顺序 + 邻位激活 + per-server 隔离 + 清空）与 L2（标签栏、文本/图片/diff/二进制/空文件分支、缓存不重拉、错误重试、标签切换内容保持、壳层主区切换与树点击打开）测试覆盖全流程（TASK-M4-03）

- 快速打开文件搜索（⌘/Ctrl+P）：基于 kobalte 的对话框（`src/features/files/QuickOpen.tsx`）走 `GET /find/file`，输入防抖 150ms 并在途响应作废（沿用 PromptBox @ 菜单模式），↑↓ 循环选择 + Enter/Esc 键盘导航与鼠标点选打开；结果排序——大小写不敏感的路径前缀命中在前、其次子串命中、再次服务端其他模糊命中，同一桶内 per-server 最近打开文件优先置顶——由纯函数 `rankResults` 实现；新增 per-server 最近打开文件记忆（localStorage `oc-recent-files:{serverId}`，上限 20 条，打开即写入，空查询时展示并带「Recent」区块，含无最近文件/搜索中/无匹配三种空态），并预留 `variant` 属性供 M7 移动端顶部搜索页；DesktopShell 新增临时 ⌘/Ctrl+P window 按键钩子（在文本输入控件内输入时不拦截，M8 将迁入命令面板注册表）打开对话框，选中文件即打开查看器标签并切到主区 Files 视图（与侧栏树点击行为一致）；L1（recentFiles + rankResults 排序）与 L2（对话框生命周期、防抖/请求/合并/过期丢弃、键盘导航、打开副作用、各空态、壳层快捷键守卫与打开全流程）测试覆盖全链路（E2E E08 随 M10 基建落地）（TASK-M4-04）

- 全文搜索面板（⌘/Ctrl+⇧F）：搜索栏 + 结果列表（`src/features/files/SearchPanel.tsx`）走 `GET /find`，输入防抖 300ms 并在途响应作废（沿用 QuickOpen 模式），Enter 立即执行搜索；正则开关（.*）先在客户端校验模式（非法正则展示内联提示且不发起请求），随后发送仅 mock 支持的 `regex=true` 查询参数——1.18.11 契约只暴露 `pattern`，真实服务端会忽略该参数并按字面匹配（限制已记录于 docs/api-coverage.md）；结果按文件分组渲染（路径头 + 命中计数，每行显示行号与高亮命中段的行文本——优先使用服务端 submatches，缺失时客户端按所搜模式（字面/正则）取首个命中，均由纯函数 `groupByFile` / `highlightSpans` 实现）；点击命中即打开查看器标签并定位该行（viewer store 新增 `activeLine` + `setActiveLine`；FileViewer 为高亮行 span 打上 data-line 编号，待标签内容渲染且查看器可见后滚动该行到视口中央并短暂闪烁，随后清除目标——新增 `visible` 属性使搜索面板覆盖查看器时延迟消费，行号超范围则直接清除不滚动）；Files 视图内查看器与搜索面板并排常驻（两者均保持挂载，结果在往返跳转后不丢失），主区标签栏新增搜索切换按钮；DesktopShell 新增临时 ⌘/Ctrl+⇧F window 按键钩子（文本输入控件内不拦截，重复按下在搜索/查看器间循环，M8 将迁入命令面板注册表）；mock server 的 `/find` 改为按 pattern 过滤 fixture（大小写不敏感子串，或 `regex=true` 时按正则解释；空 pattern / 非法正则返回空数组），新增多文件 fixture 与自测断言（含 submatch 偏移与行文本对齐校验）；L1（viewer activeLine 动作、分组与高亮辅助函数、find 正则参数）与 L2（面板防抖/合并/过期丢弃/Enter、正则校验与参数、分组渲染与高亮、命中打开副作用、空闲/加载中/无匹配/错误状态、查看器行滚动 + 闪烁 + 隐藏延迟消费、壳层 ⌘⇧F + 切换按钮 + 命中全流程）测试覆盖全链路（E2E E09 随 M10 基建落地）（TASK-M4-05）

- 快速打开对话框内的工作区符号搜索（⌘/Ctrl+P）：`#` 前缀查询（触发符加至少一个字符）将 `QuickOpen.tsx` 切换至符号模式，走 `GET /find/symbol`——请求前剥离 `#` 触发符，结果行渲染为符号类型字形（函数 ƒ / 方法 m / 类 ◇ / 接口 i / 变量 v / 常量 c，由纯函数 `symbolKindIcon` 映射，未知类型用中性符号兜底）加符号名与工作区路径，Enter/点击跳转到所在文件并定位目标行——LSP range 起点（0 基）经新增 `setActiveLine` 流程换算为查看器 1 基 data-line 目标，复用文件打开的全部副作用（查看器标签、最近文件记忆、主区视图切换）；删除 `#` 即回退到文件搜索，LSP 无结果时展示专属「No symbols found」空态，两种模式共享 150ms 防抖 / 过期响应作废 / ↑↓ 循环 / 点选等机制；纯函数集中于 `symbols.ts`（`isSymbolQuery` / `symbolQueryOf` / `symbolKindIcon` / `symbolPath`——将 `file://` URI 按活动项目目录折算为工作区相对路径——/ `symbolHitOf`）；mock server 的 `/find/symbol` 改为按符号名大小写不敏感子串过滤 fixture（空查询返回空数组），fixture 扩至 7 个符号覆盖多类型（函数/方法/类/接口/变量/常量且位置各异），自测断言覆盖过滤逻辑、位置形状（0 基 range 起点）与类型分布；L1（模式识别、触发符剥离、类型字形映射、URI→路径、0 基→1 基行号换算）与 L2（符号请求与类型/名称/路径渲染、Enter 与点击跳转并定位行、删除 `#` 回退、无符号空态、符号模式防抖合并与过期丢弃）测试覆盖全链路（TASK-M4-06）

- 会话与消息级 Diff 视图（⌘/Ctrl+D）：主区新增 diff 视图（`src/features/vcs/DiffView.tsx`）走 `GET /session/{id}/diff`（可带 `messageID` 过滤），将 `SnapshotFileDiff[]` 载荷按文件分组渲染——文件头含路径、`+N -M` 增删计数徽标与 added/deleted/modified 状态 chip，随后是统一 diff（绿色/红色增删行着色，旧/新行号随 @@ hunk 头跟踪，hunk/meta 头弱化显示，超过三行的未变更段折叠为「… N unchanged lines」展开手柄）或左右分栏视图（上下文行一对一配对、删除行与随后的新增行配对、头部横跨两栏），由视图内部 Unified/Split 按钮切换；无 patch 内容的文件以统计卡片呈现并附「Content not available for this diff.」说明；含加载 / 错误+重试 / 空态（「No changes in this diff」），挂载与 server/session/message-id 变化时带过期守卫重新拉取，diff store 版本号变化时静默重新拉取；解析/折叠/对齐辅助函数共享（`src/features/vcs/diffLines.ts`——`rowKindOf` 自 FileViewer 抽出、带行号的 `parseUnifiedDiff`、`foldGroups`、`alignSplit`），新增 diff store（`src/stores/diff.ts`——per-server/session 载荷加 `version` 计数器，`applyDiff` / `bumpDiffVersion` / `resetServer`），由事件路由器的 `session.diff` SSE 事件驱动（带载荷则整体替换，裸事件仅提升版本，两者都在上下文重建 / `server.connected` 重同步时重置）；DesktopShell 经临时 ⌘/Ctrl+D 按键钩子打开该视图（文本输入控件内不拦截；视图内再按：带过滤时清除过滤、无过滤时返回聊天，M8 将迁入命令面板注册表），diff 头部含 Back 按钮、「Session diff」标题，以及从消息进入时的 Message-{id} 过滤 chip（× 清除过滤）；消息菜单的「View diff」项全链路接线（`MessageList` → `MessageBubble` → `MessageActions.onViewDiff`）；mock server 自 M4-01 起已提供带 patch 内容与 messageID 过滤的 fixture；L1（diffLines 解析/折叠/对齐、diff store、事件路由含重置）与 L2（DiffView 按文件分组/徽标/统一行/折叠/分栏/空态/错误重试/message-id 重拉/版本号重拉、壳层 ⌘D 打开+循环+输入控件守卫、消息 view-diff 过滤 + chip 清除 + 返回）测试覆盖全链路（E2E E10 随 M10 基建落地）（TASK-M4-07）

- VCS 面板与补丁应用（TASK-M4-08）：主区新增 Changes 视图（`src/features/vcs/VcsPanel.tsx`），由 Files 标签栏的 git 按钮打开（自带 Back 头部返回 Files），展示当前分支 chip、工作区变更列表——每个文件一行，含 added/deleted/modified 状态字母 chip 与 `+N -M` 增删徽标，数据走 `GET /vcs` + `GET /vcs/status`——并含工作树清洁空态与 Refresh 按钮；Workspace diff 子视图渲染 `GET /vcs/diff`，复用共享的按文件渲染器（DiffView 的按文件分组抽取为 `DiffFileGroup.tsx`，`DiffMode` 类型移入 `diffLines.ts`，会话 diff 视图与工作区 diff 视图渲染完全一致），带 Unified/Split 切换与折叠；Apply 区块支持粘贴 unified patch 到文本框、弹确认对话框（应用会修改工作树）、POST `/vcs/apply` 并反馈结果——成功清空文本框并提升 VCS store 版本使状态与 diff 重新拉取，失败内联展示错误详情——非 git 工作区（无分支上报）渲染优雅的「Not a git repository」空态并隐藏 diff/apply 操作；新增 vcs store（`src/stores/vcs.ts`——per-server 分支 + 变更列表 + 版本计数器，`applyVcs` / `applyStatus` / `applyBranch` / `refresh` / `resetServer`），由事件路由新增的 `vcs.branch.updated` 分支（写入分支并提升版本，挂载中的面板据此重拉状态）与面板快照拉取共同驱动；DesktopShell 在三栏布局之下新增极简底部状态栏与分支 chip（`StatusBarBranch`——每个服务器挂载时拉取一次 `GET /vcs`，随 store 响应式更新，分支事件即时刷新；M9-07 将扩展 LSP/MCP/tokens 等项）；L1（vcs store 动作、事件路由含重置）与 L2（VcsPanel 分支头/变更行/徽标/清洁态/刷新/分支事件重拉、工作区 diff 子视图 + 返回、apply 确认 → POST → 成功刷新 + 清空 / 失败详情 / 取消 / 空补丁禁用、非 git 空态、壳层 Changes 视图打开 + 返回、状态栏 chip 拉取 + `vcs.branch.updated` 实时更新）测试覆盖全链路；mock server 自 M4-01 起已提供 `/vcs` 族 fixture 与 apply 自测断言（L3 契约）

- 全局权限请求队列（TASK-M5-01）：per-server 权限 store（`src/stores/permission.ts`——队列 + 版本号，按 id 去重的 `enqueue` / `dequeue` / `applyList` / `resetServer`），由事件路由新增的 `permission.asked`（入队，payload 即 PermissionRequest）与 `permission.replied`（按 requestID 出队）分支驱动，并在全量重同步与目录上下文重建时按服务器重置；权限领域服务（`src/services/permission.ts`——`list` 走 `GET /permission`，`reply` 走 `POST /permission/{requestID}/reply`）；全局权限卡片（`src/features/permissions/PermissionSheet.tsx`）挂载于 DesktopShell（顶层弹层，仅在活跃服务器有待处理请求时渲染），展示队列头——权限类型 chip、等宽 pattern chips、弱化的工具上下文（messageID · callID）、可展开的 metadata 详情与「1 of N waiting」指示器——并提供三个操作（允许一次 / 总是允许 / 拒绝）提交应答，仅在成功后出队（失败保留请求并内联报错；一次应答 POST 在途时按钮锁定；弹层不可被 Esc/遮罩关闭）；「总是允许」会记录会话内 remember 记忆（`src/features/permissions/remembered.ts`——权限+patterns 的无序稳定签名，按服务器隔离），之后同 pattern 的请求会被静默自动应答「always」（在途时隐藏卡片，静默应答失败则带错误浮出卡片，绝不静默丢弃）；mock server 注册 `/permission` fixture 列表与带校验的应答端点（非法 reply → 400），fixture 模式下将录制的 `permission.asked` fixture 映射到列表路由，并新增自测断言；L1（服务载荷组装、store 动作、事件路由含重置、remember 记忆）与 L2（卡片渲染 / 队列推进 / 详情展开 / 三操作 + 出队 / 在途锁定 / 失败重排队 / 自动应答静默 + 失败浮出 / Esc 守卫 / variant 空操作）测试覆盖全链路；原生系统通知与待处理角标延后至 M8-06 / 角标命令（TODO 见 architecture.md §7.2）

- 斜杠命令系统（TASK-M5-03）：输入框首字符 `/` 弹出命令过滤菜单（基于 `GET /command`，显示名称 + 描述 + 参数提示（等宽字体），↑↓/Enter/Esc 键盘导航，失焦/Esc 关闭；命令列表在首次触发时按挂载拉取一次并带在途防重，且与 `@` 菜单严格互斥——引用触发条件成立时优先）；选中项将输入填充为命令模板——`/{name}` 加首个参数提示作为可编辑占位文本（`commandTemplate`）；提交与已知命令（不区分大小写，`matchCommand`）匹配的消息时改走 `POST /session/{id}/command`，请求体为 `{ command, arguments }`（arguments 为名称后的文本），并清空输入——回复经常规 SSE 消息流渲染；不匹配的 `/…` 消息回退为普通提示词路径；命令执行失败时展示内联错误横幅并恢复提交文本以便重试；mock server 提供 4 条命令的 fixture（含带参数提示的自定义服务端命令）与带校验的命令执行端点（缺 command/arguments → 400），并新增自测断言；L1（服务载荷组装 + 模板/匹配纯函数）与 L2（菜单开启/过滤/导航/插入/Esc/@ 互斥/执行/未匹配回退/失败恢复/全链路）测试覆盖，另有 L3 契约块（list + run）

- Agent 问题卡片（TASK-M5-02）：per-server 问题 store（`src/stores/question.ts`——队列 + 版本号，按 id 去重的 `enqueue` / `dequeue` / `applyList` / `resetServer`），由事件路由新增的 `question.asked`（入队，payload 即 QuestionRequest）与 `question.replied` / `question.rejected`（按 requestID 出队）分支驱动，并在全量重同步与目录上下文重建时按服务器重置；问题领域服务（`src/services/question.ts`——`list` 走 `GET /question`，`reply` 走 `POST /question/{requestID}/reply`，按 schema 携带 `{answers}` 请求体——每个问题一个所选 label 数组，`reject` 走 `POST /question/{requestID}/reject`，无请求体）；全局问题卡片（`src/features/questions/QuestionSheet.tsx`）挂载于 DesktopShell 权限卡片旁，展示队列头——header chip、完整问题文本与「1 of N waiting」指示器——并提供两种回答形态：选项按钮（点击即以该 label 应答）或无选项问题的自由输入 textarea + Send，另有 Reject 操作；应答仅在成功后出队（失败保留问题并内联报错；一次 POST 在途时控件锁定；自由输入草稿在失败后保留，重试无需重打），已回答的问题由其 replied/rejected 事件清除（应答与已落定请求竞争时内联报错，事件随后清空队列），无问题内容的畸形请求防御性渲染（提示 + 仅 Reject），弹层不可被 Esc/遮罩关闭，`variant` prop 预留给 M7 移动端底部弹层；mock server 注册 `/question` fixture 列表（一个带选项问题 + 一个自由输入问题）、带校验的应答端点（非数组 answers → 400）与拒绝端点，fixture 模式下将录制的 `question.asked` fixture 映射到列表路由，并新增自测断言；L1（服务载荷组装、store 动作、事件路由含重置）与 L2（选项表单应答 / 自由输入发送含空草稿禁用 + 草稿保留 / 拒绝 / 在途锁定 / 失败重排队 / 队列推进 / 无问题防御渲染 / Esc 守卫 / variant 空操作）测试覆盖全链路；原生系统通知与待处理角标延后至 M8-06（与权限卡片共用，TODO 见 architecture.md §7.2）

- Agent 模式选择器（TASK-M5-04）：输入框上方的工具栏行新增 agent chip（`src/features/models/agents.ts`——纯函数 `visibleAgents` / `agentColor`（缺失时灰色兜底）/ `cycleAgentName`），展示当前生效 agent 名称与色点；目录来自新增 agent 领域服务（`src/services/agent.ts`——`list` 走 `GET /agent`），每次挂载拉取一次并经 per-server agents store（`src/stores/agents.ts`——目录 + loaded 标志 + `activeBySession` 选择记录，`setAgents` / `setAgentForSession` / `agentNameFor`，解析链为：已记录选择 → 会话自身 agent（仍可见时）→ 首个可见 agent）跨挂载复用；点击 chip 弹出可见（非 hidden）agent 菜单——名称、mode 与描述，当前项带对勾与强调底色——选择按会话记录（服务端无 agent PATCH，选择在客户端按会话持久）；输入框内 Tab 在无输入菜单占用按键时循环切换可见 agent（菜单打开时保持默认焦点行为），发送链路携带生效 agent——`sendPrompt` 新增可选 agent 参数并写入 `prompt_async` 请求体（已对照 1.18.11 契约核实 `agent: string` 字段），同时映射到乐观用户消息，build/plan 切换可直达服务端；mock server 注册 `/agent` fixture 列表（build/plan 带十六进制色值 + hidden 的 architect，permission/options 符合 schema），自测断言覆盖目录形状与 hidden 契约，未实现端点 501 探测改走不存在的 `/model` 路径；L1（服务载荷组装、store 解析链 + 重置、纯函数测试）与 L2（chip + 色点、隐藏过滤 + 描述/mode + 对勾的菜单、跨挂载按会话持久、Tab 循环、prompt 请求体携带 agent、Esc 关闭）测试覆盖全链路

- Provider OAuth 授权流（TASK-M5-07）：provider 领域服务新增 `oauthAuthorize`（`POST /provider/{id}/oauth/authorize`，请求体为契约的 `{ method: 认证方式索引, inputs? }`——已对照 1.18.11 契约核实 method 为数字索引，响应 `ProviderAuthAuthorization` `{ url, method: auto|code, instructions }` 由服务端决定流程类型）、`oauthCallback`（`POST /provider/{id}/oauth/callback`，请求体 `{ method, code? }`）与 `oauthPoll`（同一回调端点携带 mock 扩展 `poll: true`——契约无状态查询端点，auto 模式以 2s 间隔轮询回调直至成功（最长 60s），偏差已记录于 docs/api-coverage.md §5）；OAuth 授权对话框（`src/features/settings/providers/ProviderOAuth.tsx`，kobalte Dialog，由替代 M5-06 延后提示的 Authorize 按钮打开）先 POST authorize，再经 opener 插件（`@tauri-apps/plugin-opener` 的 `openUrl`，`opener:default` 能力已授权）在系统浏览器打开返回 URL，随后按流程分支——auto 模式轮询回调至完成（纯函数 `pollUntil(fn, {intervalMs, timeoutMs, signal})` 遇瞬时失败继续轮询、超时/中止返回 false；成功即关闭对话框并重拉 provider 列表刷新连接状态、超时内联报错、取消/Esc/卸载中止轮询），code 模式展示服务端 instructions 并输入授权码提交（空草稿与在途提交均锁定，拒绝的 code 或提交失败保留草稿可重试）；mock server 注册 OAuth 路由（authorize 创建按 state 区分的待定会话并返回其浏览器 URL，`GET /oauth/authorize?state=` 页面模拟浏览器往返——访问即置完成，callback 对 code 流程校验固定码 `mock-oauth-code`、对 auto 轮询返回 pending/completed，非法 method 索引或 code → 400），并新增带 code 流程 oauth 方法的 google provider（azure 保持 auto 流程 fixture），self-test 覆盖双流程端到端；L1（服务载荷组装含目录透传、pollUntil 时序/超时/取消/拒绝）与 L2（对话框 authorize → 打开浏览器 → 轮询序列 → 成功关闭 + 刷新 / 超时报错 / 取消停止轮询 / code 输入 → 提交 → 关闭 / 拒绝与失败保留 / 空草稿锁定 / authorize 失败 / ProviderKeys Authorize → 对话框 → 取消）测试加 L3 契约块覆盖全链路

- Provider API Key 管理（TASK-M5-06）：provider 领域服务新增 `authMethods`（`GET /provider/auth`——按 provider id 的 ProviderAuthMethod 记录）、`setKey`（`PUT /auth/{providerID}`，请求体为 schema 的 `{ type: "api", key }` ApiAuth 形态）与 `removeKey`（`DELETE /auth/{providerID}`），三者均支持目录参数；新增极简设置页（`src/features/settings/SettingsPage.tsx`，作为 M9-04 设置中心的基础——含 Back 头部、占位分区导航与 Providers 分区），由 DesktopShell 主区 Chat|Files 标签栏新增的齿轮按钮打开（自带 Back 头部返回聊天视图，与 diff/changes 视图同款）；provider 密钥面板（`src/features/settings/providers/ProviderKeys.tsx`）经模型 store 列出服务器 provider 并带连接状态徽标，按认证方式渲染表单——`api` 方式为 API Key 表单（password 输入框、空值或请求在途时 Save 禁用，已连接 provider 另附 Remove 按钮及两步内联确认；密钥值永不回读——契约无密钥 GET——已存密钥以连接徽标 + 「Key set」提示呈现），`oauth` 方式渲染延后提示（TASK-M5-07）；保存/删除密钥后重新拉取 `GET /provider` 并写入模型 store，连接状态随之刷新（失败时草稿保留可重试，加载/变更错误内联展示并带 Retry）；mock server 注册 `/provider/auth` fixture（openai/anthropic 为带 prompts 的 api 表单、azure 为 oauth）、带校验的 `PUT /auth/{providerID}`（缺 type/key → 400）与返回 true 的 DELETE 端点，self-test 覆盖认证方式形状与 set/remove 契约；L1（服务载荷组装含目录透传）与 L2（面板列表 / api 表单保存 → PUT + 刷新 / 连接状态刷新契约 / 空密钥守卫 / 删除确认 + 取消 / 失败保留 / 加载错误 + 重试 / 页面骨架 + Back / 壳层齿轮按钮导航）测试覆盖全链路

- 模型选择器（TASK-M5-05）：新增 provider 领域服务（`src/services/provider.ts`——`list` 走 `GET /provider`，`configProviders` 走 `GET /config/providers`，均支持目录参数；对照 OpenCode 源码核实 1.18.11 契约：`/provider` 返回 `{ all, default, connected }`，`default` 为 providerID → 默认模型 id 的记录，`connected` 为已认证 provider id 数组，`/config/providers` 返回 `{ providers, default }` 且 default 记录语义一致，`Provider.models` 为以模型 id 为键的记录）；per-server 模型 store（`src/stores/models.ts`——providers + connected id 数组 + `defaultModels`（config 记录，picker 的 Default 标记以它为准）+ `defaultModel` + loaded 标志 + `activeBySession` 选择记录，`setProviders` / `setConfigDefault` / `setModelForSession` / `activeModelFor`，解析链为：已记录选择 → 会话自身模型（仍在目录中时）→ config 默认模型 → 首个已连接 provider 的首个模型）；ModelPicker 对话框（`src/features/models/ModelPicker.tsx`，kobalte Dialog）由 composer 工具栏 agent chip 旁新增的 model chip 打开——chip 展示生效模型名称与所属 provider（与 agents 同款：挂载时经 store 拉取一次）——内含搜索框（按 provider 或模型名过滤，provider 名命中时展示其全部模型）、按 provider 分组的模型列表（未连接 provider 带「Not connected」标签、整组灰显且选择按钮禁用）、来自 capabilities 字段的能力徽标（tools 取自 `toolcall`、vision 取自 `input.image`、reasoning）、每百万 token 成本提示（`cost` 字段，如「$1.25/M in · $10/M out」）与上下文上限提示（`limit.context`，如「400K context」，纯函数集中在 `src/features/models/models.ts`）、跟随 `/config/providers` default 记录的 Default 徽标（picker 数据与 config 默认一致——验收点）、当前模型上的 Current 对勾，以及星标收藏区（localStorage `oc-fav-models`，键为「providerID:modelID」）；选择按会话记录在 store 中（服务端无按会话的模型 PATCH，选择在客户端按会话持久），发送链路携带生效模型——`sendPrompt` 新增可选 model 参数并写入 `prompt_async` 请求体（已对照 1.18.11 契约核实 `model: { providerID, modelID }` 字段），同时映射到乐观用户消息；mock server 注册 `/provider` 与 `/config/providers` fixtures（openai/anthropic 已连接且能力/成本/上下文各具差异，azure 未连接且带模型——覆盖灰显契约），自测断言覆盖目录形状、default 记录 ↔ 模型引用、未连接契约与 config 默认和 `/provider` 的一致性（含 fixture 模式回退）；L1（服务载荷组装、store 解析链 + 重置、纯函数单测）与 L2（picker 分组 / 徽标 / 成本 + 上下文提示 / 未连接灰显禁用 / Default 标记 / 收藏切换 + 分区 + localStorage 恢复 / 搜索 / 选择 → store + chip / Current 标记 / Esc + Close，chip 展示 + 跨挂载按会话持久 + 请求体携带模型）测试覆盖全链路
