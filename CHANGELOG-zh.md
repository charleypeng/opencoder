# 变更记录

本文件记录本项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- 会话分享与二维码 (TASK-M6-05)：DesktopShell 聊天头部新增分享图标（会话已分享时着色并标记）+ 会话行菜单新增「Share」项，两者都打开分享对话框（`src/features/sessions/ShareSessionDialog.tsx`）；未分享的会话提供 Share 动作（`POST /session/{id}/share`——经 1.18.11 契约核对无请求体，返回更新后的会话），分享后对话框展示只读分享 URL + 复制按钮（Clipboard API + execCommand 兜底，「Copied!」反馈）、由 URL 生成的二维码（`qrcode` 包 → data URL → `<img>`，160px，供移动端扫码）、「Open in browser」按钮（tauri `@tauri-apps/plugin-opener` 的 `openUrl`，能力已授予）、Unshare 动作（`DELETE /session/{id}/share`）以及「扫码设备需能访问服务器」的提示；所有状态经会话 store 流转——`shareSession`/`unshareSession` 动作以服务端返回的更新会话替换 store 中的会话，因此已分享徽标（会话行内的「shared」小徽章，tooltip：「Shared — anyone with the link can view」）与 URL 完全由契约的 `Session.share: { url }` 标记推导（经 1.18.11 OpenAPI 核对，无独立的分享状态字段或事件），分享/取消状态与服务端保持一致；失败路径（分享/取消/打开浏览器）内联报错并保留原状态，对话框在请求进行中不可关闭；会话服务新增 `share(sessionID, dir?)` 与 `unshare(sessionID, dir?)`（directory 透传）；mock server 注册分享端点族（POST 返回带 `share: { url }` 的更新会话，DELETE 返回不带标记的更新会话）并附自测断言，另有 L1（服务载荷拼装（含 directory 透传）、shareSession/unshareSession store 替换与失败路径）、L2（对话框：分享 → URL + 二维码图 + 复制 + 打开浏览器调用 opener、取消分享 → 清空并回到 Share 按钮、已分享直开、复制反馈、分享/取消/打开失败路径、可达性提示、Esc；列表：shared 徽标、行菜单 Share 项打开对话框、菜单分享后行出现徽标；壳层：头部分享图标开关/已分享标记、分享 → 二维码 → 打开 → 取消分享全流程）与 L3（live-mock 分享设置标记、取消清除标记）测试
- 消息回滚与恢复 (TASK-M6-04)：消息菜单「Revert to here」（任意消息均可用——用户与助手消息皆可）打开二次确认对话框，说明该消息之后的文件变更将在服务端回退（取消不发起任何请求；回滚失败时对话框保留并显示内联错误），快照芯片（TASK-M3-02）作为第二个入口接通——点击芯片即对承载快照的消息发起同一回滚流程；会话服务新增 `revert(sessionID, messageID, dir?)`（`POST /session/{id}/revert`，携带契约要求的 `{ messageID }` 请求体——可选的 `partID` 本任务未使用——返回更新后的会话）与 `unrevert(sessionID, dir?)`（`POST /session/{id}/unrevert`，无请求体），`revertSession`/`unrevertSession` 动作以服务端返回的更新会话替换 store 中的会话；回滚状态完全由契约中的 `Session.revert` 标记推导（`{ messageID, partID?, snapshot?, diff? }`——经 1.18.11 OpenAPI 核对，无逐消息标记、v1 亦无专属事件）：MessageList 在标记存在时渲染吸顶「Reverted to message …」条 + 一键 Unrevert 按钮（POST unrevert 清除标记），并将回滚点之后已加载的消息灰显（回滚点本身与更早消息保持活跃），状态统一由会话 store 驱动（SSE `session.updated` 与 REST 响应共用同一数据源）；mock server 注册回滚端点族（revert 校验 messageID 必须存在且为已知 fixture 消息——缺失/非法或未知均返回 400 BadRequestError——并返回带标记的更新会话；unrevert 返回不带标记的更新会话）并附自测断言，另有 L1（revert/unrevert 服务载荷拼装（含 directory 透传）、revertSession/unrevertSession store 替换与失败路径）、L2（消息菜单项禁用/回调（含右键菜单）、快照芯片惰性 vs 触发行为、列表内回滚条 + 灰显 + unrevert、壳层取消确认/确认回滚/unrevert/快照芯片全流程）与 L3（revert 设置标记、未知 messageID → 400、unrevert 清除标记）测试
- 会话分叉 (TASK-M6-03)：消息菜单「Fork from here」（任意消息均可用——用户与助手消息皆可，opencode 在消息点分叉）或会话行菜单「Fork」（整会话分叉）创建子会话；会话服务新增 `fork(sessionID, messageID?, dir?)` 封装（`POST /session/{id}/fork`，可选 `{ messageID }` 请求体，schema 派生的 SessionForkInput），`forkSession` 动作将返回的子会话写入 store 并自动打开（upsert + set active，来源标注由子会话的 parentID 承担）；会话列表将分叉子会话缩进渲染在父会话正下方并带「fork」徽标（tooltip：「Forked from <父标题>」）——父会话匹配搜索时整棵子树一并显示，仅子会话匹配时子会话带徽标独立成行，更深层嵌套递归渲染（M6-07 将以完整树视图替换）；DesktopShell 接线消息级分叉流程（失败在聊天区上方的内联横幅呈现，会话级失败在列表横幅呈现）；mock server 注册分叉端点（返回带 parentID 的子会话；messageID 存在时必须为已知 fixture 消息，否则 400 BadRequestError）并附自测断言，L1（服务载荷拼装（含/不含 messageID 与 directory 透传）、forkSession store/激活行为与失败路径）与 L2（消息菜单项禁用/回调（含右键菜单与助手消息行）、列表缩进/徽标/搜索交互、行菜单分叉流程、壳层分叉并自动打开全流程）测试，另有 L3 契约块（分叉、已知 messageID、未知 messageID → 400）
- 终端面板（xterm.js）(TASK-M6-02)：桌面壳新增 Terminal 主视图（`src/features/terminal/TerminalPanel.tsx`）——从 Chat|Files 标签栏的终端图标或临时 ⌘/Ctrl+J 快捷键打开（输入控件聚焦时屏蔽；再按一次切回聊天视图，自带 Back 头返回聊天，打开时与 diff/changes/settings 视图一样隐藏标签栏）——多 Tab 管理与 per-server ptys store 联动：Tab 以 pty id 为键、显示 shell/命令标题，每 Tab 带关闭按钮，"+" 按钮弹出 shell 选择器（`GET /pty/shells`：默认 shell 行、逐 shell 行（不支持者带标记）、加载/出错+重试状态、创建失败内联错误、Esc 关闭），经 `POST /pty`（`{ command, title }` 或服务器默认）创建终端并激活新 Tab；Tab 切换时所有实例保持挂载（CSS 隐藏而非卸载——PTY 随 WS 断开而死亡，切换绝不能断链）；关闭 Tab 时 DELETE 服务端 pty（终止进程）并从 store 移除；每个 Tab 渲染一个 `TerminalInstance`（`src/features/terminal/TerminalInstance.tsx`），拥有一个 xterm.js 6 实例（新增 `@xterm/xterm` + `@xterm/addon-fit` 依赖，主题取设计令牌（暗色兜底），字号 13，fit addon），经 M6-01 的 Rust WS 通道连接：入站 ArrayBuffer 帧 UTF-8 解码写入终端、按键 UTF-8 编码经 `ptySend` 发送、尺寸变化经 `PUT /pty/{id}` `{ size: { rows, cols } }` 同步（契约规定的 REST resize 通道）、ResizeObserver 监听容器尺寸变化重 fit（容器隐藏时跳过，避免隐藏 Tab 把 PTY 缩到 1×2 并 PUT 给服务器）、通道关闭或建连失败将 store 中的 pty 标记为 exited；退出 Tab 显示 "Process exited (code N)" 提示 + 关闭按钮并保留至用户关闭；卸载时关闭通道并 dispose 终端；L2 测试覆盖面板（空状态、store 实时 Tab、shell 选择器创建流程 + 默认行 + 列表重试 + 创建失败、Tab 切换保持实例挂载、关闭 → 通道关闭 + DELETE + 相邻激活、退出 Tab 提示）与实例（挂载/主题、通道接线、UTF-8 出入、resize PUT、可见性守卫的 fit、通道关闭 → 退出标记 + 提示、退出不建连、卸载清理）；E2E E11 随 M10 基础设施落地（docs/tasks/M6.md 已注明）
- PTY 服务层与 Rust WebSocket 通道 (TASK-M6-01)：类型化 PTY 领域服务（`src/services/pty.ts`——list/create/get/update/remove 覆盖 `GET,POST /pty` 与 `GET,PUT,DELETE /pty/{id}`、`shells` 覆盖 `GET /pty/shells`、`connectToken` 覆盖 `POST /pty/{id}/connect-token`，schema 派生类型 + directory 透传）；Rust WS 数据通道（`src-tauri/src/transport/ws.rs`——`pty_ws_connect`/`pty_ws_send`/`pty_ws_close` Tauri commands）：connect-ticket 获取与 `ws(s)://{base}/pty/{ptyID}/connect?ticket=…` URL 组装全部在 Rust 侧完成（1.18.11 契约参数名为 `ticket` 而非 `token`），tokio-tungstenite + rustls 建连，二进制帧以 `{ "bytes": [...] }` JSON 包裹经单个 tauri Channel 推送前端（tauri 2.11 无原始字节 Channel send），终止以 `{ "type": "pty.ws.closed" }` 控制帧通知，不实现重连（PTY 随 WS 断开而死亡），cargo 进程内测试覆盖帧编解码、URL 组装、注册表生命周期、服务端关闭清理与完整的 token→建连→回显往返；TS 门面（`src/services/ptyWs.ts`——`ptyConnect`/`ptySend`，返回 `{ connectionId, close }`，bytes 信封 → ArrayBuffer，关闭回调）；per-server ptys store（`src/stores/ptys.ts`——列表/顺序、upsert、退出标记、删除、reset）由事件路由新增的 `pty.created`/`pty.updated`/`pty.exited`/`pty.deleted` 分支驱动，并在重同步时随服务器重置；Mock Server 注册 PTY 路由族（fixture 列表 + shells fixture，动态 create/get/带校验的 resize update/delete/connect-token，`GET /pty/{id}/connect` 返回 426 Upgrade Required + JSON 说明——express 无法原生 WS 升级），另附独立 WS echo 服务器（`tests/mock-server/ws-echo.mjs`，基于 `ws` 包）与含二进制发送/回显往返的自测断言；L1（服务载荷组装、门面信封处理、ptys store、事件路由含重置）与 L3（live-mock PTY 契约 + WS echo）测试覆盖全链路；协议验证记录（contract-based，待真实服务端确认——本环境无真实 `opencode serve`）已写入 docs/tasks/M6.md 附录，含偏差说明（ticket vs token、契约将 connect 文档化为普通 HTTP boolean、resize 仅走 REST）
- Skill 引用与会话内 shell 命令 (TASK-M5-08)：输入框 `@` 引用菜单新增 skills 分组（位于文件结果上方）——技能目录（`src/services/skill.ts`，GET /skill）在首次 `@` 触发时拉取一次（按挂载缓存、请求中防重），查询按客户端本地过滤，行内显示技能名与描述，选中插入纯文本 `@skillName` 引用（服务端在回显消息中将 `@name` 提及解析为 AgentPartInput part，与 `@path` 解析为文件 part 一致——客户端不做 part 映射，见 docs/api-coverage.md 说明）；`!` 前缀将发送路由到 `POST /session/{id}/shell` 而非普通提示（`src/features/sessions/sendShell.ts`——`shellCommandOf` 为纯路由函数，单独的 `!` 回退到普通提示路径）：请求体携带会话当前 agent（1.18.11 契约必填）与当前 model，同步返回的 `{ info, parts }` 消息一次性写入消息 store（回复以普通 assistant 消息渲染，无 SSE 回显），`!` 条目不记入提示历史，失败时恢复已提交文本并显示内联错误横幅；会话服务新增 `shell` 封装（schema 派生的 ShellRunInput/ShellRunResult 类型）；Mock Server 提供 3 技能 fixture（契约无 hidden 字段——隐藏技能由服务端过滤）、带校验的 shell 端点（缺 agent/command 或 model 形状错误 → 400，返回创建的 assistant 消息），以及接受 AgentPartInput 形状的 prompt_async part 校验，均含自测断言；L1（技能服务载荷组装、shell 服务载荷组装、`shellCommandOf` 路由 + `runShell` store 应用、@ 菜单 `atEntriesFor`/`atInsertText` 纯函数）、L2（@ 菜单 skills 分组过滤/插入/按挂载一次拉取、shell 发送携带 agent+model、回复经 MessageList 渲染、失败恢复、单独 `!` 回退、普通提示不受影响）与 L3（skills 列表 + shell 执行契约）测试覆盖全链路
- Tauri 2 + SolidJS 工程脚手架，devtools 仅限 debug 构建启用 (TASK-M0-01)
- 设计令牌与 Tailwind CSS v4 基座，支持深浅色主题切换 (TASK-M0-02)
- OpenAPI 类型生成管线：`openapi-typescript` 从版本锁定契约生成类型，带漂移检测 (TASK-M0-03)
- Mock OpenCode Server REST 骨架：健康检查 / 项目 / 会话端点、故障注入、可选 Basic Auth (TASK-M0-04)
- Mock Server SSE 流与场景脚本：happy-chat、permission-flow、question-flow、sse-drop (TASK-M0-05)
- 真实响应样本 fixtures，含录制脚本与 schema 校验 (TASK-M0-06)
- CI 流水线与 `pnpm verify` 质量门禁，husky + lint-staged 提交前钩子 (TASK-M0-07)
- 双语工程文档骨架：README / CHANGELOG（中英）+ CONTRIBUTING (TASK-M0-08)
- Rust REST 传输通道（reqwest/rustls、Basic Auth、超时、取消、错误分类）与 TS ApiClient 门面（invoke/fetch 双传输实现）(TASK-M1-01)
- Rust SSE 订阅管理器（跨 chunk 逐行解析、`tui.*`/`workspace.*` 事件过滤、16ms 批刷 Channel 推送、指数退避重连、心跳判死）与 TS 订阅门面（tauri Channel 封装）(TASK-M1-02)
- Rust 服务器注册表：持久化 CRUD commands（`list_servers`、`add_server`、`update_server`、`remove_server`、`resolve_server_base_url`），基于 tauri-plugin-store，带 `servers-changed` 同步事件；传输通道改为经由注册表解析服务器地址 (TASK-M1-03)
- Rust 健康监控器：每服务器独立 15s `GET /global/health` 轮询（复用 REST 传输层），记录延迟与版本，连续 3 次失败判定 down 并支持恢复，状态变化经 `server-health` 事件推送前端；新增 SolidJS 连接 store 同步健康快照 (TASK-M1-04)
- 添加服务器向导：名称/URL/可选认证表单，URL 规范化、实时「测试连接」探测（显示版本/延迟）、明文 HTTP 风险提示，保存走注册表 commands；并为注册表 commands 新增类型化 TS 封装 (TASK-M1-05)
- 服务器导航首页：App 启动落点，响应式服务器卡片网格（名称/URL/状态灯/版本/延迟/最近连接），经 `servers-changed` 与 `server-health` 事件实时更新；卡片右键菜单（含菜单按钮）提供编辑/重连/删除（二次确认）；添加服务器向导支持编辑模式（`update_server`）；空状态引导页 (TASK-M1-06)
- mDNS 局域网自动发现：Rust 扫描 `_opencode._tcp`（及 `opencode serve --mdns` 发布的 `_http._tcp` 广播），按实例去重并 emit `server-discovered` 事件，提供幂等的 `start_mdns_discovery` / `stop_mdns_discovery` / `get_discovered_servers` commands，局域网不可达时静默降级；添加服务器向导新增「附近的服务器」区块，支持一键填充与自动探测 (TASK-M1-07)
- 服务器工作区壳：三栏桌面骨架（服务器图标 Rail 含每服务器健康状态点与 ⌘/Ctrl+1..9 快速切换、侧栏、主区）替换占位视图；新增激活服务器 registry store 为各 per-server store 注入上下文（切换不串数据）；离开时预留 SSE 断开挂载点（M2 接入）；移动端壳占位（M7）(TASK-M1-08)
- 凭证重认证与连接错误处理：分类错误文案（`errorTitle`/`errorDetail`）集中在 `services/errors.ts`（401/网络不可达/超时/5xx/非法 URL/已取消/响应格式异常）；ServerHome 加载与重连失败使用可关闭的 `ErrorBanner`；401 触发凭证重输对话框，新凭证先经探测验证通过后才持久化（`update_server`）；TS 侧探测响应校验将非健康负载判定为 `invalid_response`（TASK-M1-09）
- 项目/会话/消息领域服务层：基于 ApiClient 的工厂形态类型化服务（`createProjectService` / `createSessionService` / `createMessageService`），覆盖 `/project`、`/project/current`、`/path`、会话 CRUD（`/session` GET/POST、`/session/{id}` GET/PATCH/DELETE）、`/session/status`、带 `limit`/`before` 分页的消息历史（`/session/{id}/message`）、`prompt_async` 与 `abort`；类型全部取自 schema，错误按 ApiError 透传；mock server 新增 `/path`、`/session/status`、创建/更新/删除、`prompt_async`（204）、`abort` 与支持 `limit` 的消息分页，并实现跨 fixture 根目录的 fixture 回退（TASK-M2-01）
- SSE 事件路由与归一化 store：per-server 会话 store（列表/顺序/状态/当前会话 id）、消息归一化 store（`parts: Record<id, Part>` + `order`，delta 追加 O(1)，delta 先于 part 到达时建 text stub，part/消息删除）、项目 store（项目列表 + 当前目录），以及 `events.ts` 路由表将 `session.*` / `message.*` / `message.part.*` 事件分发进各 store；`server.connected` 经 `subscribeToServerEvents` 触发 `syncAll` 全量对齐（会话列表 + 状态表 + 项目列表 + 当前目录）；L1 测试直接驱动 mock happy-chat 场景断言最终 store 状态（TASK-M2-02）
- 项目/文件夹切换器：Sidebar 上段下拉组件（项目名 + 路径、当前项高亮、每服务器「最近项目」localStorage 记忆，上限 5 条），数据来自 `/project` + `/project/current`；切换项目即切换全局 `?directory=` 上下文（显式 per-call directory 现在优先于全局值），DesktopShell 重建该目录的 SSE 订阅（先退订旧流、再订阅新目录并全量对齐，切换前清空会话/消息状态保证跨目录互不串扰），新增 `project.updated` / `project.directories.updated` 项目列表刷新路由；mock server 扩展为双项目 fixture（`/session` 与 `/project/current` 按目录返回隔离数据，新增 labs 会话列表）（TASK-M2-03）
- 会话列表与状态徽标：Sidebar 下段按本地时间分组（今天/昨天/本周/更早，周一为周起始，跨 DST 安全）渲染会话，每条会话带状态徽标（busy/retry 转圈、idle 圆点、错误红点含 message 提示），由 SSE 驱动 store 实时更新；本地大小写不敏感搜索过滤（按标题/slug，含无结果与空列表空态）、当前会话高亮、行选择接线（`setActiveSession` + 回调）、hover 操作占位「⋯」按钮（改名/删除由 M2-05 接线）与虚拟滚动准备注释；DesktopShell 在侧栏挂载会话列表，主区占位改为回显所选会话 id（聊天视图 M2-06/08 落地）（TASK-M2-04）
- 会话创建/改名/删除：「+ New session」按钮（侧栏头部 + 空态）经 `POST /session` 创建空会话并直接打开；行级操作菜单替换 M2-04 占位按钮，提供改名（预填对话框，Enter 提交 / Esc 取消，`PATCH /session/{id}`）与删除（二次确认对话框，`DELETE /session/{id}`）；三类变更均先落 store（乐观更新），失败时回滚到捕获的原值并在对话框内联/横幅展示错误；DesktopShell 主区占位改为由 store 的当前会话 id 驱动（TASK-M2-05）
- 消息历史渲染（初版）：主区聊天转录（`src/features/messages/`）在挂载/切换会话时拉取 `GET /session/{id}/message` 历史并合并进归一化消息 store（新增按消息 id 的 `infos: Record<id, Message>`，保留原单条 info 槽位兼容旧逻辑），用户气泡右对齐加 accent 底色、助手气泡左对齐，附 hh:mm 时间戳；Part 经 `TextPart`（纯文本保留空白与换行）、`ReasoningPart`（默认折叠，带预览与箭头展开）与 `ToolPart`（v1 卡片：工具名 + pending/running/completed/error 状态图标与文案，展开可见 JSON 美化后的 input 与原始 output/error）渲染；未支持的类型静默跳过；含加载/空态/内联错误 + 重试状态，流式 part 先于消息 info 到达时按助手角色兜底渲染，以及自动滚动（贴近底部时跟随、上翻暂停并显示「New messages」跳转按钮）；DesktopShell 为当前会话挂载消息列表（无会话时保留占位）（TASK-M2-06）
- Markdown 渲染与代码高亮：文本 Part 经 markdown-it 管线渲染（GFM：表格、删除线、任务列表、linkify、typographer），禁用原始 HTML 并将外链改写为新窗口打开（`target="_blank"` + `rel="noopener noreferrer"`），随后再过一层 DOMPurify（allowlist 扩展以放行 Shiki 内联样式、任务列表复选框与链接 target）；围栏代码块经懒加载的 Shiki 单例异步高亮（预载常用语言、其他内置语言包按需 `loadLanguage` 加载、任一步失败回退为纯转义 `<pre>`），每块代码带语言标签与复制按钮（Clipboard API + `execCommand` 回退，点击显示「Copied!」反馈）；基于设计令牌补齐 markdown 正文样式（标题/表格/任务列表/引用/行内代码/代码围栏）；安全用例覆盖双层消毒下的 script/onerror/javascript:/data: 注入以及复杂 markdown fixture 渲染（TASK-M2-07）
- 消息输入与异步发送：主区消息列表下方固定输入框（`src/features/sessions/PromptBox.tsx`），自适应高度 textarea（上限约 10 行）、⌘/Ctrl+Enter 发送（裸 Enter 只换行）、空输入 ↑ 召回并循环浏览每服务器 prompt 历史（内存态，上限 20 条，最近优先，↓/Esc 回退）；发送时先乐观插入本地用户消息（`local-*` 本地 id，会话 agent/model 字段映射为消息 schema 形态）并立即清空输入框，`POST /session/{id}/prompt_async` 失败时回滚乐观消息并展示内联错误横幅；POST 进行中锁定发送，store 状态 busy/retry 时锁定输入（占位符「Generating…」+ 顶部细进度条；Esc 中断由 M2-10 接线）；附件按钮为禁用占位（M3 接线）；DesktopShell 为当前会话挂载输入框；L2 测试覆盖发送/键盘/历史/锁定/失败流程，并含全链路用例（乐观发送后驱动 happy-chat SSE 场景，断言最终渲染包含用户消息与助手回复；E2E E03 随 M10 基建落地）（TASK-M2-08）
- 流式增量渲染管线：消息 store 新增 `messageParts` 分组映射（消息 id -> 有序 part id 列表，仅在 part 成员变化时整体替换）与每会话 `lastDeltaAt` 流式时间戳，并提供 `applyMessageBatch` 将整段历史载荷在一次 produce 内落地；转录列表改用手写虚拟列表（`createVirtualList`：固定估算高度 + 实测高度，ResizeObserver 感知流式行增高，带 overscan 与 `scrollToIndex`），长对话只挂载可见区间的行；每条消息抽成独立 `MessageBubble` 组件只订阅自身 info/parts，delta 到来时仅重渲染对应 part 行（消除 M2-06 每个 delta 全量重新分组渲染）；新增呼吸式打字光标（`TypingCursor`，追加在流式消息最后一个已渲染 token 的行内），由 `useStreamingIndicator`（busy 状态 + 5 秒 delta 窗口 + 1s 滴答定时器）驱动；会话生成期间聊天区顶部显示 2px 细进度条（删除 M2-08 PromptBox 自带进度条，统一以会话 busy 为唯一信号）；自动滚动跟随/暂停/跳转逻辑在虚拟列表之上保持可用；L1 测试覆盖分组不变性（文本 delta 不触碰 `messageParts`）、批量应用正确性/对账与 `lastDeltaAt`，含 1000-delta 性能基准；L2 覆盖气泡级细粒度重渲染（兄弟 part DOM 节点保持不变）、虚拟化行数、进度条与光标生命周期（fake timers）；性能基准套件（`perf.bench.test.tsx`）约束虚拟列表（1000 行 x 1000 次滚动定位）、300 条消息转录渲染与单 token delta 成本（TASK-M2-09）
- 中断生成与错误处理：会话生成中（busy/retry 状态）输入框的 Send 按钮替换为 Stop 按钮（方形危险图标，本地在途锁防止双击重复），点击即 `POST /session/{id}/abort`；Esc 经 window 级监听在任意焦点位置触发相同中断（生成时 textarea 被禁用，无法自行接收按键）；abort 失败以内联横幅呈现，服务端 `session.status` idle 事件到达后恢复 Send 按钮（store 驱动的输入自动恢复）；`session.error` 事件渲染为消息列表与输入框之间的可关闭横幅（`SessionErrorBanner`）：标题使用分类文案（`errorTitle`/`errorDetail` 新增限流分类——状态 429 或消息含 "rate limit"/"429" 提示 → "Rate limited — try again shortly"），原始消息置于可展开的「Show details」区；含 Retry 按钮，经共享 `sendPrompt` 管线（自 PromptBox 提取，输入框与横幅共用）重发该服务器 prompt 历史中的最后一条（重发失败以分类错误重挂横幅，成功则关闭）；横幅由 store 状态条目派生，任何替换状态（idle/busy）到达即自动隐藏；session store 新增 `dismissSessionError` 动作将 error 状态回退为 idle；L2 测试覆盖停止/Esc/abort 失败/idle 恢复、横幅渲染/关闭/自动隐藏/重试（含防双击重试）与限流文案，L1 测试覆盖 `dismissSessionError`、`getLastPrompt` 与 sendPrompt 管线（E2E E04 随 M10 基建落地）（TASK-M2-10）
- 工具调用完整状态渲染：按工具类型定制卡片（bash 终端视图含命令提示符与退出码、edit 内联 diff 预览、read/write 代码块、glob/grep 结果列表带计数徽标、通用 JSON 兜底），经工具注册表按工具名分发组件；完整四态状态机（pending 呼吸时钟 / running 旋转图标 + CSS shimmer 扫光 + 实时跳动耗时并在状态离开 running 时清理定时器 / completed 对勾 + 实测或 metadata 耗时 / error 红叉 + 危险色错误信息）；输入参数可折叠（原始 JSON 披露区），输出可展开并可复制（「Copied!」反馈）；all-parts fixture 补齐全部工具族与四个状态，每类工具卡片均有 L2 快照覆盖（TASK-M3-01）
- 文件/补丁/快照 Part 渲染：文件附件卡片（按 mime 区分 image/text/file 图标，文件名带 URL 路径兜底，点击展开内联预览——base64 data-URL 图片或可折叠文本块——仅读取 Part 自带的内联 source 文本、绝不加载远程 URL，无内容时显示「Content unavailable」提示）；补丁卡片（短提交哈希徽标 + 文件总数，每个改动文件一行，作为跳转 diff 的占位点击项，经 `onOpenDiff` 回调留给 M4 接线）；快照标识 chip（相机图标 + 短快照 id，暂为惰性样式并附 M6 revert 提示）；三类 Part 均接入 MessageBubble 的 part 分发，含 L2 测试与各类型 fixture 快照，以及 all-parts fixture 气泡整体渲染用例（TASK-M3-02）
- 步骤/子任务/Agent Part 渲染：步骤边界以细分隔线呈现（「Step」标签 + 可选起始快照短 id），步骤完成行展示 reason、格式化 token 数（k/M 取整）与 USD 费用；子任务卡片（prompt 标题 + 共享 agent chip，可展开描述与 model/command 元信息，附 M6 子会话导航占位提示——1.18.11 schema 无 status 与子会话 id 字段，故暂无状态徽标与导航）；agent 引用高亮 chip（accent 色调 + 机器人图标 + 提及来源 tooltip）；四类 Part 均接入 MessageBubble 的 part 分发；含 `formatTokens`/`formatUSD` 单元用例、每类 L2 测试与 fixture 快照；all-parts fixture 追加第二组 step 对与 agent part 以覆盖多步骤层级渲染，另含 all-parts fixture 气泡整体渲染用例（TASK-M3-03）
- 重试与压缩提示渲染：retry part 渲染为系统样式 chip（刷新图标 + 「Retrying (attempt N)」尝试次数 + 失败原因——限流错误即 429 状态码或消息含 "rate limit"/"429" 提示时用危险色，其余用弱化色）；调用方提供 `retryAt` 纪元毫秒时（1.18.11 schema 无调度字段）倒计时标签每秒滴答递减「next attempt in Xs」，无定时数据时回退为静态「Retrying…」，且定时器在组件卸载时清理；compaction part 渲染为「Context compacted」系统行（压缩图标），可展开 schema 提供的明细（自动/手动模式、overflow 标记、被截断的 tail part id），part 无明细字段时仅渲染单行且不出现展开开关；两类 Part 均接入 MessageBubble 的 part 分发；L2 测试覆盖尝试次数与错误展示、限流分类、倒计时滴答/归零回退/卸载清理（fake timers）、展开收起与单行回退，含 fixture 快照及 all-parts fixture 的气泡与列表整体渲染用例（TASK-M3-04）
- 消息历史分页与向上加载：转录列表打开时只拉取最新一页（`GET /session/{id}/message?limit=50`），用户滚动到顶部时自动加载更早一页（`before` 游标），聊天区上方显示细加载指示条，按消息 id 去重合并，并保持滚动位置（视口 scrollTop 随插入高度精确增长重新锚定到先前可见内容，前置插入行不会造成跳动，也不会误触「New messages」按钮）；消息 store 为 `applyMessageBatch` 新增前置插入模式（更早页面插入已加载历史之前，且不覆盖最新消息槽位）；抽出纯函数 `mergePages` 分页助手（去重 + 游标推进 + hasMore 启发式 + 服务端忽略 `before` 时的回放检测）供 L1 覆盖；mock server 现支持 `before` 游标（`limit` 返回最近一页、`before` 仅返回更早消息、未知 id 返回空数组），self-test 增加翻页链断言；新增 120 条消息长 fixture（生成脚本随仓库提交），L2 覆盖仅加载最近页、分段加载、加载指示、防重复请求、游标重叠去重、回放终止以及 1000+ 消息分段遍历（无跳动、无重复）（TASK-M3-05）
- 消息操作菜单：每条消息的 hover 工具条（「⋯」按钮，行悬停时显现）与右键上下文菜单共享同一组操作——复制全文（消息全部 text part 以换行拼接）、复制代码（仅提取 `围栏` 代码块内容，无围栏时禁用）、用户消息「编辑并重发」（对话框预填第一个 text part 内容，经 `PATCH /session/{id}/message/{mid}/part/{partID}` 提交整份 Part——响应回写 store——再走共享 sendPrompt 管线重新发 prompt；任一步失败对话框保持打开并内联报错）、用户消息「删除」（二次确认，`DELETE /session/{id}/message/{mid}`，乐观删除复用既有 store 动作，失败时经新增 `restoreMessage` store 动作将 info 与 parts 恢复到记录的原顺序位置）与「查看 diff」占位项（M4-07 接线前禁用）；message 服务新增 remove / updatePart / removePart 封装，mock server 新增 DELETE message 与 PATCH/DELETE part 路由及 self-test 断言；L1（服务载荷拼装 + store 恢复）、L2（动作回滚 + 组件复制/编辑/删除/右键菜单全流程）与 L3（真实 mock 契约）测试覆盖全链路（TASK-M3-06）
- Todo 实时任务面板：单会话任务清单（`src/features/sessions/TodoPanel.tsx`）在挂载/切换会话时经 `GET /session/{id}/todo` 拉取初值并写入新增的 per-server per-session todos store，渲染完全由 store 驱动，因此 `todo.updated` SSE 事件（在 `stores/events.ts` 路由：兼容 schema 的完整 `todos` 数组与防御性的单条 todo 载荷，`server.connected` 全量对齐与目录切换时按服务器清空）实时刷新列表；每行含状态图标（pending 空心圆 / in_progress 旋转图标 / completed 对勾 / cancelled 叉）、优先级色点（high 红 / medium 琥珀 / low 灰，缺失时省略）与已完成项的删除线 + 弱化文本，附「No todos yet」空态与拉取失败的内联错误 + 重试；DesktopShell 将其挂载在新增聊天头部行（会话标题 + Todos 按钮）触发的右侧固定抽屉中（Esc / 点击遮罩 / 关闭按钮均可关闭），组件预留 `variant` 属性供 M7 移动端底部 Sheet 使用；happy-chat mock 场景新增连贯的 todo 生命周期（in_progress → completed）事件流并附带 self-test 断言；L1（服务载荷拼装 + store 动作 + 事件路由载荷形态）、L2（面板渲染/空态/实时 store 更新/拉取与重试、抽屉开关/Esc/遮罩）与 L3（真实 mock 契约）测试覆盖全链路（TASK-M3-07）
- 输入框附件与 @ 文件引用：剪贴板图片与拖拽/选取的文件变为 textarea 上方的可删除附件 chips（图片与大型/二进制文件以 base64 data URL 读取，小型文本类文件以纯文本读取、发送时编码为 utf-8 data URL）；回形针按钮启用为系统文件选择器，另设 M7 图片选择器禁用占位按钮；输入框内词首 `@` 触发防抖（150ms、在途响应作废）文件引用菜单，走新增的 `createFindService().files()`（`GET /find/file`），支持 ↑↓/Enter/Esc 键盘导航与鼠标点选，选中后在光标处插入 `@路径`；共享 sendPrompt 管线新增 attachments 参数，将 FilePartInput 部件追加在文本部件之后（乐观插入与历史仅覆盖文本部件；纯附件发送亦可）；发送成功清空 chips、失败保留以便重试；新增纯函数 `attachmentToPart` 完成附件→schema 映射（data URL 透传、纯文本编码为 data URL、可选磁盘路径挂到 FileSource）；mock server 注册 `/find` 与 `/find/symbol` fixtures 及按 query 过滤的 `/find/file`（空 query 返回空数组），self-test 断言覆盖文件部件 prompt 与三个搜索端点；L1（find 服务参数拼装、附件→部件映射、FileReader 附件转换）、L2（粘贴/拖拽/文件选择 chips、删除 chip、带附件发送的请求体与成功清空/失败保留、@ 菜单防抖/键盘导航/插入/Esc、词首门槛）与 L3（真实 mock 搜索与文件部件契约）测试覆盖全链路（TASK-M3-08）
- 文件/搜索/VCS 服务层：基于 ApiClient 的工厂形态类型化服务——`createFileService`（`/file` 扁平 FileNode 树，可选 `path` 子树展开，`/file/content`、`/file/status`）、`createVcsService`（`/vcs` 分支信息、`/vcs/status`、`/vcs/diff` 契约必填的 `mode` 默认 git 并可传 `context`、`/vcs/diff/raw` 从 `bodyText` 取 text/x-diff 文本、`/vcs/apply` 提交 `{patch}` 体、`/session/{id}/diff` 可选 `messageID` 供 M4-07 diff 视图）以及 `createFindService().symbols` / `.search`（`/find/symbol`、`/find`，带 schema 派生 FindMatch 类型）——类型全部取自 schema，`dir` 透传、错误按 ApiError 透传；mock server 注册 M4 文件/VCS 全端点路由与 fixtures（file.tree / file.content / file.status、vcs / vcs.status / vcs.diff / vcs.diff.raw、session.diff），`/vcs/diff/raw` 以 text/x-diff 返回、`/vcs/apply` 返回 `{applied:true}`、`/session/{id}/diff` 按 `messageID` 过滤（固定子集，未知 id 返回空数组），并补全 self-test 形状断言与 fixture 模式覆盖；L1（每方法精确载荷拼装）与 L3（真实 mock 契约）测试覆盖全部端点；录制脚本为新增 JSON 端点补充 targets，录制 index 将 `session.diff` 映射到已捕获的 diff.session 载荷（TASK-M4-01）
- 文件树与状态标记：新增 per-server 文件 store（`src/stores/files.ts`），从扁平 GET /file 载荷逐层构建工作区树（直接子级、目录优先、字母序），维护每路径展开状态、path→git 状态映射（added/deleted/modified）与版本计数器；`file.watcher.updated` / `file.edited` SSE 事件（在 `stores/events.ts` 路由，重同步时按服务器清空）递增版本号，挂载中的 FileTree 视图随版本变化重拉树与状态，实现 watcher 事件自动刷新；`src/features/files/FileTree.tsx` 在侧栏渲染文件树：目录懒加载展开（GET /file?path= 拉取子树并嫁接子节点，失败回卷展开状态并展示错误横幅）、文件类型图标（文件夹 + 按扩展名区分的文档字形）、git 状态色点（added 绿 / modified 琥珀 / deleted 红）、`ignored` 条目灰显加斜体、右键菜单（复制路径；「在会话中引用」在 prompt 插入钩子落地（M8）前复制 `@路径`；「打开」走 M4-03 查看器回调），以及加载中/空态/错误 + 重试状态；DesktopShell 侧栏新增 Sessions|Files 分段切换，Files 视图以文件树替换会话列表；L1（树构建/嫁接/展开折叠/状态/版本递增/清空）与 L2（渲染、懒展开与折叠不重拉、状态点、ignored 样式、右键菜单 + 剪贴板、watcher 触发重拉、加载/空态/错误重试、壳层视图切换）测试覆盖全流程（TASK-M4-02）
- 带编辑器式标签页的文件查看器：新增 per-server viewer store（`src/stores/viewer.ts`）维护有序打开文件标签（标题取路径 basename，显式名称优先），提供打开/激活/关闭/清空动作——重复打开同一文件仅激活既有标签、关闭当前标签激活左邻（或补位标签）、未知路径激活被忽略；`src/features/files/FileViewer.tsx` 渲染标签栏与当前文件内容，分四类分支——Shiki 高亮只读文本（语言由扩展名映射、复用既有懒加载高亮单例，失败回退为纯转义文本）、图片（mimeType + encoding 拼装 base64 data URL，或透传 `data:` 内容）、unified diff/patch（text/x-diff mime、`diff` 字段、`patch` hunks、或 `diff `/`--- ` 内容前缀判定）按行着色（新增绿/删除红/hunk 与文件头弱化，无 diff 依赖库）、以及其他二进制载荷的「Binary file」提示——按 (server,path) 缓存内容（重新激活已缓存标签不再拉取；仅成功载荷入缓存，反应式 view-state memo 在取到新缓存后立即刷新渲染），含加载中/错误 + 重试/空文件/无标签页空态，并预留 `fullscreen` 属性供 M7 移动端全屏页；DesktopShell 主区新增 Chat|Files 标签切换——Files 页挂载查看器，侧栏文件树点击文件即打开标签并切到主区 Files 视图，上下文重建时 viewer store 与会话/消息/todos 一并清空；L1（store 动作：打开/激活/关闭顺序 + 邻位激活 + per-server 隔离 + 清空）与 L2（标签栏、文本/图片/diff/二进制/空文件分支、缓存不重拉、错误重试、标签切换内容保持、壳层主区切换与树点击打开）测试覆盖全流程（TASK-M4-03）
- 快速打开文件搜索（⌘/Ctrl+P）：基于 kobalte 的对话框（`src/features/files/QuickOpen.tsx`）走 `GET /find/file`，输入防抖 150ms 并在途响应作废（沿用 PromptBox @ 菜单模式），↑↓ 循环选择 + Enter/Esc 键盘导航与鼠标点选打开；结果排序——大小写不敏感的路径前缀命中在前、其次子串命中、再次服务端其他模糊命中，同一桶内 per-server 最近打开文件优先置顶——由纯函数 `rankResults` 实现；新增 per-server 最近打开文件记忆（localStorage `oc-recent-files:{serverId}`，上限 20 条，打开即写入，空查询时展示并带「Recent」区块，含无最近文件/搜索中/无匹配三种空态），并预留 `variant` 属性供 M7 移动端顶部搜索页；DesktopShell 新增临时 ⌘/Ctrl+P window 按键钩子（在文本输入控件内输入时不拦截，M8 将迁入命令面板注册表）打开对话框，选中文件即打开查看器标签并切到主区 Files 视图（与侧栏树点击行为一致）；L1（recentFiles + rankResults 排序）与 L2（对话框生命周期、防抖/请求/合并/过期丢弃、键盘导航、打开副作用、各空态、壳层快捷键守卫与打开全流程）测试覆盖全链路（E2E E08 随 M10 基建落地）（TASK-M4-04）
- 全文搜索面板（⌘/Ctrl+⇧F）：搜索栏 + 结果列表（`src/features/files/SearchPanel.tsx`）走 `GET /find`，输入防抖 300ms 并在途响应作废（沿用 QuickOpen 模式），Enter 立即执行搜索；正则开关（.*）先在客户端校验模式（非法正则展示内联提示且不发起请求），随后发送仅 mock 支持的 `regex=true` 查询参数——1.18.11 契约只暴露 `pattern`，真实服务端会忽略该参数并按字面匹配（限制已记录于 docs/api-coverage.md）；结果按文件分组渲染（路径头 + 命中计数，每行显示行号与高亮命中段的行文本——优先使用服务端 submatches，缺失时客户端按所搜模式（字面/正则）取首个命中，均由纯函数 `groupByFile` / `highlightSpans` 实现）；点击命中即打开查看器标签并定位该行（viewer store 新增 `activeLine` + `setActiveLine`；FileViewer 为高亮行 span 打上 data-line 编号，待标签内容渲染且查看器可见后滚动该行到视口中央并短暂闪烁，随后清除目标——新增 `visible` 属性使搜索面板覆盖查看器时延迟消费，行号超范围则直接清除不滚动）；Files 视图内查看器与搜索面板并排常驻（两者均保持挂载，结果在往返跳转后不丢失），主区标签栏新增搜索切换按钮；DesktopShell 新增临时 ⌘/Ctrl+⇧F window 按键钩子（文本输入控件内不拦截，重复按下在搜索/查看器间循环，M8 将迁入命令面板注册表）；mock server 的 `/find` 改为按 pattern 过滤 fixture（大小写不敏感子串，或 `regex=true` 时按正则解释；空 pattern / 非法正则返回空数组），新增多文件 fixture 与自测断言（含 submatch 偏移与行文本对齐校验）；L1（viewer activeLine 动作、分组与高亮辅助函数、find 正则参数）与 L2（面板防抖/合并/过期丢弃/Enter、正则校验与参数、分组渲染与高亮、命中打开副作用、空闲/加载中/无匹配/错误状态、查看器行滚动 + 闪烁 + 隐藏延迟消费、壳层 ⌘⇧F + 切换按钮 + 命中全流程）测试覆盖全链路（E2E E09 随 M10 基建落地）（TASK-M4-05）
- 快速打开对话框内的工作区符号搜索（⌘/Ctrl+P）：`#` 前缀查询（触发符加至少一个字符）将 `QuickOpen.tsx` 切换至符号模式，走 `GET /find/symbol`——请求前剥离 `#` 触发符，结果行渲染为符号类型字形（函数 ƒ / 方法 m / 类 ◇ / 接口 i / 变量 v / 常量 c，由纯函数 `symbolKindIcon` 映射，未知类型用中性符号兜底）加符号名与工作区路径，Enter/点击跳转到所在文件并定位目标行——LSP range 起点（0 基）经新增 `setActiveLine` 流程换算为查看器 1 基 data-line 目标，复用文件打开的全部副作用（查看器标签、最近文件记忆、主区视图切换）；删除 `#` 即回退到文件搜索，LSP 无结果时展示专属「No symbols found」空态，两种模式共享 150ms 防抖 / 过期响应作废 / ↑↓ 循环 / 点选等机制；纯函数集中于 `symbols.ts`（`isSymbolQuery` / `symbolQueryOf` / `symbolKindIcon` / `symbolPath`——将 `file://` URI 按活动项目目录折算为工作区相对路径——/ `symbolHitOf`）；mock server 的 `/find/symbol` 改为按符号名大小写不敏感子串过滤 fixture（空查询返回空数组），fixture 扩至 7 个符号覆盖多类型（函数/方法/类/接口/变量/常量且位置各异），自测断言覆盖过滤逻辑、位置形状（0 基 range 起点）与类型分布；L1（模式识别、触发符剥离、类型字形映射、URI→路径、0 基→1 基行号换算）与 L2（符号请求与类型/名称/路径渲染、Enter 与点击跳转并定位行、删除 `#` 回退、无符号空态、符号模式防抖合并与过期丢弃）测试覆盖全链路（TASK-M4-06）
- 会话与消息级 Diff 视图（⌘/Ctrl+D）：主区新增 diff 视图（`src/features/vcs/DiffView.tsx`）走 `GET /session/{id}/diff`（可带 `messageID` 过滤），将 `SnapshotFileDiff[]` 载荷按文件分组渲染——文件头含路径、`+N -M` 增删计数徽标与 added/deleted/modified 状态 chip，随后是统一 diff（绿色/红色增删行着色，旧/新行号随 @@ hunk 头跟踪，hunk/meta 头弱化显示，超过三行的未变更段折叠为「… N unchanged lines」展开手柄）或左右分栏视图（上下文行一对一配对、删除行与随后的新增行配对、头部横跨两栏），由视图内部 Unified/Split 按钮切换；无 patch 内容的文件以统计卡片呈现并附「Content not available for this diff.」说明；含加载 / 错误+重试 / 空态（「No changes in this diff」），挂载与 server/session/message-id 变化时带过期守卫重新拉取，diff store 版本号变化时静默重新拉取；解析/折叠/对齐辅助函数共享（`src/features/vcs/diffLines.ts`——`rowKindOf` 自 FileViewer 抽出、带行号的 `parseUnifiedDiff`、`foldGroups`、`alignSplit`），新增 diff store（`src/stores/diff.ts`——per-server/session 载荷加 `version` 计数器，`applyDiff` / `bumpDiffVersion` / `resetServer`），由事件路由器的 `session.diff` SSE 事件驱动（带载荷则整体替换，裸事件仅提升版本，两者都在上下文重建 / `server.connected` 重同步时重置）；DesktopShell 经临时 ⌘/Ctrl+D 按键钩子打开该视图（文本输入控件内不拦截；视图内再按：带过滤时清除过滤、无过滤时返回聊天，M8 将迁入命令面板注册表），diff 头部含 Back 按钮、「Session diff」标题，以及从消息进入时的 Message-{id} 过滤 chip（× 清除过滤）；消息菜单的「View diff」项全链路接线（`MessageList` → `MessageBubble` → `MessageActions.onViewDiff`）；mock server 自 M4-01 起已提供带 patch 内容与 messageID 过滤的 fixture；L1（diffLines 解析/折叠/对齐、diff store、事件路由含重置）与 L2（DiffView 按文件分组/徽标/统一行/折叠/分栏/空态/错误重试/message-id 重拉/版本号重拉、壳层 ⌘D 打开+循环+输入控件守卫、消息 view-diff 过滤 + chip 清除 + 返回）测试覆盖全链路（E2E E10 随 M10 基建落地）（TASK-M4-07）
- VCS 面板与补丁应用（TASK-M4-08）：主区新增 Changes 视图（`src/features/vcs/VcsPanel.tsx`），由 Files 标签栏的 git 按钮打开（自带 Back 头部返回 Files），展示当前分支 chip、工作区变更列表——每个文件一行，含 added/deleted/modified 状态字母 chip 与 `+N -M` 增删徽标，数据走 `GET /vcs` + `GET /vcs/status`——并含工作树清洁空态与 Refresh 按钮；Workspace diff 子视图渲染 `GET /vcs/diff`，复用共享的按文件渲染器（DiffView 的按文件分组抽取为 `DiffFileGroup.tsx`，`DiffMode` 类型移入 `diffLines.ts`，会话 diff 视图与工作区 diff 视图渲染完全一致），带 Unified/Split 切换与折叠；Apply 区块支持粘贴 unified patch 到文本框、弹确认对话框（应用会修改工作树）、POST `/vcs/apply` 并反馈结果——成功清空文本框并提升 VCS store 版本使状态与 diff 重新拉取，失败内联展示错误详情——非 git 工作区（无分支上报）渲染优雅的「Not a git repository」空态并隐藏 diff/apply 操作；新增 vcs store（`src/stores/vcs.ts`——per-server 分支 + 变更列表 + 版本计数器，`applyVcs` / `applyStatus` / `applyBranch` / `refresh` / `resetServer`），由事件路由新增的 `vcs.branch.updated` 分支（写入分支并提升版本，挂载中的面板据此重拉状态）与面板快照拉取共同驱动；DesktopShell 在三栏布局之下新增极简底部状态栏与分支 chip（`StatusBarBranch`——每个服务器挂载时拉取一次 `GET /vcs`，随 store 响应式更新，分支事件即时刷新；M9-07 将扩展 LSP/MCP/tokens 等项）；L1（vcs store 动作、事件路由含重置）与 L2（VcsPanel 分支头/变更行/徽标/清洁态/刷新/分支事件重拉、工作区 diff 子视图 + 返回、apply 确认 → POST → 成功刷新 + 清空 / 失败详情 / 取消 / 空补丁禁用、非 git 空态、壳层 Changes 视图打开 + 返回、状态栏 chip 拉取 + `vcs.branch.updated` 实时更新）测试覆盖全链路；mock server 自 M4-01 起已提供 `/vcs` 族 fixture 与 apply 自测断言（L3 契约）
- 全局权限请求队列（TASK-M5-01）：per-server 权限 store（`src/stores/permission.ts`——队列 + 版本号，按 id 去重的 `enqueue` / `dequeue` / `applyList` / `resetServer`），由事件路由新增的 `permission.asked`（入队，payload 即 PermissionRequest）与 `permission.replied`（按 requestID 出队）分支驱动，并在全量重同步与目录上下文重建时按服务器重置；权限领域服务（`src/services/permission.ts`——`list` 走 `GET /permission`，`reply` 走 `POST /permission/{requestID}/reply`）；全局权限卡片（`src/features/permissions/PermissionSheet.tsx`）挂载于 DesktopShell（顶层弹层，仅在活跃服务器有待处理请求时渲染），展示队列头——权限类型 chip、等宽 pattern chips、弱化的工具上下文（messageID · callID）、可展开的 metadata 详情与「1 of N waiting」指示器——并提供三个操作（允许一次 / 总是允许 / 拒绝）提交应答，仅在成功后出队（失败保留请求并内联报错；一次应答 POST 在途时按钮锁定；弹层不可被 Esc/遮罩关闭）；「总是允许」会记录会话内 remember 记忆（`src/features/permissions/remembered.ts`——权限+patterns 的无序稳定签名，按服务器隔离），之后同 pattern 的请求会被静默自动应答「always」（在途时隐藏卡片，静默应答失败则带错误浮出卡片，绝不静默丢弃）；mock server 注册 `/permission` fixture 列表与带校验的应答端点（非法 reply → 400），fixture 模式下将录制的 `permission.asked` fixture 映射到列表路由，并新增自测断言；L1（服务载荷组装、store 动作、事件路由含重置、remember 记忆）与 L2（卡片渲染 / 队列推进 / 详情展开 / 三操作 + 出队 / 在途锁定 / 失败重排队 / 自动应答静默 + 失败浮出 / Esc 守卫 / variant 空操作）测试覆盖全链路；原生系统通知与待处理角标延后至 M8-06 / 角标命令（TODO 见 architecture.md §7.2）
- 斜杠命令系统（TASK-M5-03）：输入框首字符 `/` 弹出命令过滤菜单（基于 `GET /command`，显示名称 + 描述 + 参数提示（等宽字体），↑↓/Enter/Esc 键盘导航，失焦/Esc 关闭；命令列表在首次触发时按挂载拉取一次并带在途防重，且与 `@` 菜单严格互斥——引用触发条件成立时优先）；选中项将输入填充为命令模板——`/{name}` 加首个参数提示作为可编辑占位文本（`commandTemplate`）；提交与已知命令（不区分大小写，`matchCommand`）匹配的消息时改走 `POST /session/{id}/command`，请求体为 `{ command, arguments }`（arguments 为名称后的文本），并清空输入——回复经常规 SSE 消息流渲染；不匹配的 `/…` 消息回退为普通提示词路径；命令执行失败时展示内联错误横幅并恢复提交文本以便重试；mock server 提供 4 条命令的 fixture（含带参数提示的自定义服务端命令）与带校验的命令执行端点（缺 command/arguments → 400），并新增自测断言；L1（服务载荷组装 + 模板/匹配纯函数）与 L2（菜单开启/过滤/导航/插入/Esc/@ 互斥/执行/未匹配回退/失败恢复/全链路）测试覆盖，另有 L3 契约块（list + run）
- Agent 问题卡片（TASK-M5-02）：per-server 问题 store（`src/stores/question.ts`——队列 + 版本号，按 id 去重的 `enqueue` / `dequeue` / `applyList` / `resetServer`），由事件路由新增的 `question.asked`（入队，payload 即 QuestionRequest）与 `question.replied` / `question.rejected`（按 requestID 出队）分支驱动，并在全量重同步与目录上下文重建时按服务器重置；问题领域服务（`src/services/question.ts`——`list` 走 `GET /question`，`reply` 走 `POST /question/{requestID}/reply`，按 schema 携带 `{answers}` 请求体——每个问题一个所选 label 数组，`reject` 走 `POST /question/{requestID}/reject`，无请求体）；全局问题卡片（`src/features/questions/QuestionSheet.tsx`）挂载于 DesktopShell 权限卡片旁，展示队列头——header chip、完整问题文本与「1 of N waiting」指示器——并提供两种回答形态：选项按钮（点击即以该 label 应答）或无选项问题的自由输入 textarea + Send，另有 Reject 操作；应答仅在成功后出队（失败保留问题并内联报错；一次 POST 在途时控件锁定；自由输入草稿在失败后保留，重试无需重打），已回答的问题由其 replied/rejected 事件清除（应答与已落定请求竞争时内联报错，事件随后清空队列），无问题内容的畸形请求防御性渲染（提示 + 仅 Reject），弹层不可被 Esc/遮罩关闭，`variant` prop 预留给 M7 移动端底部弹层；mock server 注册 `/question` fixture 列表（一个带选项问题 + 一个自由输入问题）、带校验的应答端点（非数组 answers → 400）与拒绝端点，fixture 模式下将录制的 `question.asked` fixture 映射到列表路由，并新增自测断言；L1（服务载荷组装、store 动作、事件路由含重置）与 L2（选项表单应答 / 自由输入发送含空草稿禁用 + 草稿保留 / 拒绝 / 在途锁定 / 失败重排队 / 队列推进 / 无问题防御渲染 / Esc 守卫 / variant 空操作）测试覆盖全链路；原生系统通知与待处理角标延后至 M8-06（与权限卡片共用，TODO 见 architecture.md §7.2）
- Agent 模式选择器（TASK-M5-04）：输入框上方的工具栏行新增 agent chip（`src/features/models/agents.ts`——纯函数 `visibleAgents` / `agentColor`（缺失时灰色兜底）/ `cycleAgentName`），展示当前生效 agent 名称与色点；目录来自新增 agent 领域服务（`src/services/agent.ts`——`list` 走 `GET /agent`），每次挂载拉取一次并经 per-server agents store（`src/stores/agents.ts`——目录 + loaded 标志 + `activeBySession` 选择记录，`setAgents` / `setAgentForSession` / `agentNameFor`，解析链为：已记录选择 → 会话自身 agent（仍可见时）→ 首个可见 agent）跨挂载复用；点击 chip 弹出可见（非 hidden）agent 菜单——名称、mode 与描述，当前项带对勾与强调底色——选择按会话记录（服务端无 agent PATCH，选择在客户端按会话持久）；输入框内 Tab 在无输入菜单占用按键时循环切换可见 agent（菜单打开时保持默认焦点行为），发送链路携带生效 agent——`sendPrompt` 新增可选 agent 参数并写入 `prompt_async` 请求体（已对照 1.18.11 契约核实 `agent: string` 字段），同时映射到乐观用户消息，build/plan 切换可直达服务端；mock server 注册 `/agent` fixture 列表（build/plan 带十六进制色值 + hidden 的 architect，permission/options 符合 schema），自测断言覆盖目录形状与 hidden 契约，未实现端点 501 探测改走不存在的 `/model` 路径；L1（服务载荷组装、store 解析链 + 重置、纯函数测试）与 L2（chip + 色点、隐藏过滤 + 描述/mode + 对勾的菜单、跨挂载按会话持久、Tab 循环、prompt 请求体携带 agent、Esc 关闭）测试覆盖全链路
- Provider OAuth 授权流（TASK-M5-07）：provider 领域服务新增 `oauthAuthorize`（`POST /provider/{id}/oauth/authorize`，请求体为契约的 `{ method: 认证方式索引, inputs? }`——已对照 1.18.11 契约核实 method 为数字索引，响应 `ProviderAuthAuthorization` `{ url, method: auto|code, instructions }` 由服务端决定流程类型）、`oauthCallback`（`POST /provider/{id}/oauth/callback`，请求体 `{ method, code? }`）与 `oauthPoll`（同一回调端点携带 mock 扩展 `poll: true`——契约无状态查询端点，auto 模式以 2s 间隔轮询回调直至成功（最长 60s），偏差已记录于 docs/api-coverage.md §5）；OAuth 授权对话框（`src/features/settings/providers/ProviderOAuth.tsx`，kobalte Dialog，由替代 M5-06 延后提示的 Authorize 按钮打开）先 POST authorize，再经 opener 插件（`@tauri-apps/plugin-opener` 的 `openUrl`，`opener:default` 能力已授权）在系统浏览器打开返回 URL，随后按流程分支——auto 模式轮询回调至完成（纯函数 `pollUntil(fn, {intervalMs, timeoutMs, signal})` 遇瞬时失败继续轮询、超时/中止返回 false；成功即关闭对话框并重拉 provider 列表刷新连接状态、超时内联报错、取消/Esc/卸载中止轮询），code 模式展示服务端 instructions 并输入授权码提交（空草稿与在途提交均锁定，拒绝的 code 或提交失败保留草稿可重试）；mock server 注册 OAuth 路由（authorize 创建按 state 区分的待定会话并返回其浏览器 URL，`GET /oauth/authorize?state=` 页面模拟浏览器往返——访问即置完成，callback 对 code 流程校验固定码 `mock-oauth-code`、对 auto 轮询返回 pending/completed，非法 method 索引或 code → 400），并新增带 code 流程 oauth 方法的 google provider（azure 保持 auto 流程 fixture），self-test 覆盖双流程端到端；L1（服务载荷组装含目录透传、pollUntil 时序/超时/取消/拒绝）与 L2（对话框 authorize → 打开浏览器 → 轮询序列 → 成功关闭 + 刷新 / 超时报错 / 取消停止轮询 / code 输入 → 提交 → 关闭 / 拒绝与失败保留 / 空草稿锁定 / authorize 失败 / ProviderKeys Authorize → 对话框 → 取消）测试加 L3 契约块覆盖全链路
- Provider API Key 管理（TASK-M5-06）：provider 领域服务新增 `authMethods`（`GET /provider/auth`——按 provider id 的 ProviderAuthMethod 记录）、`setKey`（`PUT /auth/{providerID}`，请求体为 schema 的 `{ type: "api", key }` ApiAuth 形态）与 `removeKey`（`DELETE /auth/{providerID}`），三者均支持目录参数；新增极简设置页（`src/features/settings/SettingsPage.tsx`，作为 M9-04 设置中心的基础——含 Back 头部、占位分区导航与 Providers 分区），由 DesktopShell 主区 Chat|Files 标签栏新增的齿轮按钮打开（自带 Back 头部返回聊天视图，与 diff/changes 视图同款）；provider 密钥面板（`src/features/settings/providers/ProviderKeys.tsx`）经模型 store 列出服务器 provider 并带连接状态徽标，按认证方式渲染表单——`api` 方式为 API Key 表单（password 输入框、空值或请求在途时 Save 禁用，已连接 provider 另附 Remove 按钮及两步内联确认；密钥值永不回读——契约无密钥 GET——已存密钥以连接徽标 + 「Key set」提示呈现），`oauth` 方式渲染延后提示（TASK-M5-07）；保存/删除密钥后重新拉取 `GET /provider` 并写入模型 store，连接状态随之刷新（失败时草稿保留可重试，加载/变更错误内联展示并带 Retry）；mock server 注册 `/provider/auth` fixture（openai/anthropic 为带 prompts 的 api 表单、azure 为 oauth）、带校验的 `PUT /auth/{providerID}`（缺 type/key → 400）与返回 true 的 DELETE 端点，self-test 覆盖认证方式形状与 set/remove 契约；L1（服务载荷组装含目录透传）与 L2（面板列表 / api 表单保存 → PUT + 刷新 / 连接状态刷新契约 / 空密钥守卫 / 删除确认 + 取消 / 失败保留 / 加载错误 + 重试 / 页面骨架 + Back / 壳层齿轮按钮导航）测试覆盖全链路
- 模型选择器（TASK-M5-05）：新增 provider 领域服务（`src/services/provider.ts`——`list` 走 `GET /provider`，`configProviders` 走 `GET /config/providers`，均支持目录参数；对照 OpenCode 源码核实 1.18.11 契约：`/provider` 返回 `{ all, default, connected }`，`default` 为 providerID → 默认模型 id 的记录，`connected` 为已认证 provider id 数组，`/config/providers` 返回 `{ providers, default }` 且 default 记录语义一致，`Provider.models` 为以模型 id 为键的记录）；per-server 模型 store（`src/stores/models.ts`——providers + connected id 数组 + `defaultModels`（config 记录，picker 的 Default 标记以它为准）+ `defaultModel` + loaded 标志 + `activeBySession` 选择记录，`setProviders` / `setConfigDefault` / `setModelForSession` / `activeModelFor`，解析链为：已记录选择 → 会话自身模型（仍在目录中时）→ config 默认模型 → 首个已连接 provider 的首个模型）；ModelPicker 对话框（`src/features/models/ModelPicker.tsx`，kobalte Dialog）由 composer 工具栏 agent chip 旁新增的 model chip 打开——chip 展示生效模型名称与所属 provider（与 agents 同款：挂载时经 store 拉取一次）——内含搜索框（按 provider 或模型名过滤，provider 名命中时展示其全部模型）、按 provider 分组的模型列表（未连接 provider 带「Not connected」标签、整组灰显且选择按钮禁用）、来自 capabilities 字段的能力徽标（tools 取自 `toolcall`、vision 取自 `input.image`、reasoning）、每百万 token 成本提示（`cost` 字段，如「$1.25/M in · $10/M out」）与上下文上限提示（`limit.context`，如「400K context」，纯函数集中在 `src/features/models/models.ts`）、跟随 `/config/providers` default 记录的 Default 徽标（picker 数据与 config 默认一致——验收点）、当前模型上的 Current 对勾，以及星标收藏区（localStorage `oc-fav-models`，键为「providerID:modelID」）；选择按会话记录在 store 中（服务端无按会话的模型 PATCH，选择在客户端按会话持久），发送链路携带生效模型——`sendPrompt` 新增可选 model 参数并写入 `prompt_async` 请求体（已对照 1.18.11 契约核实 `model: { providerID, modelID }` 字段），同时映射到乐观用户消息；mock server 注册 `/provider` 与 `/config/providers` fixtures（openai/anthropic 已连接且能力/成本/上下文各具差异，azure 未连接且带模型——覆盖灰显契约），自测断言覆盖目录形状、default 记录 ↔ 模型引用、未连接契约与 config 默认和 `/provider` 的一致性（含 fixture 模式回退）；L1（服务载荷组装、store 解析链 + 重置、纯函数单测）与 L2（picker 分组 / 徽标 / 成本 + 上下文提示 / 未连接灰显禁用 / Default 标记 / 收藏切换 + 分区 + localStorage 恢复 / 搜索 / 选择 → store + chip / Current 标记 / Esc + Close，chip 展示 + 跨挂载按会话持久 + 请求体携带模型）测试覆盖全链路
