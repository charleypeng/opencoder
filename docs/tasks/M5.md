# M5 — 权限 · 问题 · 命令 · 模型

> 出口标准：权限请求/应答/记忆规则闭环；问题卡片；斜杠命令；agent/model/provider 选择与认证流。

---

### TASK-M5-01 权限请求全局队列
- **前置**：M2-02 ｜ **模块**：`src/features/permissions/`、`src/stores/permission.ts`
- **范围**：`PermissionSheet.tsx`、store、全局挂载
- **API**：`GET /permission`、`POST /permission/{requestID}/reply`；事件 `permission.asked/replied`
- **规格**：队列化（多请求排队）；卡片显示 permission 类型/patterns/工具上下文/详情展开；三选：允许一次 / 总是允许（remember）/ 拒绝；桌面弹层 + 移动 Bottom Sheet；系统通知（notification 插件）+ 角标
- **验收**：E2E E05；remember 后同 pattern 不再询问
- **测试**：L1（队列）+ L2 + E2E E05
- **提交**：`feat(permissions): 全局权限请求队列 (TASK-M5-01)`

### TASK-M5-02 问题卡片
- **前置**：M5-01 ｜ **模块**：`src/features/questions/`
- **范围**：`QuestionSheet.tsx`、store
- **API**：`GET /question`、`POST /question/{id}/reply`、`POST /question/{id}/reject`；事件 `question.*`
- **规格**：问题文本 + 选项/自由输入两种回答形态；拒绝按钮；与权限共用通知通道；超时/已回答状态的防御渲染
- **验收**：E2E E06
- **测试**：L2 + E2E E06
- **提交**：`feat(questions): Agent 问题卡片 (TASK-M5-02)`

### TASK-M5-03 斜杠命令
- **前置**：M2-08 ｜ **模块**：`src/features/commands/`
- **范围**：PromptBox `/` 触发菜单、命令执行
- **API**：`GET /command`、`POST /session/{id}/command`
- **规格**：`/` 弹出过滤菜单（名称/描述/参数提示）；选中补全模板；执行后消息流正常渲染；自定义命令（服务端下发）同样支持
- **验收**：fixture 命令列表与执行链路
- **测试**：L2 + L3
- **提交**：`feat(commands): 斜杠命令系统 (TASK-M5-03)`

### TASK-M5-04 Agent 选择器
- **前置**：M2-08 ｜ **模块**：`src/features/models/`
- **范围**：PromptBox agent 切换 chip
- **API**：`GET /agent`
- **规格**：agent 列表（名称/描述/mode/color）；Tab 键循环切换；当前 agent 色点指示；`hidden` 过滤；选择随会话持久
- **验收**：build/plan 等切换后 prompt_async 携带正确 agent
- **测试**：L2 + L3
- **提交**：`feat(models): Agent 模式选择器 (TASK-M5-04)`

### TASK-M5-05 模型选择器
- **前置**：M5-04 ｜ **模块**：`src/features/models/`
- **范围**：ModelPicker（分组列表/搜索/收藏）
- **API**：`GET /provider`、`GET /config/providers`
- **规格**：按 provider 分组；模型能力徽标（tools/vision/reasoning，来自 capabilities）；成本与上下文上限提示；未连接 provider 灰显；默认模型标识；选择随会话持久
- **验收**：picker 数据与 `/config/providers` default 一致
- **测试**：L2 + L3
- **提交**：`feat(models): 模型选择器 (TASK-M5-05)`

### TASK-M5-06 Provider API Key 认证
- **前置**：M5-05 ｜ **模块**：`src/features/settings/providers/`
- **范围**：Provider 设置页、API Key 表单
- **API**：`GET /provider/auth`、`PUT /auth/{providerID}`、`DELETE /auth/{providerID}`
- **规格**：按 `ProviderAuthMethod` 动态生成表单（api 类型）；密钥掩码显示、可删除重设；保存后刷新 provider connected 状态
- **验收**：mock 认证流闭环
- **测试**：L2 + L3
- **提交**：`feat(settings): Provider API Key 管理 (TASK-M5-06)`

### TASK-M5-07 Provider OAuth 流
- **前置**：M5-06 ｜ **模块**：`src/features/settings/providers/`
- **范围**：OAuth 授权页
- **API**：`POST /provider/{id}/oauth/authorize`、`POST /provider/{id}/oauth/callback`
- **规格**：authorize → opener 插件打开系统浏览器；`method: auto` 轮询等待 / `code` 方式输入授权码；callback 提交后刷新状态；失败/取消路径完整
- **验收**：mock 双方法（auto/code）通过
- **测试**：L2 + L3
- **提交**：`feat(settings): Provider OAuth 授权流 (TASK-M5-07)`

### TASK-M5-08 Skill 引用与会话内 shell
- **前置**：M5-03 ｜ **模块**：`src/features/commands/`、`src/features/sessions/`
- **范围**：`@` 菜单 skill 段、shell 命令入口（`!` 前缀）
- **API**：`GET /skill`、`POST /session/{id}/shell`
- **规格**：`@` 菜单加入 skills 分组（AgentPartInput）；`!` 前缀走 `/shell` 执行（需选择 agent/model）；结果以 Shell 消息渲染
- **验收**：fixture 全链路
- **测试**：L2 + L3
- **提交**：`feat(commands): Skill 引用与会话 shell 命令 (TASK-M5-08)`
