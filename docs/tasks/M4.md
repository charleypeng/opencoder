# M4 — 文件与 VCS

> 出口标准：文件树/查看器/三种搜索可用；文件状态标记；会话 diff 与 VCS status/diff/apply 完整。

---

### TASK-M4-01 文件服务层
- **前置**：M2-01 ｜ **模块**：`src/services/`
- **范围**：`file.ts`、`find.ts`、`vcs.ts`
- **API**：`/file`、`/file/content`、`/file/status`、`/find`、`/find/file`、`/find/symbol`、`/vcs`、`/vcs/status`、`/vcs/diff`、`/vcs/diff/raw`、`/vcs/apply`、`/session/{id}/diff`
- **规格**：统一封装 + 契约测试；大文件 content 流式/分段策略（>1MB 提示）
- **验收**：全端点契约绿
- **测试**：L3
- **提交**：`feat(api): 文件/搜索/VCS 服务层 (TASK-M4-01)`

### TASK-M4-02 文件树
- **前置**：M4-01 ｜ **模块**：`src/features/files/`
- **范围**：`FileTree.tsx`、Files 面板（桌面侧栏 Tab / 移动 Tab 页）
- **API**：`/file`、`/file/status`；事件 `file.watcher.updated`
- **规格**：懒加载目录展开；文件类型图标；git 状态色点（M/A/D）；`ignored` 灰显；watcher 事件增量刷新；右键菜单（复制路径/引用到输入框/打开）
- **验收**：watcher 事件后树自动更新
- **测试**：L1 + L2
- **提交**：`feat(files): 文件树与状态标记 (TASK-M4-02)`

### TASK-M4-03 文件查看器
- **前置**：M4-02 ｜ **模块**：`src/features/files/`
- **范围**：`FileViewer.tsx`、编辑器 Tab 管理
- **API**：`/file/content`
- **规格**：Shiki 高亮只读视图；图片/二进制分支（mimeType）；`diff`/`patch` 类型内容的着色展示；多 Tab 打开；移动端全屏页
- **验收**：文本/图片/diff 三类内容正确
- **测试**：L2
- **提交**：`feat(files): 文件查看器 (TASK-M4-03)`

### TASK-M4-04 快速打开（⌘P）
- **前置**：M4-01 ｜ **模块**：`src/features/files/`、命令面板
- **范围**：QuickOpen 组件
- **API**：`/find/file`
- **规格**：模糊搜索（防抖 150ms、取消在途）；键盘上下选择 Enter 打开；最近打开加权；移动端顶部搜索页形态
- **验收**：E2E E08
- **测试**：L2 + E2E E08
- **提交**：`feat(files): ⌘P 快速打开文件 (TASK-M4-04)`

### TASK-M4-05 全文搜索
- **前置**：M4-01 ｜ **模块**：`src/features/files/SearchPanel.tsx`
- **范围**：搜索面板 + 结果列表
- **API**：`/find?pattern=`
- **规格**：关键字/正则开关；结果按文件分组（path + line_number + 上下文行）；点击跳查看器并高亮命中行；桌面 ⌘⇧F
- **验收**：E2E E09
- **测试**：L2 + E2E E09
- **提交**：`feat(files): 全文搜索面板 (TASK-M4-05)`

### TASK-M4-06 符号搜索
- **前置**：M4-01 ｜ **模块**：`src/features/files/`
- **范围**：QuickOpen 内 `#` 前缀模式或独立入口
- **API**：`/find/symbol`
- **规格**：符号类型图标（函数/类/变量）；跳转到文件+位置；无 LSP 结果时空态提示
- **验收**：fixture 符号结果渲染与跳转
- **测试**：L2
- **提交**：`feat(files): 工作区符号搜索 (TASK-M4-06)`

### TASK-M4-07 会话 Diff 视图
- **前置**：M4-03 ｜ **模块**：`src/features/vcs/`、`src/features/messages/`
- **范围**：`DiffView.tsx`（统一 diff 渲染组件）、会话 diff 页
- **API**：`/session/{id}/diff`（可带 `messageID`）；事件 `session.diff`
- **规格**：按文件分组的 diff（增删行着色、行号、折叠未变更段）；统一/分栏双模式；消息操作「查看 diff」跳此视图并过滤 messageID；桌面 ⌘D
- **验收**：E2E E10
- **测试**：L2 + E2E E10
- **提交**：`feat(vcs): 会话/消息级 Diff 视图 (TASK-M4-07)`

### TASK-M4-08 VCS 状态与应用补丁
- **前置**：M4-07 ｜ **模块**：`src/features/vcs/`
- **范围**：VCS 面板（分支/变更列表/diff/apply）
- **API**：`/vcs`、`/vcs/status`、`/vcs/diff`、`/vcs/diff/raw`、`/vcs/apply`；事件 `vcs.branch.updated`
- **规格**：状态栏分支名（事件联动）；变更文件列表（status+增删）；工作区 diff 查看；`apply`（粘贴 patch 应用，带确认与结果反馈）；非 git 项目优雅空态
- **验收**：git fixture 全流程；apply 成功后状态刷新
- **测试**：L2 + L3
- **提交**：`feat(vcs): 版本控制面板与补丁应用 (TASK-M4-08)`
