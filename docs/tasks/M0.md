# M0 — 工程基建

> 出口标准：仓库可构建（桌面 dev 可跑）；CI 绿；`pnpm gen:api` 从 OpenAPI 产出类型；Mock Server 可启动并响应健康检查。

---

### TASK-M0-01 仓库初始化与 Tauri 脚手架
- **前置**：- ｜ **模块**：根目录、`src-tauri/`
- **范围**：`package.json`、`src-tauri/`、`tauri.conf.json`、`vite.config.ts`、`index.html`
- **API**：-
- **规格**：`create-tauri-app` 初始化（SolidJS + TS 模板）；配置 app 标识（bundle id 占位 `com.opencode.client`，待 D1 定稿）；开启 `devtools`（仅 debug）；git 初始化并提交 `.gitignore`（node_modules/dist/target/fixtures 例外规则）
- **验收**：`pnpm tauri dev` 在 macOS 启动空白窗口；`pnpm tauri build --debug` 成功
- **测试**：CI 能跑 `cargo check`
- **提交**：`chore: 初始化 Tauri 2 + SolidJS 工程脚手架 (TASK-M0-01)`

### TASK-M0-02 前端工具链与设计令牌
- **前置**：M0-01 ｜ **模块**：`src/styles/`、`src/components/`
- **范围**：`src/styles/`、`tailwind.config`（v4 `@theme`）、`src/components/`（占位）
- **API**：-
- **规格**：接入 Tailwind v4；按 `docs/ui-design.md` §2 落地 tokens.css（色彩/字阶/圆角/动效/密度/玻璃变量）；深浅色 `[data-theme]` 切换基座；Kobalte 引入
- **验收**：示例页展示全部 token 与深浅色切换
- **测试**：L2 token 快照
- **提交**：`feat(ui): 设计令牌与 Tailwind 基座 (TASK-M0-02)`

### TASK-M0-03 OpenAPI 代码生成管线
- **前置**：M0-01 ｜ **模块**：`src/services/api/`
- **范围**：`scripts/gen-api.mjs`、`src/services/api/schema.d.ts`、package.json scripts
- **API**：全部（类型源）
- **规格**：`openapi-typescript` 从 `docs/openapi_v1.18.11.json` 生成类型；`pnpm gen:api` 与 `pnpm gen:api --check`（漂移检测）；README 注释说明契约升级流程
- **验收**：`schema.d.ts` 生成且被 `tsc` 通过；改动 json 后 `--check` 能报错
- **测试**：L0 加入 gen check
- **提交**：`chore(api): OpenAPI 类型生成管线 (TASK-M0-03)`

### TASK-M0-04 Mock Server — REST 骨架
- **前置**：M0-03 ｜ **模块**：`tests/mock-server/`
- **范围**：`tests/mock-server/**`
- **API**：`/global/health`、`/project`、`/session` 族（先行）；框架支持声明式注册其余端点
- **规格**：Node+tsx；路由表按 `docs/api-coverage.md` 分组；未实现端点 501 并记录日志；`?__fail`/`?__slow` 故障注入；可选 Basic Auth
- **验收**：`pnpm mock:start` 后 curl 健康检查/项目/会话返回 fixture 结构
- **测试**：mock 自测脚本
- **提交**：`test(mock): Mock Server REST 骨架 (TASK-M0-04)`

### TASK-M0-05 Mock Server — SSE 与场景脚本
- **前置**：M0-04 ｜ **模块**：`tests/mock-server/`
- **范围**：`tests/mock-server/sse.ts`、`tests/mock-server/scenarios/`
- **API**：`/event`、`/global/event`
- **规格**：SSE 流（首事件 `server.connected`）；场景 DSL：`scenario("happy-chat", [ {at:0, event:...}, ... ])`；内置 happy-chat / permission-flow / question-flow / sse-drop 四个场景
- **验收**：curl -N 可观察到场景事件按时间线推送
- **测试**：场景回放自测
- **提交**：`test(mock): SSE 流与场景脚本 (TASK-M0-05)`

### TASK-M0-06 真实响应 fixture 录制
- **前置**：M0-04 ｜ **模块**：`tests/fixtures/`
- **范围**：`tests/fixtures/**`、`scripts/record-fixtures.mjs`
- **API**：消息全 Part 类型、权限、问题、文件树、diff、todo
- **规格**：录制脚本对真实 `opencode serve`（用户本地安装）抓样本；脱敏（路径/用户名替换）；fixture 索引文件 `index.json`
- **验收**：fixture 覆盖 `docs/testing.md` §2.1 录制轨清单；mock server 可切换 fixture 模式
- **测试**：fixture schema 校验测试（对照生成类型）
- **提交**：`test(fixtures): 真实响应样本录制 (TASK-M0-06)`

### TASK-M0-07 CI 流水线与质量门禁
- **前置**：M0-01 ｜ **模块**：`.github/workflows/`
- **范围**：`ci.yml`、`e2e.yml`（骨架）、`desktop.yml`（骨架）
- **API**：-
- **规格**：按 `docs/testing.md` §4 实现 ci.yml（L0+L1+L2+L3，缓存 node/cargo）；`pnpm verify` 聚合脚本；husky + lint-staged（commit 前 L0）
- **验收**：空项目 PR 全绿；本地 `pnpm verify` 一键通过
- **测试**：CI 自身
- **提交**：`chore(ci): CI 流水线与 verify 门禁 (TASK-M0-07)`

### TASK-M0-08 工程规范与文档骨架
- **前置**：M0-01 ｜ **模块**：根目录文档
- **范围**：`README.md`、`README-zh.md`、`CHANGELOG.md`、`CHANGELOG-zh.md`、`CONTRIBUTING.md`
- **API**：-
- **规格**：README 双语骨架（项目简介/架构图/开发命令/文档地图，内容随里程碑充实）；CHANGELOG 双语 Keep a Changelog 头 + Unreleased 段；CONTRIBUTING 指向 AGENT_PLAYBOOK
- **验收**：四文件齐全且相互链接正确
- **测试**：markdown link check（CI）
- **提交**：`docs: README/CHANGELOG 双语骨架 (TASK-M0-08)`
