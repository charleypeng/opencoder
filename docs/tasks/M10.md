# M10 — 测试加固与发布

> 出口标准：E2E 全绿 + 覆盖率达标；五平台可安装包；README/README-zh 完整；发布流水线可用。

---

### TASK-M10-01 E2E 补全与覆盖率门禁
- **前置**：M9-08 ｜ **模块**：`tests/`
- **范围**：E2E 全 12 旅程复核、覆盖率配置
- **API**：-
- **规格**：E01–E12 全部稳定通过（去 flake：重试策略/等待条件收敛）；vitest 覆盖率门槛生效（services/stores ≥80%）；tauri-driver 壳级用例并入 CI
- **验收**：CI 连续 3 次全绿
- **测试**：自身
- **提交**：`test(e2e): 全旅程稳定化与覆盖率门禁 (TASK-M10-01)`

### TASK-M10-02 桌面打包与签名
- **前置**：M10-01 ｜ **模块**：`desktop.yml`、`src-tauri/`
- **范围**：三平台构建配置、签名/notarize 文档
- **API**：-
- **规格**：macOS universal（签名+notarize，密钥文档化）；Windows x64（nsis/msi + 签名可选）；Linux（deb + AppImage）；图标全尺寸（icns/ico/png）；安装后首启走查
- **验收**：三平台安装包手动安装验证
- **测试**：L4 壳级 + 手动
- **提交**：`chore(release): 桌面三平台打包签名 (TASK-M10-02)`

### TASK-M10-03 移动端发布包
- **前置**：M10-01 ｜ **模块**：`mobile.yml`、`src-tauri/gen/`
- **范围**：iOS release 构建、Android release（aab/apk）
- **API**：-
- **规格**：iOS 签名/描述文件文档化（App Store Connect 上架清单：截图/隐私清单/未成年人分级说明）；Android 签名 keystore 文档化；权限声明最小化（相机仅扫码用）
- **验收**：release 包真机安装主流程过
- **测试**：L5
- **提交**：`chore(release): 移动端发布包构建 (TASK-M10-03)`

### TASK-M10-04 发布流水线
- **前置**：M10-02、M10-03 ｜ **模块**：`release.yml`
- **范围**：tag 触发发布、Release notes 生成
- **API**：-
- **规格**：`v*` tag → 全平台构建 → 产物上传 GitHub Release；notes 从 CHANGELOG.md 提取对应版本段；版本号三处同步校验（package.json/tauri.conf/Cargo.toml）
- **验收**：dry-run tag 全流程绿
- **测试**：CI
- **提交**：`chore(release): 发布流水线 (TASK-M10-04)`

### TASK-M10-05 README 双语定稿
- **前置**：M9-04 ｜ **模块**：根目录文档
- **范围**：`README.md`、`README-zh.md`、截图/录屏资产
- **API**：-
- **规格**：双语言完整版（特性清单含 Liquid Glass/萌宠/多服务器；五平台安装指引；服务端要求 `opencode serve` 版本与启动命令含认证说明；快捷键表；架构图；开发指南；贡献指南链接）；截图双端
- **验收**：新用户按 README 可独立完成 安装→连服务器→发消息
- **测试**：markdown link check
- **提交**：`docs: README 双语定稿 (TASK-M10-05)`

### TASK-M10-06 v1.0.0 发布与 Beta  checklist
- **前置**：M10-05 ｜ **模块**：全局
- **范围**：CHANGELOG 定版、回归 checklist、tag
- **API**：-
- **规格**：`[Unreleased]` 归集为 `## [1.0.0]`（双文件）；全平台手动回归 checklist 执行归档；已知问题写入 Release notes；打 `v1.0.0` tag 触发 M10-04
- **验收**：Release 页面完整、产物可装
- **测试**：回归 checklist
- **提交**：`chore(release): v1.0.0 (TASK-M10-06)`

---

## 附录：TASK-M10-01 记录（E2E 全 12 旅程 + 覆盖率门禁）

### 运行结果（本机，3 次连续全绿）

```
pnpm test:e2e   → 12 passed（约 27s/次，本地 1 worker）
pnpm test:coverage
  Statements   : 95.85%  (1550/1617)   [阈值 80]
  Branches     : 90.95%  ( 995/1094)   [阈值 70]
  Functions    : 98.67%  ( 594/602)    [阈值 80]
  Lines        : 97.66%  (1340/1372)   [阈值 80]
  （services 99.12 / stores 96.61 行覆盖；services/stores 之外不计入门禁）
```

覆盖率门禁在 `vitest.config.ts` 中只约束 `src/services/**` 与 `src/stores/**`（
docs/testing.md §3 L1 门槛），通过 `pnpm test:coverage` 独立执行；`pnpm verify`
不跑覆盖率（保持 CI 快），e2e.yml 亦不跑单测——职责分离，均记录于此。

### 基础设施决策（M10 记录）

1. **Tauri IPC shim**：浏览器（vite dev + `VITE_TRANSPORT=fetch`）没有 invoke/
   Channel/事件桥，`tests/e2e/tauri-shim.js` 以 `addInitScript` 注入
   `window.__TAURI_INTERNALS__`，实现服务注册表（内存）、健康监控（1.5s 轮询
   /global/health）、SSE（EventSource→Channel）、PTY 通道（客户端回显）与
   `plugin:event|listen`。测试通过 `window.__TAURI_SHIM__` 读遥测。
2. **E03 发消息与流式渲染分离**：mock 的 `prompt_async` 答 204 且不触发 SSE；
   UI 订阅 `/event` 默认重放 happy-chat。旅程 = 乐观气泡 + 204 往返（发），
   + 场景重放渲染 text/tool（流式），中途释放被 hold 的流以控制时序。
3. **场景选择**：UI 不携带 `?scenario=`，旅程用 `page.route` 改写 /event URL
   选择 permission-flow / question-flow / sse-drop；`holdEventStream` 按住首个
   /event 请求直到旅程就绪再放行（timeline 从放行时刻起播）。
4. **`?syncDelay=`（mock 扩展，docs/api-coverage.md §5）**：`server.connected`
   会触发客户端全量 re-sync，其快照替换会清掉 sync 期间到达的 t=0 事件（
   happy-chat 的 `session.created` 与 re-sync 竞争，E2E 首跑暴露）。shim 在
   SSE URL 追加 `syncDelay=250`，让场景在 re-sync 落定后开播。
5. **E12 断网→重连**：`scenario=sse-drop` 播放 3 个事件后断流；EventSource
   自动重连，mock 按连接重放场景 → 再次 `server.connected` → 重置 + re-sync
   （状态自动对齐）。健康腿用 Playwright route 掐断 /global/health → 状态点
   变红 → 恢复 → 变绿；`session.created` 在重连后仍对齐。
6. **tauri-driver 壳级用例（WebdriverIO）**：环境无 tauri-driver（本机未安装
   WebDriver 基础设施，桌面壳级验证归 M10-02/03 的打包首启走查），记为
   文档级条目而非 CI 步骤；e2e.yml 只跑 Playwright 12 旅程。
7. **app bug（E2E 首跑发现并修复）**：`stores/events.ts` 的 `server.connected`
   处理器原会重置 project store 桶，DesktopShell 的按目录订阅 effect 跟踪
   `current`——重置→effect 重建流→新流再发 `server.connected`→再重置，形成
   无限重订阅循环（~70 次/秒）。移除 project 桶重置（syncAll 本就整体重放
   project 列表与 current），1 个连接稳定。L2 测试 mock 了订阅层，未覆盖该
   路径。另记录：re-sync 的整表替换与同步窗口内事件的竞态在真实服务器上
   同样存在（影响面小，事件在后续刷新中自愈）。

---

## 附录：TASK-M10-02 记录（桌面三平台打包与签名）

### 落地内容

1. **bundle 配置（src-tauri/tauri.conf.json）**：`targets` 由 `"all"` 改为
   显式清单 `["app", "dmg", "nsis", "msi", "deb", "appimage"]`（各平台只构建
   自己子集；Linux 不再产出 rpm——按规格仅 deb + AppImage）；
   `createUpdaterArtifacts: true`——每次构建产出签名 updater 产物（macOS
   `.app.tar.gz` + `.sig`、Windows 安装包旁 `.sig`），供 M8-09 updater 消费
   （latest.json 由 M10-04 发布流水线组装）；`windows.nsis`
   （installMode currentUser）+ `windows.wix`（msi，language en-US）；
   `linux.deb.depends`（libwebkit2gtk-4.1-0 / libayatana-appindicator3-1 /
   librsvg2-2）；macOS 保持 hardenedRuntime 默认（签名/公证走环境变量，见下）。
   注意：Tauri 2 schema 中 MSI 的配置键是 `windows.wix`（target 名仍为
   `msi`），非 `windows.msi`——按真实 schema 落地。
2. **desktop.yml 全量流水线**（M10-02 接管 M0-07 骨架）：矩阵 macos-14
   `--target universal-apple-darwin`（app+dmg，rust targets 含双架构）、
   windows-latest（x86_64，nsis+msi）、ubuntu-22.04（deb+AppImage，完整
   Tauri v2 Linux 依赖：libwebkit2gtk-4.1-dev、build-essential、curl、wget、
   file、libxdo-dev、libssl-dev、libayatana-appindicator3-dev、librsvg2-dev、
   patchelf）；条件签名——macOS 仅在 APPLE_* secrets 存在时导入证书到临时
   keychain 并签名+公证（否则 unsigned）；updater 密钥缺失时用
   `tauri signer generate --ci` 生成一次性 CI 密钥保证未签名验证构建全绿
   （其产物不得发布）；产物按平台上传 artifact。
3. **docs/release-signing.md（新文档）**：三平台签名路线——macOS Developer
   ID 证书 + 公证环境变量（APPLE_CERTIFICATE/APPLE_CERTIFICATE_PASSWORD/
   APPLE_SIGNING_IDENTITY/APPLE_ID/APPLE_PASSWORD/APPLE_TEAM_ID）与
   `spctl`/`stapler` 验证命令；Windows 可选 Authenticode（本地
   certificateThumbprint 或 CI 用 `signCommand` 扩展点，默认 unsigned）；
   Linux 默认 unsigned + 可选 GPG；updater 密钥引用（M8-09 密钥对，绝不入库）。
4. **图标核查**：scaffold 图标集（32x32/128x128/128x128@2x/icon.icns/
   icon.ico/icon.png + Square*/StoreLogo 移动集，Tauri v2 默认图标）已覆盖
   三平台 icns/ico/png 全部尺寸，原样保留（注意：无 icon.svg——桌面端不要求）。
5. **本地验证（macOS arm64，如实记录）**：
   - `pnpm tauri build --ci --bundles app,dmg`（带
     `TAURI_SIGNING_PRIVATE_KEY`）→ `opencoder.app` + `opencoder_0.1.0_aarch64.dmg`
     + updater `.tar.gz` + `.sig`；`.sig` 头部 key-id 与 tauri.conf.json 的
     pubkey 匹配（同一密钥对，`tauri signer sign` 空密码可用——M8-09 生成的
     密钥无密码，文件头 `rsign encrypted` 是 minisign 默认格式）。
   - Info.plist 核对：bundle id `com.opencode.opencoder`、版本 0.1.0、
     LSMinimumSystemVersion 10.13；`codesign -dv` 为 thin arm64、未签名
     （本机无 Apple 证书，符合文档预期）。
   - 首启走查：`open` 启动 bundle 内 app → 主进程存活（13s+ 观察）；退出用
     `pkill`（AppleScript quit 不被处理——与 close-to-tray 设计一致，属应用
     行为非打包问题）；无视觉交互（headless 会话），按任务说明记录步骤。
   - universal 构建 ✓：先 `rustup target add x86_64-apple-darwin`（下载较慢，
     ~4 min），随后 `pnpm tauri build --ci --target universal-apple-darwin
     --bundles app,dmg` 成功——`lipo -info` 确认 fat binary（x86_64 + arm64），
     产出 `opencoder_0.1.0_universal.dmg`（16.4MB）+ updater `.tar.gz` + `.sig`
     （本机耗时约 18 min，含双架构全量编译）。
6. **本地构建前提（环境修复，非仓库改动）**：Homebrew rust 在 PATH 首位且
   缺 `rust-objcopy`（icu_*/zmij 等 build script 需要），导致
   `could not compile ... (build script)`——已 `rustup component add
   llvm-tools-preview` 并将 rustup 工具链
   （`~/.rustup/toolchains/stable-aarch64-apple-darwin/bin`）置于 PATH 前。
   `createUpdaterArtifacts: true` 后未设 `TAURI_SIGNING_PRIVATE_KEY` 的构建
   会在 bundle 阶段快速失败（"A public key has been found, but no private
   key"）——CI 的一次性密钥步骤即为此设计。

### 验收对照

- macOS 本机 ✓（app+dmg+updater 产物、首启进程存活；签名/公证待 Apple 证书，
  文档化）；Windows/Linux 安装包按验收注明为 CI 验证 + 文档化
  （本机无 Windows/Linux 环境）。

---

## 附录：TASK-M10-03 记录（移动端发布包构建）

### 落地内容

1. **iOS release（simulator，unsigned）本地验证 ✓**：
   `pnpm tauri ios build --target aarch64-sim --ci --no-sign`（release 为
   `tauri ios build` 默认模式，`--debug` 才是反向开关；`--release` 参数不存在，
   M10-03 首个命令即踩到并已记录于此）→ 产物
   `src-tauri/gen/apple/build/arm64-sim/opencoder.app`（12MB，
   `opencoder` 可执行文件 12.7MB，Mach-O 64-bit arm64，release 无调试膨胀；
   Info.plist `com.opencode.opencoder` / 0.1.0 / NSCameraUsageDescription 齐备）。
   模拟器实测：`simctl install` ✓ → `simctl launch` ✓（PID 5601）→ 截图渲染
   正常（159KB，壳级）。发布构建暴露一个真实 bug：
   **`tauri-plugin-window-state` 2.4.1 是全 crate `#![cfg(not(any(target_os = "android", target_os = "ios")))]`**
   （移动端编译为空 crate），而 `src-tauri/src/lib.rs` 的注册未加
   `#[cfg(desktop)]` 门控——M7-01 的 iOS debug 构建早于 M8-04 的 window-state
   落地，桌面里程碑从未编译过移动目标，CI 的移动骨架又有全局
   continue-on-error 兜底，此缺口一直静默。修复：注册改为
   `#[cfg(desktop)] let builder = builder.plugin(tauri_plugin_window_state::Builder::default().build());`
   （与 barcode-scanner/single-instance 同一纪律，注释说明）；该 crates 的
   `Builder` 在移动端不存在。另核实：updater/store 插件在移动端可编译，无需门控。
2. **`tauri ios build` 二次构建 rename 问题根因修正（M7-02 注记纠偏）**：
   tauri-cli 将 archive 内 `Products/Applications/opencoder.app` rename 到
   `build/<target>/opencoder.app`，**目标是旧产物目录时失败**（M10-03 实测
   仅删 archive 仍复现；删整个 `src-tauri/gen/apple/build/` 后通过）。M7.md
   spike 注记已更新为准确修法。
3. **mobile.yml 加固**：移除全局 continue-on-error——`ios` job（simulator
   release，**必过**）为验证链；新增 `ios-device` job（device release
   unsigned，job 级 continue-on-error，注明需 Apple team 才有可安装产物，
   CI 只验证 aarch64-apple-ios 工具链）；`android` job：显式
   `tauri android init --ci`（gen/android 未入库，CI 现场脚手架）+ debug
   构建（**必过**，debug keystore 自动签名）+ release 构建（step 级
   continue-on-error——无 keystore 必然失败，按规格文档化不阻塞）。
4. **docs/mobile-signing.md 扩充（release 章节）**：iOS 设备签名路径
   （Xcode 工程 `DEVELOPMENT_TEAM` 是唯一设置点——M7-01 占位的
   `tauri.ios.conf.json` `ios.teamId` 经 tauri 2.11.4 config schema 核实
   **不存在**，已更正；CI 侧 tauri-cli 2.11 支持 `IOS_CERTIFICATE` /
   `IOS_CERTIFICATE_PASSWORD` / `IOS_MOBILE_PROVISION` 环境变量自动配置
   签名）；release 构建模式表（simulator/device-unsigned/App Store 三行，
   命令 + 签名 + 产物路径）；App Store Connect 上架清单（截图尺寸：
   iPhone 6.7" 1290×2796 / 6.5" 1284×2778 / 5.5" 1242×2208、iPad 12.9"
   2048×2732 可选；隐私清单 PrivacyInfo.xcprivacy——本 app 无追踪、无
   第三方 SDK、相机用途已由 NSCameraUsageDescription 声明，决策：**暂不
   添加清单文件**，首次提审前按清单项评估（UserDefaults 等 required-reason
   API 若被 Rust/插件栈触碰再补最小清单 + project.yml 同步）；年龄分级
   ——回答问卷，含用户生成内容需如实勾选审核/举报能力，预期 4+；隐私
   「营养标签」；TestFlight 内部测试先于正式发布）；Android keystore
   （keytool 命令 + Gradle `signingConfigs` 模板（env 变量注入，绝不入库）
   待 gen/android 落地后应用；release 无 keystore 必然失败已文档化）；
   **权限最小化**章节：仅相机（扫码），iOS 仅 NSCameraUsageDescription，
   Android 仅 CAMERA（待脚手架，M7-08 注记），无定位/相册/麦克风等。
5. **环境状态表更新**：本地无签名（simulator release unsigned ✓ 已验证）；
   Android 本地仍 env-blocked（SDK/JDK 缺失，gen/android 未生成），全部
   交由 CI（init + debug 必过 + release best-effort）。
6. **iOS device release（unsigned）本地验证 ✓**：`pnpm tauri ios build
   --target aarch64 --ci --no-sign` 全量编译 aarch64-apple-ios release
   依赖后成功，产物 `src-tauri/gen/apple/build/arm64/opencoder.ipa`
   （6.5MB）。`--no-sign` 下无需 team 即可产出 ipa——`codesign -dv`
   确认「code object is not signed at all」、无 `embedded.mobileprovision`，
   即不可真机安装（符合文档预期；真机安装需 team + 描述文件，走
   TestFlight/App Store 路径）。结论：CI 的 `ios-device` job（同一命令）
   预期可过——device 工具链验证成立。

### 验证

- iOS simulator release 构建 ✓（产物路径 + 大小 + 模拟器安装启动截图）
- iOS device release（unsigned）✓（`opencoder.ipa` 6.5MB，未签名确认；
  CI best-effort job 同命令预期通过）
- Android：本机 env-blocked → CI（init + debug 必过 + release best-effort）
- `pnpm verify` 全绿（详见下方提交时的实测输出）

### 遗留

- 真机安装主流程（验收项「release 包真机安装主流程过」）：iOS 需签名
  team + 描述文件（文档化，TestFlight 路径）；Android 需本机 SDK +
  keystore（文档化）——按任务验收口径记为「文档化 + CI best-effort」。
- App Store Connect 清单（截图/隐私清单/年龄分级）为提审时 checklist，
  截图需在装有 Xcode 模拟器的 macOS 机器上补拍（本机 headless 无法产出
  真机截图）。

---

## 附录：TASK-M10-04 记录（发布流水线）

### 落地内容

1. **三个发布脚本**（`scripts/`，全部英文注释）：
   - `check-version.mjs <version>`：校验 tag 推导版本（接受 `v` 前缀）与
     package.json / `src-tauri/tauri.conf.json`（JSON `version` 字段）/
     `src-tauri/Cargo.toml`（首个 `version = "..."` 行）三处版本一致，
     任一不一致 exit 1 并列出全部不匹配项；release.yml 在任何构建前执行。
   - `release-notes.mjs <version> [--out file] [--changelog file]`：从
     CHANGELOG.md 提取 `## [X.Y.Z]` 版本段（支持 `## [1.0.0] - 日期` 与
     link 式标题，截到下一个 `## ` 二级标题）；版本段尚不存在时回退
     `[Unreleased]` 段并打印警告（当前仓库即此状态，dry-run 走此路径；
     M10-06 归集为 `## [1.0.0]` 后自动切到正式段）；两段皆无或文件缺失
     时干净报错 exit 1。
   - `gen-latest-json.mjs <assets-dir> <tag> [--notes-file] [--require
     os-arch,...] [--out] [--repo]`：扫描资产目录里的 updater 产物
     （签名 + 对应资产），组装 M8-09 updater 消费的 latest.json——
     `{ version, notes, pub_date, platforms: { "<os>-<arch>": { signature,
     url } } }`。URL 为**公开** `https://github.com/<owner>/<repo>/
     releases/download/<tag>/<asset>`（repo 默认从 tauri.conf.json 的
     updater endpoint 解析，`--repo` 可覆盖）。平台映射**镜像 tauri-action
     的 uploadUpdaterJson**（逐行核对源码的参考实现）：签名按后缀优先级
     排序——darwin 仅 `*.app.tar.gz.sig`；windows `-setup.nsis.zip.sig` >
     `*.msi.zip.sig` > `-setup.exe.sig` > `*.msi.sig`（zip 变体取旧格式主
     键，裸安装包取 `{os}-{arch}-{bundle}` 键，与 action 循环写入顺序一
     致）；linux `*.AppImage.tar.gz.sig` > `*.AppImage.sig` > `*.deb.sig`；
     universal macOS 产物同时写 darwin-aarch64 / darwin-x86_64 两个主键
     与两个 `-app` 键。`--require` 缺平台 exit 1（finalize 用它兼作产物
     校验）。

2. **`.github/workflows/release.yml`**（触发：`v*` tag push +
   `workflow_dispatch`，`dry-run` input 默认 true；`permissions:
   contents: write`）：
   - `version-check` job：解析 tag（tag push 用 `github.ref_name`，manual
     用 package.json 版本拼 `v` 前缀）→ `check-version.mjs` →
     `release-notes.mjs --out release-notes.md` → `gh release create
     --draft --title "opencoder <tag>" --notes-file release-notes.md`
     （tag 已有 release 则跳过，支持重跑）。
   - `build` 矩阵（macos-14 universal app+dmg / windows-latest x64
     nsis+msi / ubuntu-22.04 x64 deb+AppImage；Linux 依赖清单、Apple
     证书导入、rust targets 与 desktop.yml 完全一致）：每 job 用
     `tauri-apps/tauri-action@v0` 构建并上传——`tagName` 为解析出的 tag，
     `args: --ci --target <t> --bundles <b>`；tauri-action 按 tag 查找
     已存在的草稿 release（draft 模式走全部 release 列表检索，找到后
     **不修改**其 name/body，仅追加本 job 产物），未找到才创建（
     `releaseName` 作兜底）；`uploadUpdaterJson: false`。dry-run 时跳过
     tauri-action，改为 upload-artifact（同 desktop.yml）。
   - `finalize` job（非 dry-run，依赖全部构建）：`gh release download`
     下载 updater 产物（`*.sig` / `*.tar.gz` / `*.zip` / `*.AppImage` /
     `*.deb`）→ 重新提取 notes → `gen-latest-json.mjs assets <tag>
     --notes-file release-notes.md --require darwin-aarch64,
     darwin-x86_64,windows-x86_64,linux-x86_64` → `gh release upload <tag>
     latest.json --clobber`。
   - **签名门禁**：tag 构建（非 dry-run）缺 `TAURI_SIGNING_PRIVATE_KEY`
     时快速失败并给出指引——updater 签名必须匹配 app 内置公钥（
     tauri.conf.json `plugins.updater.pubkey`），一次性 CI 密钥的产物
     **不得发布**（desktop.yml 的该步骤只为验证构建服务）；仅 dry-run
     允许生成一次性 CI 密钥保证构建全绿。

3. **决策记录**：
   - Release 始终为 **draft**：发布是 M10-06 的显式收尾动作；draft 产物
     公开不可下载，updater 端点（`.../releases/latest/download/
     latest.json` 的 latest 指向非 draft 最新版）在发布前不可用——符合
     「先审后发」。
   - latest.json 由自研脚本生成而非 tauri-action 原生
     `uploadUpdaterJson`（已读源码确认其写 `api.github.com/.../
     releases/assets/{id}` URL——未认证限速 60 次/时且依赖 auth，不适合
     updater 客户端直接拉取）；公开下载 URL 无需认证、走 CDN。
   - 构建步骤与 desktop.yml 有意重复：GitHub Actions 无法跨工作流文件
     复用 steps（除非 composite action）；发布矩阵的目标是 draft release
     而非 artifact，二者职责不同。
   - dry-run 默认 true：本机无法真实推 tag（验收为文档化 + 本地证据），
     manual 触发 = CI 侧全矩阵构建验证 + 版本/notes 脚本实跑，不触碰
     真实 Release。

### 验证

- 脚本本地测试（见下，均在当前仓库状态上实测）：
  - `check-version`：`0.1.0` 与 `v0.1.0` → OK（3 源全匹配）；`1.0.0` →
    exit 1 列出三处不匹配。
  - `release-notes`：伪造 changelog 精确提取 `## [1.0.0]` 段（link 式
    标题、跨 `## [0.9.0]` 边界截断）；当前仓库（无版本段）→ `[Unreleased]`
    回退成功且内容含 M10-03 条目；两段皆无 → exit 1；文件缺失 → 干净报错。
  - `gen-latest-json`：全平台 fixture（真实 macOS 本地产物 + 伪造
    win/linux 签名）→ 四主键 + 各 bundle 键、nsis.zip/AppImage.tar.gz
    优先、universal 双 darwin 键（真实签名文本）、公开 URL、repo 自动从
    tauri.conf.json 解析；`--require` 缺平台 → exit 1；空目录 → exit 1；
    无参数 → usage exit 2；真实本地 `bundle/macos` 目录 → 仅
    darwin-aarch64/darwin-x86_64（+ `-app` 键）。
- `pnpm verify` 10/10 全绿（提交时实测）。
- dry-run 全流程验收（本机无法推 tag，按任务口径记录）：workflow 正确性
  经逐行核对 tauri-action 源码（`create-release.ts` 的 draft 查找/不修改
  语义、`index.ts` 的 `body_path` 未用、`upload-version-json.ts` 的平台
  映射与优先级）+ 上述脚本本地测试 + M10-02 的真实本地产物（updater
  tar.gz + sig 直接作为 fixture 输入）作为构建证据。

### 遗留

- 真实 tag 触发的全流程（含签名/公证、draft → 发布）需在 GitHub 仓库
  首次执行后回填结果；发布动作归 M10-06。
- `release.yml` 与 desktop.yml 的构建矩阵重复（无 composite action 时的
  取舍）；若后续引入 composite action 可收敛为共享步骤。
- Windows/Linux 的 updater 产物组合（nsis.zip/msi.zip/AppImage.tar.gz
  是否存在）以实际 CI 构建为准，gen-latest-json 的优先级表已覆盖两种
  情形。

---

## 附录：v1.0.0 回归 Checklist（M10-06 执行）

- [ ] 五平台安装启动
- [ ] 添加服务器（手动/mDNS/扫码三通道）
- [ ] 健康检查与断网重连
- [ ] 会话全流程（发消息/流式/中断/权限/问题/命令/fork/revert/share）
- [ ] 文件树/搜索/diff/VCS
- [ ] 终端（桌面+移动）
- [ ] iOS 26 Liquid Glass 真机确认
- [ ] 萌宠三桌面平台行为
- [ ] 双语切换 + 双主题
- [ ] 快捷键/右键菜单/命令面板（桌面）
- [ ] 手势/Sheet/触觉（移动）

---

## 附录：TASK-M10-05 截图复现管线（M10-05 修复记录）

截图资产在 `docs/screenshots/`，全部为真实 UI 驱动 Mock OpenCode Server
（dev transport）或 iOS 模拟器实拍；一键式「再截图」脚本**有意不入库**
（一次性 Playwright 脚本，避免把测试基建写成维护负担），本附录记录完整
复现步骤。

### 桌面 4 张（1280×800 @2x → 2560×1600 PNG）

1. 起服务（与 playwright.config.ts 的 webServer 一致）：
   ```bash
   pnpm mock:start --cors --port 14096
   VITE_TRANSPORT=fetch VITE_MOCK_BASE_URL=http://localhost:14096 pnpm dev -- --port 1420
   ```
2. 一次性 Playwright 脚本（/tmp 下临时文件，不提交），要点：
   - `chromium.launch()` + `viewport { width: 1280, height: 800 }` +
     `deviceScaleFactor: 2`（正好产出 2560×1600）；
   - `page.addInitScript(readFileSync("tests/e2e/tauri-shim.js"))` ——
     Tauri IPC shim 注入（服务器注册表/健康监控/SSE 全走内存实现）；
   - 各截图的旅程 = E2E 旅程（fixtures.ts 的 addServer/enterWorkspace）：
     - `desktop-home.png`：addServer 后留在服务器主页（卡片 + 健康徽标）；
     - `desktop-chat.png`：进入工作区打开会话（`sess_01`，happy-chat 流式）；
     - `desktop-files.png`：侧栏切到 Files（`sidebar-view-files`），展开
       `src/features/sessions` 打开 `PromptBox.tsx`，等 `viewer-code`
       非空（shiki 高亮异步到达）再拍；
     - `desktop-dark-chat.png`：同 chat，截图前设 `localStorage.oc-theme=dark`
       （或经设置界面切深色）。
   - `page.screenshot({ path: "docs/screenshots/<名>.png" })`。
3. 像素校验（不靠肉眼）：PIL 统计全图唯一颜色数 + 区域方差，聊天/文件
   图应 ≥3000 种唯一色（本次实测：chat 3145 / dark-chat 3938 / files 3660），
   代码区饱和色像素（shiki token）数千量级；纯空态/白屏会低一个数量级。

### iOS 2 张（iPhone 17 模拟器 / iOS 26.0，1206×2622）

- `ios-glass.png`：归档的 M7-02 Liquid Glass 演示图，非本次重拍。
- `ios-home.png`（M10-05 修复重拍）：服务器主页，应用已连宿主机上的
  Mock OpenCode Server（模拟器与宿主共享网络，`127.0.0.1:14096` 直达）。
  复现步骤：
  1. 构建并安装（注意 M10-02/03 记录的两个环境坑）：
     ```bash
     # rustup 工具链放 PATH 前（Homebrew rust 缺 rust-objcopy 会编不过 build script）
     PATH="$(rustup which rustc | xargs dirname):$PATH" \
       pnpm tauri ios build --target aarch64-sim --ci --no-sign
     rm -rf src-tauri/gen/apple/build   # rename "Directory not empty" 旧坑
     xcrun simctl install booted src-tauri/gen/apple/build/arm64-sim/opencoder.app
     ```
  2. 起 mock（宿主）：`pnpm mock:start --cors --port 14096`。
  3. 向应用数据容器种子 `servers.json`（store 插件路径 =
     `$HOME/Library/Application Support/<bundleId>/servers.json`，键为
     **camelCase**：`id/name/url/username/password/createdAt/
     lastConnectedAt`；容器路径用
     `xcrun simctl get_app_container booted com.opencode.opencoder data`）：
     ```json
     {"servers":[{"id":"srv_<millis-hex>_0","name":"Mock Server",
       "url":"http://127.0.0.1:14096","username":null,"password":null,
       "createdAt":1785920286926,"lastConnectedAt":null}]}
     ```
  4. `xcrun simctl launch booted com.opencode.opencoder`，等 ≥35s（健康
     轮询 15s/次）后 `xcrun simctl io booted screenshot` → 覆盖
     `docs/screenshots/ios-home.png`。
  5. 校验：OCR（macOS Vision，一次性 swift 脚本）+ 唯一色数；卡片区应
     出现 `Mock Server / 1.18.11-mock • N ms`。

**本次修复暴露的真实启动崩溃**：iOS（及裸启桌面二进制）凡存在已保存
服务器，启动即 SIGABRT——`connections/health.rs` 在 setup 钩子（主线程、
无 tokio 运行时上下文）里用裸 `tokio::spawn` 派生健康监控任务，必触发
「no reactor running」panic（单测在 `#[tokio::test]` 内运行、历次实机启动
注册表皆空，故从未暴露）。修复：改经 `tauri::async_runtime::spawn`
（内部 `Handle::enter()` 进入运行时上下文），abort 句柄取
`task.inner().abort_handle()`；桌面复测 40s 内 3 次命中
`/global/health`，iOS 重拍截图卡片显示 `1.18.11-mock • 0 ms`。
